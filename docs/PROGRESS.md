# PROGRESS.md — Living Log

> THE single source of truth for what is DONE, IN PROGRESS, and REMAINING.
> - **Cursor**: update your ticket's row in the SAME commit as the work. Work without a log entry = not done.
> - **PM (Claude)**: adds the Verdict column entries during review. Only the PM writes verdicts.
> - **Stakeholder**: re-uploads this file to the Claude Project after every review cycle.
>
> Status legend: ⬜ todo · 🔨 in progress · 🧪 ready for review · ✅ accepted · 🔧 changes requested · ❌ rejected · ⏸ blocked

---

## Current sprint: SPRINT 3 — Internal Dashboard + First Sends

| # | Ticket | Status | Files touched | Cursor notes | PM verdict |
|---|---|---|---|---|---|
| 3.1 | Backend admin API | ✅ | app/routers/admin.py, app/main.py, app/config.py, .env.example, README.md, tests/conftest.py, tests/test_admin.py | **`app/routers/admin.py`** — all four endpoints behind `require_admin_key` (constant-time `secrets.compare_digest`, fails closed if `ADMIN_API_KEY` is unset rather than letting an empty key match an empty header). `GET /leads` filters (status/channel/health_flag/search) + sort (`review_date_asc`/`review_date_desc`/`created_at`, default `review_date_desc` per LOGIC §6's "newest review first") + `limit`/`offset` (200/500 default/max — pagination safety net, not in the ticket text but the lead count only grows; disclosed rather than left unbounded). `GET /leads/{id}` returns the full place+review join, 404 if missing. `GET /stats` returns counts for every status (zero-filled, not just the ones present), `sent_today`/`sent_by_channel`/`replies`; the Europe/Warsaw day-boundary helper (`warsaw_today_utc_bounds`) is written now so ticket 3.4's 20/day cap reuses the exact same "today" instead of two definitions drifting apart. **`PATCH /leads/{id}`** — `ALLOWED_TRANSITIONS` is the LOGIC §3 diagram as a literal adjacency dict; a same-status PATCH is always a no-op (skips validation entirely, so it can't re-stamp `sent_at`); anything else not a listed edge is 422 with the specific from/to states named. `sent` requires a channel already on the lead or provided in the same body; health-flagged leads need `confirm_health_reviewed: true` in that exact request to enter `queued`/`sent` (LOGIC §2/§6) — it's a request-time gate only, not a stored column, so it must be resent on every such PATCH; `confirm_health_reviewed` matches nothing else that transition might touch. Editable fields (`notes`, `generated_response`, `outreach_message`, `channel`) apply independently of any status change. **CORS** via `CORSMiddleware` in `app/main.py`, `allow_origins=[APP_ORIGIN]` (default `http://localhost:3000`) — live-verified: an `Origin: http://evil.example` preflight gets `400`, the configured origin gets `200`. **Housekeeping while touching `.env.example`:** it was missing `REPLY_ADDRESS` from ticket 2.4 — added, plus the two new ticket-3.1 vars. **Tests:** new `tests/conftest.py` introduces an in-memory SQLite `db_session` fixture (`StaticPool` so the same in-memory DB persists across the session) with `get_session` dependency-overridden on the app — a first for this codebase; every prior job test mocks `SessionLocal`, but the admin router's value is almost entirely in its SQL (joins/filters/sorts/grouped counts), so a mock would test the mock. **Live-verified against the real DB** (not just the test suite): `GET /stats` against the live 213-lead table returned real counts; `GET /leads/999999` → 404; missing/wrong key → 401 through an actual running `uvicorn` process, not just `TestClient`. <br><br>**§3 amendment applied (2026-08-06, PM-approved, raised by this ticket's own disclosed gap):** the original diagram only modeled `dead` reachable from `sent`/`replied` — ticket 3.3's planned "Skip → dead" action needs it from earlier states too, which was flagged rather than silently widened. LOGIC.md §3 now reads "dead reachable from ANY status except converted; pre-sent skips require a note; dead is terminal", and `ALLOWED_TRANSITIONS` was rebuilt to match exactly: every status except `converted` and `dead` itself gained a `dead` edge; `converted` and `dead` still have zero outgoing edges (so `converted → dead` correctly 422s, and `dead` accepts nothing, including re-entering `new`). New `_PRE_SENT_STATUSES = {new, response_generated, enriched, queued}` gate: a skip to `dead` from one of these now 422s unless the PATCH body's own `notes` field is a non-empty string. **Interpretation call, disclosed:** the note must be supplied *in that specific request*, not merely already sitting on the lead (mirrors ticket 3.3's wording, "Skip... requires a note", i.e. writing one is part of doing the skip) — an existing unrelated note (e.g. a stale `HEALTH_FLAG` marker) does not satisfy it; a test asserts this explicitly. `sent`/`replied → dead` need no note (LOGIC's own text: those already carry an implicit reason — no reply after 14 days, or a negative reply). 10 new tests (parametrized over all 4 pre-sent statuses + both post-send statuses, blank-note rejection, stale-note rejection, `converted → dead` still 422, `dead` terminal). 184/184 tests pass, `ruff` clean. | ✅ ACCEPTED (PM, 2026-08-06) — fail-closed key auth, request-time health gate, shared day-boundary helper, real-DB test fixture all specifically approved. Open question resolved via LOGIC §3 amendment. |
| 3.2 | App repo bootstrap (/admin basic auth) | ✅ | (new repo) reviewguide-app: app/page.tsx, app/layout.tsx, app/globals.css, app/admin/page.tsx, lib/api.ts, middleware.ts, netlify.toml, .env.example, .gitignore, README.md, package.json | New sibling repo pushed: https://github.com/pedram-kh/reviewguide-app (main branch). Next.js 16 (App Router, TS, Tailwind v4), scaffolded via `create-next-app`, boilerplate (AGENTS.md/CLAUDE.md/demo SVGs) stripped. `/` is a minimal "ReviewGuide" placeholder. **`middleware.ts`** — Basic Auth on `/admin` + `/admin/:path*` via `ADMIN_USER`/`ADMIN_PASS`; fails closed if either env var is unset; credential compare is a constant-time-ish char-XOR loop (Edge/Node-portable, no `crypto` module needed) — not asked for explicitly by the ticket text but cheap and consistent with the backend's own auth posture (ticket 3.1), disclosing rather than silently skipping it. Next.js 16.3 prints a deprecation notice nudging `middleware.ts` → `proxy.ts`; kept the ticket's literal filename since the file still works and migrating is a mechanical, separate change. **`lib/api.ts`** — the only place that calls the backend; wrapped in `import "server-only"` so an accidental Client Component import fails the *build*, not just a lint rule. `getStats()` calls `GET /api/admin/stats` with `X-Admin-Key: ADMIN_API_KEY` (server env only, `cache: "no-store"`). **`/admin`** — Server Component (`export const dynamic = "force-dynamic"`) renders 8 status cards (LOGIC §3 order) + sent-today/replies/sent-by-channel cards from live `getStats()`; renders a plain error state instead of crashing if the backend is unreachable. **Verified the key doesn't leak (ticket's explicit ask):** built with `ADMIN_API_KEY=test-secret-key-do-not-leak`, then `grep -R` for both the literal value and the string `ADMIN_API_KEY` across the entire `.next/` output — zero matches in `.next/static` (the browser bundle); for contrast, the same string *does* appear in `.next/server/...` chunks (middleware + SSR), which never ship to the browser. Two independent guards, not just a convention: `server-only`'s build-time failure, and no `NEXT_PUBLIC_*` prefix. **Bug caught while staging:** `create-next-app`'s default `.gitignore` has a bare `.env*` pattern, which silently swallowed `.env.example` too — added `!.env.example` before the first commit so the template file (with no real secrets) actually got tracked; confirmed via `git check-ignore -v`. `npm run lint` and `next build` (which runs its own TypeScript check) both clean. **Not done yet, disclosed:** the Netlify site itself doesn't exist — still a Stakeholder action per the ticket ("create the Netlify site linked to it") and the open Sprint 3 question below; so `netlify.toml`/README deploy steps are written and the app builds locally, but there's no live Netlify URL to verify against yet. <br><br>**Deployment wiring complete, chain verified, 2026-08-06.** Netlify site `dynamic-puppy-631956` (created by Stakeholder) linked via `netlify link`. New random production credentials generated locally (`ADMIN_USER=pedram`, 20-char `ADMIN_PASS`, `openssl rand -hex 24` `ADMIN_API_KEY`) and written to `reviewguide-app/.env` (gitignored, confirmed via `git check-ignore -v`) for the Stakeholder to read into a password manager. Set on Netlify via `netlify env:set`; `ADMIN_PASS`/`ADMIN_API_KEY` use `--secret` (write-only, can't be read back via CLI/UI once set) scoped to the `production` context. Backend App Runner service (`reviewpilot-backend`) updated via `aws apprunner update-service` with the existing env vars (`DATABASE_URL`, `OUTSCRAPER_API_KEY`, `ANTHROPIC_API_KEY`) preserved unchanged and three added: `ADMIN_API_KEY` (matching the app's), `APP_ORIGIN=https://dynamic-puppy-631956.netlify.app`, and `REPLY_ADDRESS` — the last one flagged mid-task and added with Stakeholder sign-off: it was documented in the README as a required App Runner var (ticket 2.4) but had only ever made it into local `.env`, never the live service; now fixed. Service reached `RUNNING` cleanly. **Two real bugs found and fixed live, not anticipated by the ticket:** (1) `netlify env:set` echoes the plaintext value back in its own CLI confirmation — the first `ADMIN_PASS`/`ADMIN_API_KEY` set leaked into command output; treated as compromised immediately, both fully unset and regenerated, then re-set with `--secret --context production` (which doesn't echo). (2) the Netlify site had its own platform-level SSO/visitor-access gate enabled (`sso_login: true`, both site- and account-level) that intercepted *every* request — including the intentionally-public `/` — before our own `middleware.ts` ever ran; found via a `WWW-Authenticate`-less 401 with an unfamiliar body (`app.netlify.com/edge-access` login redirect) that didn't match `middleware.ts`'s actual unauthorized response, disabled via `netlify api updateSite` (`sso_login: false`, this site only). Production redeploy via `netlify deploy --build --prod`. **Full chain live-verified:** `/` → 200 (public); `/admin` no creds → 401 with the correct `WWW-Authenticate: Basic realm="ReviewGuide Admin"` header (confirms it's genuinely our middleware, not a fallback page); `/admin` with the real generated credentials → 200, dashboard renders live counts (`new: 173, enriched: 39, sent_today: 0, reply_rate: 0.0%`); direct `GET /api/admin/stats` with the real `X-Admin-Key` → 200, numbers match the dashboard exactly; CORS preflight from `APP_ORIGIN` → `200` with the correct `access-control-allow-origin`, the same preflight from an unrelated origin → `400` with no allow-origin header. All secret values were kept out of command text and command output throughout (variable references, `--secret`, output redirected to files/discarded, only first-4-chars previews and non-sensitive metadata printed); temp files holding the backend's existing secrets were deleted after use. | ✅ ACCEPTED (PM, 2026-08-06) — key-leak grep verification confirmed (static/ clean, server/ only); .env.example gitignore fix and constant-time middleware comparison approved. Deployment wiring ✅ (PM, 2026-08-06) — secret rotation after CLI echo leak specifically commended; SSO-gate conflict found and resolved via live verification. |
| 3.3 | Lead workspace UI | ✅ | reviewguide-app: app/admin/leads/page.tsx, app/admin/leads/LeadsFilterBar.tsx, app/admin/leads/[id]/page.tsx, app/admin/leads/[id]/LeadDetailClient.tsx, app/api/leads/[id]/route.ts, lib/api.ts, lib/leadTransitions.ts, lib/format.ts, app/admin/page.tsx | **`/admin/leads`** — filters (status, channel, health flag: any/flagged/not-flagged, name search) + sort (default `review_date_desc` per LOGIC §6 "newest review first"), all as plain URL query params driven by a small client-side filter bar (`LeadsFilterBar.tsx`) so the list itself stays a Server Component reading `GET /api/admin/leads` directly — no client-side data-fetching needed for the list at all. Health-flagged rows show a ⚠️ badge. Rows are real `<a>` links (no onClick hacks) to the detail page, carrying the exact current filter/sort querystring through a `from` param so "Mark sent"/"Skip" can return to the same view (ticket's "return to list, filtered as before"). **`/admin/leads/{id}`** — read-only review block; `generated_response`/`outreach_message` are independent editable textareas each with their own Save button (PATCH); Copy message copies the current `outreach_message` textarea value to the clipboard; notes field autosaves on an 800ms debounce; contact buttons render only for fields the backend actually has (fb_url/mailto/tel/website), each opening in a new tab. **Mark sent** — channel `<select>` prefilled from `lead.channel`; health-flagged leads show an amber warning banner and require an "I reviewed this response" checkbox before the button is enabled, which sends `confirm_health_reviewed: true` (LOGIC §2/§6). **Skip → dead** — clicking it opens an inline textarea (pre-filled with any existing notes) that must be non-empty before "Confirm skip" is enabled; the PATCH always includes `notes` in the same request as `status: "dead"`, matching the backend's requirement that the note be supplied *in that request* rather than merely already stored (see ticket 3.1's amendment notes) — the confirmation UI is shown for every skip regardless of pre/post-send status, which is stricter than the backend strictly requires (post-send skips don't need one) but never conflicts with it. **Client-side transition guardrails, disclosed as UX-only:** `lib/leadTransitions.ts` mirrors the backend's `ALLOWED_TRANSITIONS` (amended 2026-08-06) purely to disable "Mark sent"/hide "Skip" when the current status makes them illegal — verified this mirror is accurate against the live `app/routers/admin.py` before building on it. The backend re-validates and 422s regardless; nothing here is trusted as the source of truth. **Never-trust-the-frontend enforced structurally, not just by convention:** all PATCHes go through a new `app/api/leads/[id]/route.ts` server route rather than calling `lib/api.ts` from client code — that file is `import "server-only"`, so doing so would fail the *build*. The route forwards the backend's exact HTTP status and `detail` message (e.g. "Skipping a pre-sent lead to 'dead' requires a note explaining why (LOGIC.md §3)") back to the UI so validation errors are never generic. **Live-smoke-tested against the real backend and real RDS data** (not just a local build): started both dev servers, verified Basic Auth returns 401/401/200 for no-auth/wrong-auth/correct-auth, confirmed real lead names (e.g. "SAPKO KEBAB", "Specjały Regionalne...") render in both the list and detail pages, round-tripped a real notes PATCH through the proxy route and reverted it, and confirmed both an illegal transition (`new`→`sent`) and a note-less skip return their exact backend 422 messages through the proxy. Re-ran the ticket-3.2 key-leak grep against the full `.next/` output after adding these routes — still zero matches in `.next/static`. `npm run lint` and `next build` both clean. **Noted, not fought:** `next build`/`next dev` on Next.js 16.3 auto-regenerate `AGENTS.md`/`CLAUDE.md` (a framework feature warning AI agents that this Next version postdates their training data) — committed rather than repeatedly deleted, since the guidance is genuinely relevant to future Cursor sessions in this repo. | ✅ ACCEPTED (PM, 2026-08-06) — server-component filters, non-trusted client guardrails, 422 passthrough, and live illegal-transition smoke tests all specifically approved. |
| 3.4 | Send-day guardrails + tracking | ✅ | app/routers/admin.py, tests/test_admin.py, reviewguide-app: app/admin/layout.tsx, app/admin/page.tsx, app/admin/leads/page.tsx, app/admin/leads/[id]/page.tsx, app/admin/leads/[id]/LeadDetailClient.tsx, app/admin/replies/page.tsx, app/admin/replies/ReplyRow.tsx, lib/api.ts, lib/limits.ts | **Backend:** `MAX_SENDS_PER_DAY = 20` (LOGIC §6); a new `_count_sent_today` helper factors the query both `GET /stats`'s `sent_today` and the new cap check share, so they can't drift on what "today"/"sent" means (reuses ticket 3.1's `warsaw_today_utc_bounds`, as asked). `PATCH` to `status: sent` now checks that count *before* stamping `sent_at` and 429s with a clear message if it's already at 20 — checked only on a real transition into `sent` (a same-status no-op PATCH still skips everything, consistent with 3.1). `StatsResponse` gained `reply_rate` (a 0.0-1.0 fraction, not a %): `replies / total-ever-sent`, `0.0` rather than a division error when nothing's been sent yet; `total-ever-sent` reuses the existing `sent_by_channel` sum rather than a new query. 6 new tests: 429 at exactly 20/day, 200 at 19, a day-boundary test (20 sent *yesterday* doesn't block today), and reply_rate both at zero and as a real fraction. 189/189 tests pass, `ruff` clean. **App:** new `app/admin/layout.tsx` wraps every `/admin/*` page in a shared header — nav (Dashboard/Leads/Replies) + a live "Sent today: N/20" pill (red at cap) — and replaces the one-off nav links ad-libbed in 3.2/3.3's pages (disclosed cleanup, not asked for verbatim but the ticket's own "counter in the /admin header" implied one shared header across pages, not just the dashboard). `MAX_SENDS_PER_DAY` mirrored client-side in `lib/limits.ts` (same pattern as `lib/leadTransitions.ts`) purely for the header/disable-button UX — the backend's 429 is the actual enforcement, this is pre-emptive only. Lead detail page now also fetches `sent_today` and disables "Mark sent" with an explanatory title once the cap is hit, rather than letting the click round-trip to a 429. `/admin` gets a "Reply rate (G2 gate metric)" stat card. **New `/admin/replies`:** lists every lead currently `sent` or `replied` (not just `sent` literally, per the ticket's own listed quick actions — see interpretation note below), sorted by `sent_at` desc, with inline one-click actions. **Interpretation call, disclosed:** the ticket's "lists leads in status=sent with quick actions: Mark replied / Mark converted / Mark dead" is contradictory taken literally — `converted` is only a legal transition from `replied`, never directly from `sent` (LOGIC §3), so a `sent`-only view could never actually offer a working "Mark converted" button. Read the intent as "the post-send tracking view": rows in `sent` offer *Mark replied*/*Mark dead*; rows in `replied` offer *Mark converted*/*Mark dead*. `Mark dead` always prompts for a note in this UI (consistent with 3.3's Skip, even though LOGIC only strictly requires one pre-send). Fetches the `sent`/`replied` lists via 3.1's existing `GET /leads?status=` (no backend schema change — out of this ticket's stated backend scope, which only touches `/stats` + the `sent` PATCH) then a per-row `GET /leads/{id}` for `sent_at` (that endpoint already returns it) since the list endpoint doesn't; sorts client-side. Small N+1 (bounded by the 20/day cap), acceptable for an internal tool, disclosed rather than silently expanding the list endpoint's schema/sort options. **Live-smoke-tested against the real backend/RDS:** marked a real lead (id 24) `sent` through the actual `/api/leads/[id]` proxy route, watched the header go 0/20 → 1/20 and the lead appear in `/admin/replies` offering only *Mark replied*; marked it `replied` and confirmed the row switched to offering only *Mark converted*; confirmed `reply_rate` went 0.0 → 1.0; reverted the lead to its original `enriched`/no-channel/no-timestamps state afterward and re-confirmed `/stats` matches the pre-test baseline exactly. Re-ran the ticket-3.2 key-leak grep against a full production build (`.next/static` and the rest of `.next/`) for both the test key and the real `ADMIN_API_KEY` — zero matches anywhere outside `.next/server`. `npm run lint` and `next build` both clean. | ✅ ACCEPTED (PM, 2026-08-06) — interpretation call approved (ticket text was self-contradictory; implementation matches LOGIC §3). Shared sent-today helper, day-boundary test, and baseline-restoring live verification specifically commended. |
| 3.5 | First real sends (milestone; needs 2.4/2.5) | ⬜ | | Gate (b) cleared 2026-08-09 — Sprint 2 tickets 2.4/2.5 are ✅, 86 leads are `queued` and ready to send (lead 76 pulled to PM-HOLD, see review cycle log). Gate (a) (Stakeholder UAT confirmation of the 3.4-UAT round) is the only remaining blocker before the first real send day. | |

### 3.4-UAT — Stakeholder UAT fixes on the dashboard (must-fix batch, 2026-08-06)

| # | Ticket | Status | Files touched | Cursor notes |
|---|---|---|---|---|
| UAT-1 | Mark sent button must explain its disabled state | ✅ | reviewguide-app: lib/leadTransitions.ts, app/admin/leads/[id]/LeadDetailClient.tsx | The old implementation only put the reason in a `title` attribute — invisible until hover, which is what the Stakeholder actually caught. New `explainWhyNotSendable(status)` in `lib/leadTransitions.ts` gives an illegal-transition reason specific to how far the lead still has to go ("Lead still needs a response generated, enrichment, and queueing before it can be marked sent (currently: new)." vs "...still needs to be queued." vs "Lead is already 'sent' — it can't be marked sent from here." for post-send statuses). A single `sendBlockedReason` in the detail client picks one of the four causes in priority order — illegal transition, then daily cap, then no channel, then the health checkbox — because the first two make the other two moot (picking a channel doesn't help if the transition itself is illegal or the cap's hit), and renders it as a permanent red line directly under the button, not a tooltip. Also newly disables "Mark sent" when no channel is selected (previously only checked on click, silently doing nothing useful) so the disabled state and its explanation always agree with each other. **Live-verified the ticket's own bug-doubt via the real API, not the test suite:** took a real `enriched`, non-health-flagged lead (id 24) through the actual legal chain — `PATCH status=queued` (200), then `PATCH status=sent, channel=email` (200, `sent_at` stamped) — confirming `queued` + a channel genuinely can be marked sent; no bug exists. (A first attempt at this same check hit a local `curl`/`python` output-parsing bug on my end that looked like a real 422 — traced it back to the *raw* HTTP calls having actually already succeeded moments earlier, confirmed via a direct DB read, then reverted lead 24 to its exact prior baseline (`enriched`, no channel, no timestamps) and re-confirmed `/stats` matches.) Also fixed an unrelated pollution bug found along the way: `eslint.config.mjs` had no ignore for `.netlify/` (Netlify CLI's local link/deploy output dir), so `npm run lint` was linting ~5,300 issues in generated files — added to `globalIgnores`. |
| UAT-2 | Contact section becomes cards | ✅ | reviewguide-app: app/admin/leads/[id]/LeadDetailClient.tsx | Replaced the four always-visible (greyed-when-absent) `ContactButton`s with a `ContactCard` per *available* channel only — Facebook/Email/Phone/Website each render their own card with a copy-to-clipboard icon (reusing the existing toast `flash()` helper for "Copied to clipboard.") and an action button (Open/Email/Call/Visit); a channel with no value on the place renders nothing, not a disabled placeholder. Facebook's headline value is the page slug extracted from the URL path (e.g. `wloskistrajk`, not the full `https://www.facebook.com/wloskistrajk/`), Website strips the protocol/trailing slash for display while still copying/opening the real URL, and Phone's `tel:` href strips formatting characters (`+48 797 437 660` → `tel:+48797437660`) so it's dialable rather than passing spaces through raw. A "no contact channels available" line covers the all-absent case. **Live-verified** against two real leads: one with all four channels populated (id 57, Włoski Strajk) rendered all four cards correctly; one missing Facebook and email (id 60) rendered only Phone/Website — confirmed by rendering the actual page HTML through the dev server, not just reading the code. |
| UAT-3 | Alembic migration + place data enrichment | ✅ | alembic/versions/003_places_enrichment_fields.py, app/models.py, app/jobs/discover.py, app/routers/admin.py, tests/test_discover.py, tests/test_admin.py, reviewguide-app: lib/api.ts, app/admin/leads/[id]/LeadDetailClient.tsx | **Backend:** migration `003` adds `rating FLOAT`, `reviews_count INT`, `lat FLOAT`, `lng FLOAT`, `google_maps_url TEXT` to `places` (all nullable — no backfill-before-migrate ordering issue). Field mapping in `discover.py` was built from inspecting a real `outscraper.google_maps_search` response live (per the standing habit), not guessed: Outscraper's actual field names are `rating`, `reviews` (the count, not `reviews_count`), `latitude`, `longitude`, and `location_link` (a direct Google Maps URL for the place — there is no field literally named `google_maps_url`). `GET /api/admin/leads/{id}`'s `PlaceInfo` schema extended with the same five fields. **Backfill run live** (approved ~$2-3): `python -m app.jobs.discover --district srodmiescie --yes` — 561 unique places found across the 10 sub-area queries, 41 inserted / 520 updated, actual cost **$2.09** (within budget). 559-561 of 661 total places in the table now carry the new fields (the remainder predate this run and weren't in this pass's result set). **Frontend:** lead detail header now shows `★ {rating} ({reviews_count} reviews) · {address} · Open in Google Maps` (only the parts that exist render; the maps link falls back to a `lat`/`lng`-built URL if `google_maps_url` is null, e.g. for a place that hasn't been re-polled since this ticket). **Live-verified with a real lead already carrying real data:** lead 135 (Zapiecek) rendered `★ 4.3 (4,087 reviews) · Freta 18, 00-227 Warszawa, Polska · Open in Google Maps` with a working maps link, confirmed both via a direct `GET /api/admin/leads/135` call and by rendering the actual detail page HTML. New tests: `upsert_places` field-mapping test (asserts the exact Outscraper→column name mapping via the compiled insert statement's params) and two `GET /leads/{id}` tests (fields present with real values; fields null rather than erroring for an unenriched place). 192/192 backend tests pass. |
| UAT-4 | Visual pass (glassmorphism) | ✅ | reviewguide-app: lib/theme.ts (new), app/admin/icons.tsx (new), app/admin/layout.tsx, app/admin/page.tsx, app/admin/leads/page.tsx, app/admin/leads/LeadsFilterBar.tsx, app/admin/leads/[id]/page.tsx, app/admin/leads/[id]/LeadDetailClient.tsx, app/admin/replies/page.tsx, app/admin/replies/ReplyRow.tsx | Styling-only pass, scoped to `/admin/*` (the public `/` placeholder and root layout are untouched, so this doesn't affect anything outside the dashboard). New `lib/theme.ts` holds three shared literal Tailwind class strings (`PAGE_BACKGROUND` — a soft `indigo-50 → white → sky-50` gradient; `GLASS_CARD`/`GLASS_NAV` — `rounded-2xl`/translucent-white/`backdrop-blur-xl`/subtle border+shadow) reused across every page so the look doesn't drift file-to-file; kept as plain literal strings rather than computed ones since Tailwind's build-time class scanner needs to see them statically. New `app/admin/icons.tsx` is a small hand-rolled outline-icon set (no icon library was installed) used only for the dashboard's stat cards — each status/metric gets a distinct colored icon badge (e.g. `new`=slate circle, `queued`=amber clock, `sent`=indigo paper-plane, `converted`=emerald check, `dead`=rose ×) replacing the old dark "highlight" card variant, which was itself just a styling choice — no card lost any label or value in the swap. Top nav is now a `sticky` frosted bar; the "Sent today: N/20" pill got a matching glass treatment (still turns red at cap). Table/filter-bar/contact-card containers all moved to the same `GLASS_CARD` token; hover/divide colors adjusted to `white/40-60` variants so they still read correctly against the translucent background; text itself was kept on opaque-enough surfaces and never blurred, per the ticket's explicit readability caveat. **Live-verified visually, not just by code review:** temporarily installed Playwright + Chromium (`npm install -D playwright --no-save`, removed after use — `package.json`/`package-lock.json` untouched, confirmed via `git diff`), screenshotted all four page types (dashboard, leads list, lead detail incl. a health-flagged lead with the UAT-1 red reason text and UAT-2 contact cards visible, replies) against the live dev server, and caught one real bug from the screenshot itself: the "Reply rate (G2 gate metric)" label was being `truncate`d and cut off mid-word — fixed by dropping `truncate` in favor of natural wrapping on stat card labels. `npx tsc --noEmit` and `npm run lint` both clean after every step. |

**UI polish round 2** (Stakeholder-directed, styling/layout only, 2026-08-07, `reviewguide-app`):
- Stat cards: label moved above the icon/value row so title, icon, and number line up; `GLASS_CARD` shadow strengthened (`shadow-sm` → `shadow-lg` + `ring-1 ring-black/5`) so card edges (especially the top edge, which a box-shadow alone doesn't touch) read as more defined against the gradient background.
- Lead detail: swapped section order — Contact now appears before Review (read-only).
- Lead detail: "Copy message" moved inline next to "Save" in the Outreach message section (`EditableField` gained an `extraActions` slot instead of a separately-positioned button).
- Lead detail: final action row (Channel select / Mark sent / Skip) — buttons realigned onto one row; the red disabled-reason text now sits below the full row instead of squeezed under just the Mark sent button.
- Lead detail: header block (name/status/rating/address/maps link) wrapped in its own card; status is now a colored pill (new shared `lib/statusStyle.ts`, reused by the dashboard's stat cards so the two palettes can't drift apart), rating is an amber chip, address has a map-pin icon, and "Open in Google Maps" is a bordered button — each piece of info visually distinguished instead of one dot-separated line.
- Quality bar: `npm run lint`, `npx tsc --noEmit`, and `next build` all clean; key-leak grep re-run against the production `.next/` output — zero matches outside `.next/server`. No backend, data, or transition-logic changes in this round.

### Sprint 3 blockers
- 3.5 (first real sends) blocked on one remaining external gate: (a) Stakeholder UAT of the
  dashboard — the first round came back as batch 3.4-UAT (4 must-fix tickets), all now ✅ and
  live-verified; standing by for the Stakeholder to confirm this round satisfies the UAT.
  ~~(b) Sprint 2 tickets 2.4/2.5 executing~~ — **cleared 2026-08-09**: Sprint 2 closed, 87 leads
  `queued` and ready. No further Sprint 3 build work is planned while (a) is outstanding; the PM
  will triage any further UAT feedback into additional 3.4.x fix tickets if needed.

### Sprint 3 open questions for Stakeholder
- ~~Create GitHub repo `reviewguide-app`~~ — **done 2026-08-06**, repo exists and ticket 3.2 is
  pushed to it. ~~Still open: create the Netlify site linked to it~~ — **done 2026-08-06**,
  Stakeholder created `dynamic-puppy-631956`; deployment wiring completed and chain verified
  live same day (see ticket 3.2 notes above).
- ~~Choose ADMIN_USER/ADMIN_PASS (basic auth) — put in Netlify env, never in chat/repo~~
  **done 2026-08-06** — generated randomly, set on Netlify as write-only secrets, written to
  `reviewguide-app/.env` (gitignored) for the Stakeholder to move into a password manager;
  never appeared in chat/logs (see ticket 3.2 notes above for how a CLI-echo near-leak was
  caught and remediated by rotation).
- ~~**From 3.1:** `ALLOWED_TRANSITIONS` only permitted `dead` from `sent`/`replied`, exactly per
  LOGIC §3's diagram, while ticket 3.3's "Skip → dead" action implied no source-status
  restriction.~~ **Resolved 2026-08-06** — LOGIC.md §3 amended (dead reachable from any status
  except converted; pre-sent skips require a note; dead is terminal). `ALLOWED_TRANSITIONS` and
  tests updated to match; see ticket 3.1 notes above.

### Sprint 3 notes
- 2026-08-06 (ticket 3.1): a generated `ADMIN_API_KEY` was written to `.env` (`secrets.token_urlsafe(32)`,
  local dev only, never committed) so the admin API isn't accidentally left open locally.
- 2026-08-06 (ticket 3.1 amendment): LOGIC.md §3 changed after this ticket surfaced the gap above.
  `ALLOWED_TRANSITIONS` rebuilt so every status except `converted`/`dead` itself gained a `dead`
  edge; a new pre-sent-skip note requirement was added (see ticket 3.1 notes). 10 new tests cover
  the amendment; 184/184 total tests pass.
- 2026-08-06: Sprint 3 tickets 3.1-3.4 all ✅. Standing by — no further build work until either
  Stakeholder UAT feedback on the live dashboard arrives or Sprint 2's 2.4/2.5 unblock 3.5.
- 2026-08-06: Stakeholder UAT round 1 (batch 3.4-UAT, 4 must-fix tickets) all ✅ — see the
  3.4-UAT subsection above. Backend migration 003 applied to the live DB and the srodmiescie
  discovery backfill re-run live ($2.09 actual). 192/192 backend tests pass; frontend
  `tsc`/`lint` clean throughout. Standing by for Stakeholder confirmation.
- 2026-08-07: Stakeholder-directed "UI polish round 2" session on `reviewguide-app` (styling/layout
  only, no PM ticket) — see the 3.4-UAT subsection above for the itemized change list. Committed
  and deployed; live site confirmed serving the new build.

---

## SPRINT 4 — Product Foundation — ✅ CLOSED 2026-08-07

| # | Ticket | Status | Files touched | Cursor notes | PM verdict |
|---|---|---|---|---|---|
| 4.1 | Marketing repo + landing (PL) | ✅ | (new repo) reviewguide-marketing: app/page.tsx, app/layout.tsx, app/globals.css, app/icon.tsx, app/opengraph-image.tsx, components/ui/illuminated-hero.tsx, components/how-it-works.tsx, components/demo-section.tsx, components/pricing-section.tsx, components/faq-section.tsx, components/site-nav.tsx, components/site-footer.tsx, lib/utils.ts, lib/theme.ts, next.config.ts, netlify.toml, README.md, .env.example | New sibling repo pushed: https://github.com/pedram-kh/reviewguide-marketing (main branch). Next.js 16 (App Router, TS, Tailwind v4) as a **static export** (`output: "export"`, no server code at all) — deliberately the lightest of the three repos. shadcn-style `components/ui/` + `lib/utils.ts`'s `cn()` (clsx + tailwind-merge) per the ticket's ask, without running the actual shadcn CLI since no other primitives were needed. **Hero:** the Stakeholder-provided `IlluminatedHero` reference component adapted with Polish copy in the glow structure ("Każda opinia dostaje odpowiedź."), a real `<h1>` (screen-reader/SEO text distinct from the `aria-hidden` decorative heading), a CTA pair, and the amendment's `prefers-reduced-motion` + `<768px` fallback (SVG glow filter → text-shadow). **Two real bugs found and fixed while integrating the reference component** (not anticipated by the ticket): (1) the bottom light-orb div shared the top orb's `translate-[0_-70%]` baseline instead of its own keyframe's `0 70%` start, which would have animated both orbs from the same position; (2) the `::before` "crisp text fades in over the glow" trick duplicates the heading via `content: attr(data-text)` and absolutely-positions it over the real text — on narrow viewports the real text wraps to 3 lines while the absolutely-positioned duplicate doesn't, producing visibly overlapping doubled text, caught via an actual mobile-viewport screenshot rather than by reading the code. Fixed by dropping the duplicate-text pseudo-element under the same mobile/reduced-motion fallback rather than trying to keep both copies' wrapping in sync. **Sections below the hero** (dark frosted-glass, mirroring `reviewguide-app`'s light-glass tokens inverted): how-it-works (3 steps), demo block — 2 real review→response pairs from `docs/review/generation_batch_2026-08-05_v1.2.md`, both restaurant name **and** address redacted to "Restauracja w Śródmieściu" (the ticket only asked for the name, but a specific Warsaw street address next to a very specific review is itself enough to re-identify the place on Google, so it's blanked too — disclosed rather than silently over-redacting), pricing (129 zł/mies., "14 dni za darmo, bez karty"), FAQ as a native `<details>`/`<summary>` accordion (4 questions, no client JS needed — the health-flag FAQ answer mirrors the real product logic rather than being generic marketing copy), footer with `anna@reviewguide.eu`. **Disclosed addition, not in the ticket's section list:** a slim sticky top nav (brand + one CTA) — a single-page scroll layout with no way back to a CTA once scrolled past the hero seemed like an omission worth flagging rather than silently adding or silently skipping. CTAs link to `${NEXT_PUBLIC_APP_URL}/signup`, which doesn't exist yet (ticket 4.2) — stub per the ticket's own text. OG image + favicon generated at build time via `next/og`'s `ImageResponse` (`app/icon.tsx`, `app/opengraph-image.tsx`; both needed `export const dynamic = "force-static"` to be compatible with `output: "export"`). `netlify.toml` publishes `out/` directly with no `@netlify/plugin-nextjs` (unlike `reviewguide-app` — there's no SSR/middleware surface here to run at the edge); README documents the Netlify deploy + GoDaddy DNS domain-connect steps for `reviewguide.eu` as a Stakeholder action. **Lighthouse (headless Chrome, simulated throttling):** desktop 99/100 performance (target ≥90) on the first run. Mobile initially measured **84/100** (target ≥85) — root-caused via the LCP-breakdown and speed-index audits (not guessed) to the two background light-orbs' `blur(48px)` being repainted every animation frame, which is exactly the class of cost the ticket's own mobile-fallback amendment was written to avoid, just not extended far enough; the fallback only touched the text glow filter. Extended the same `<768px`/reduced-motion media query to freeze the orbs at their end state with a smaller blur — re-measured mobile at **89/100**, both targets met. Accessibility 95, best-practices 100, SEO 91 on both form factors (not the ticket's stated targets, but recorded since Lighthouse reports them regardless). `npm run lint` and `next build` both clean; `.gitignore`'s `.env*` pattern had the same bare-swallows-`.env.example` bug found in ticket 3.2 — pre-empted with `!.env.example` before the first commit this time. <br><br>**Follow-up fix, 2026-08-07 (Stakeholder-caught via local run + screenshots):** bug (2) above turned out to be worse than the mobile-only framing at first commit — the ticket's own screenshots showed the same duplicate-text ghosting at a plain ~820–1000px desktop width (once the glow line itself wrapped to 2 lines, not the 3-line mobile case originally tested), and separately, far worse, under Chrome's built-in page-translate feature: translate rewrites the *visible* text node in place but never touches the `data-text` attribute the `::before` duplicate reads its content from, so translating the page to English left the stale untranslated Polish duplicate rendering underneath the newly-English real text, overlapping both languages. Root cause: the whole "duplicate the heading via `content: attr(data-text)`, absolutely-position it on top" technique is only ever correct if both copies wrap byte-for-byte identically, which isn't guaranteed at any width, let alone under translation — the earlier fix patched the symptom (disable the duplicate under one specific breakpoint) rather than the cause. Real fix: removed the duplicate `::before` element entirely; the gradient/clip-text styling and fade-in animation now apply directly to the one real text node, at every viewport and in both the normal and reduced-motion/mobile fallback modes. There is now only ever a single copy of the heading text in the DOM, so it cannot desync from itself — correct by construction, not by breakpoint. Verified via Playwright screenshots at 390px (mobile), 820px (the exact width that reproduced the Stakeholder's bug), 1024px, and 1440px — no ghosting at any of them; `npm run lint`, `tsc --noEmit`, and `next build` all clean. Committed as `fix: hero glow text ghosting on wrap + Chrome auto-translate` and pushed. <br><br>**Deployed live, 2026-08-07.** Netlify site `reviewguide-marketing` created via CLI/API (account `sideprojects`), pointed at the GitHub repo with `reviewguide-app`-matching build settings (`npm run build` → `out`), `NEXT_PUBLIC_APP_URL` set to the live `reviewguide-app` URL, site-level SSO gate disabled (same fix as ticket 3.2's deployment), `reviewguide.eu` + `www.reviewguide.eu` pre-registered as the site's custom domain/alias. **Live and content-verified**: `curl` + a Playwright screenshot against `https://reviewguide-marketing.netlify.app` confirm the hero, demo block (with the ghosting fix visibly clean), pricing, and footer all render, and the CTA `href`s correctly point at the `reviewguide-app` signup URL. **Real gap found and disclosed, not silently worked around:** push-to-deploy isn't live yet — an API-triggered on-demand build works and is what actually shipped this to production, but two subsequent `git push`es did not auto-trigger a build. Root-caused by diffing this site's Netlify records against `reviewguide-app`'s working one: the working site has `github_app_commit_status`/`github_app_checks` hook rows that this one lacks, meaning the CLI/API `repo` field update sets enough metadata for on-demand builds but doesn't run the full webhook-subscription flow the Netlify UI/`netlify init` normally does. Traced further to a likely repo-scoped GitHub App installation — confirmed indirectly by attempting `gh api -X PUT /user/installations/{id}/repositories/{repo_id}`, which GitHub rejected as needing a browser-authorized action, not a plain token (mirrors the one-time OAuth authorization pattern from ticket 0.5's App Runner GitHub connection). Documented in the marketing repo's README as a ~1-minute Stakeholder action (add the repo to the "Netlify" GitHub App's installation at github.com/settings/installations) plus the manual-deploy command to use until then. DNS: `reviewguide.eu`/`www` records for GoDaddy printed with today's confirmed Netlify values (A `75.2.60.5` for apex — GoDaddy doesn't support ALIAS/ANAME on apex — CNAME `www` → `reviewguide-marketing.netlify.app`), also in the README. | ✅ ACCEPTED (PM, 2026-08-07) — Stakeholder-caught ghosting bug root-caused (single-text-node fix, translate-proof) rather than patched; address redaction beyond spec and sticky nav approved; Lighthouse 89/99 meets targets. |
| 4.2 | Magic-link auth + customers table | ✅ | Backend: alembic/versions/004_customers_auth_tokens.py, app/models.py, app/auth.py, app/routers/auth.py, app/services/postmark_client.py, app/templates.py, app/config.py, app/main.py, .env.example, tests/test_auth.py, pyproject.toml. App repo (reviewguide-app): lib/session.ts, lib/authApi.ts, lib/theme.ts, lib/requestOrigin.ts, app/(customer)/layout.tsx, app/(customer)/EmailAuthForm.tsx, app/(customer)/signup/page.tsx, app/(customer)/login/page.tsx, app/(customer)/app/page.tsx, app/api/auth/request-link/route.ts, app/api/auth/logout/route.ts, app/auth/verify/route.ts, middleware.ts, package.json, .env.example, README.md | Migration 004 creates `customers` and `auth_tokens` exactly per spec (UNIQUE email, `place_id` nullable FK to `places`, `subscription_status` defaulting `'none'`; token hash unique + indexed email). `app/auth.py` centralizes the token/session primitives: 32-byte urlsafe raw token (only its SHA-256 hex digest is ever persisted — same "never store the usable secret" principle as ticket 3.1's `ADMIN_API_KEY`), 15-min token TTL, 30-day session JWT (`AUTH_JWT_SECRET`, HS256), 3-requests/email/hour rate limit, all as named constants the tests assert against directly rather than duplicating magic numbers. **`POST /api/auth/request-link`**: rate-limited by counting `auth_tokens` rows for that email in the trailing hour: over the cap → 429 (not a silent 200 — a clear, honest error to a human hitting a real limit, distinct from the enumeration-resistance case below); otherwise a token is minted and a send is *attempted* the same way regardless of whether a `customers` row exists yet, and the endpoint always returns the same 200 body. **Disclosed interpretation call:** the ticket's test-list phrase "unknown email → same 200 + no send call" is contradictory with the milestone's own requirement that a stranger can self-serve signup via the landing page — read literally it would mean brand-new visitors' first-ever request silently does nothing. Implemented so enumeration resistance instead comes from response/code-path identity (constant 200, a send always attempted, Postmark failures always swallowed the same way) rather than from suppressing sends to addresses without an existing row; a `customers` row is created *lazily on first successful verify*, not at request time, so "new customer" and "existing customer" are indistinguishable at request-link time regardless. **`POST /api/auth/verify`**: single-use enforced via one atomic `UPDATE ... WHERE used_at IS NULL AND expires_at > now` + rowcount check (not a read-then-write, which two concurrent requests with the same token could race) — SQLite (tests) and Postgres (prod) both support this without relying on dialect-specific `RETURNING`. Wrong/reused/expired token → 401 with a human-readable PL message the frontend surfaces via `/login?error=`. The magic-link email template (`app/templates.py`) is a 4-line PL constant with no marketing copy, per spec. **App repo, dark theme (per the ticket's explicit "no theme seam" note):** `/signup` and `/login` share one `EmailAuthForm` client component and a dark route-group layout reusing `reviewguide-marketing`'s glass-panel/gradient tokens, while `/admin` stays untouched light-glass. `/auth/verify` is a Route Handler that calls the backend's verify endpoint, sets the returned JWT as an `httpOnly`/`Secure`/`SameSite=lax` cookie, and redirects to `/app` on success or `/login?error=invalid_link` on failure. `/app` is protected two ways: `middleware.ts` verifies the session cookie's JWT signature via `jose` (Edge-compatible) before the request reaches the page, and the page independently re-verifies rather than trusting middleware ran. Includes a disclosed "Wyloguj" logout route not explicitly in the ticket's text.<br><br>**Production wiring + live end-to-end verification, 2026-08-07 (real Postmark account, real inbox: pedram@reviewguide.eu):** Stakeholder verified `mail.reviewguide.eu` (DKIM + Return-Path) and provided the Postmark Server API token via the same "gitignored-file, never echoed in chat" handoff pattern as prior secrets. Generated a fresh production `AUTH_JWT_SECRET` (independent from the one used for local dev, which stays paired with the local backend for local testing only); App Runner updated with `POSTMARK_TOKEN` + `AUTH_JWT_SECRET` added, all 7 existing env vars preserved verbatim, service reached RUNNING; Netlify (`reviewguide-app`) got `AUTH_JWT_SECRET` added as a `--secret`-flagged var (write-only, unlike the existing `ADMIN_API_KEY`/`ADMIN_PASS`, which are plain — a small hardening improvement scoped to only the var touched this session, not a retrofit of the others). **Secret-handling incident, self-caught and corrected within the same session:** the first `netlify env:set AUTH_JWT_SECRET <value>` call (without `--secret`) echoed the full value back in its own confirmation output — the same class of "CLI echo leak" this project rotated once before (ticket 3.2's deployment wiring). Treated identically: the secret was rotated immediately (new value generated, App Runner and Netlify both re-applied, ops-secrets file updated) rather than accepted as "probably fine," and the second `netlify env:set` call used `--secret` with output redirected to a file, confirmed clean via grep, specifically to not repeat the exposure. **Two real bugs found live, both root-caused rather than patched, matching ticket 4.1's precedent:** (1) `/auth/verify`'s post-login redirect to `/app` landed on Netlify's *internal per-deploy* hostname (`<deploy-id>--dynamic-puppy-631956.netlify.app`) rather than the public domain — the session cookie, scoped to the public host that set it, never reached that host, so a successful login silently looked logged-out. Root cause: `new URL(path, request.url)` trusts `request.url`'s origin, but Netlify's Next.js runtime hands route handlers/middleware a `request.url` whose origin is that internal deploy URL. The same pattern already existed in `middleware.ts`'s `/admin`→unauthorized path and the logout route; only ticket 4.2's `/app` flow happened to expose it because it's the first redirect a browser actually follows cross-page. Fixed by deriving the origin from `X-Forwarded-Host`/`Host` headers (`lib/requestOrigin.ts`) instead of `request.url`, applied at all three redirect sites. (2) Even after (1), the spent one-time token remained visible in the `/app` URL (`/app?token=...`) — traced to a *documented* Netlify platform behavior (github.com/opennextjs/opennextjs-netlify#2209): Netlify auto-propagates the original request's query string onto any redirect Location that has none of its own. Confirmed by reproducing cleanly against a local `next start` hitting the live production backend (no leak locally, isolating it to Netlify's layer, not app code) and by noting `/login?error=...` redirects — which already carry their own query — were never affected. Fixed per Netlify's own documented workaround: give the target a query param of its own (`withNetlifyRedirectSafety()`) so there's nothing for Netlify to substitute. Both fixes verified live using **DB-inserted test tokens** (bypassing Postmark entirely, since `/api/auth/verify` isn't rate-limited — `/api/auth/request-link` is, and this session's 3/hour cap for `pedram@reviewguide.eu` was already spent on the real-email tests below) before being trusted. **Real-email loop evidence:** `POST /api/auth/request-link` against the deployed `/signup` → Postmark's `/messages/outbound` activity API confirms `Status: Sent`, `From: "ReviewGuide" <alerts@mail.reviewguide.eu>`, `To: pedram@reviewguide.eu`, `Subject: Twój link logowania do ReviewGuide`, body matching the 4-line PL template exactly. First real link was consumed 14 seconds after send — before this session's own verify attempt — almost certainly the recipient mailbox provider's automated link-scanning/prefetch security feature rather than a code issue (single-use tokens make this a UX/telemetry footnote, not a security hole: the legitimate user's own click 14 seconds later would see "already used" — worth the Stakeholder's awareness, not a blocker). After both redirect fixes, a second real email's magic link click completed the full loop end-to-end: correct host, no token leak (`?_r=1` in its place), session cookie set, `/app` returns 200 with "Zalogowano jako pedram@reviewguide.eu" and the logout button. All synthetic tokens and the real test customer row were deleted from RDS after verification; `docs/PROGRESS.md` and this session's temp files never retained the actual secret values (masked to last 4 chars throughout, `--secret`-flagged where the tool supports it, and one incident rotated on sight). 208/208 backend tests still pass; frontend `eslint`/`tsc --noEmit`/`next build` clean; ticket 3.2's key-leak grep re-run clean against the final production build. | ✅ ACCEPTED (PM, 2026-08-07) — X-Forwarded-Host and query-propagation fixes root-caused and approved; secret rotation self-catch commended. Follow-up 4.2b REQUIRED before 4.5. |
| 4.2b | Verify interstitial (survive mailbox link-prescanning) | ✅ | App repo (reviewguide-app): app/(customer)/auth/verify/page.tsx (new — replaces the old GET route handler), app/api/auth/complete-verify/route.ts (new — the token-consuming POST, moved verbatim from the deleted app/auth/verify/route.ts's try/catch), app/auth/verify/route.ts (deleted) | Backend untouched — `POST /api/auth/verify` was already single-use-safe (ticket 4.2's atomic `UPDATE ... WHERE used_at IS NULL` + rowcount check); the bug was purely in *when* the frontend called it. `GET /auth/verify` is now a dark-themed page ("Kliknij, aby dokończyć logowanie" + one button) that only reads the `token` query param and echoes it into a hidden `<input>` — it never touches the backend, so it is provably side-effect-free no matter how many times a mailbox's link-prescanner fetches it. The button is a plain HTML `<form method="POST">` (not a Next.js Server Action) specifically so the consuming step stays a directly-curlable endpoint for both this session's own verification and any future automated check — a page.tsx and a route.ts can't share one URL in the App Router, so the POST target is the new sibling route `/api/auth/complete-verify`, reachable only by an actual form submission. That POST redirects with `303 See Other` (not the default `307`) since a redirect after a POST should not replay the POST on the target. Invalid/expired token on POST redirects to `/login?error=invalid_link` — reusing ticket 4.2's existing PL copy ("Ten link jest nieprawidłowy, już wykorzystany lub wygasł. Wyślij nowy poniżej.") with its email form right there, rather than building a second, separate "wyślij nowy link" page for the same job. No JS test framework exists in this repo (consistent with every prior frontend ticket in this project, which verified behavior via live curl/DB checks rather than Jest/Vitest); the three required properties were verified the same way: **(1)** 3 repeated `GET /auth/verify?token=...` against a DB-inserted test token left `auth_tokens.used_at` `NULL` after all three; **(2)** `POST /api/auth/complete-verify` with that token returned `303` to `/app?_r=1` with the session cookie set, and the DB row's `used_at` was then non-null; **(3)** a second `POST` with the same (now-used) token was rejected, redirecting to `/login?error=invalid_link` — single-use behavior unchanged. **Infra rabbit hole during verification, disclosed rather than hidden:** the first two attempts at this local verification gave false failures unrelated to the code — (a) a stale `next start` process from an earlier chat session was still holding the intended port and served a pre-4.2b build, producing misleading `error=error` redirects that looked like a bug in the new code; (b) once past that, the *sandboxed* shell's restricted network egress couldn't resolve the RDS hostname, so the local backend's own DB queries failed with a DNS error surfaced through the frontend as a generic 500 — not a bug either. Both were environmental; re-run with a clean port and `full_network` permission reproduced the correct behavior described above. **Live real-Postmark verification, 2026-08-07:** committed and pushed to `reviewguide-app` (push-to-deploy confirmed working this time — no repeat of ticket 4.1's GitHub-App gap); requested a real magic link for `pedram@reviewguide.eu` from the deployed `/signup`. Postmark confirms `Status: Sent`. The token's `used_at` was still `NULL` 40+ seconds after send (versus 14 seconds to consumption in ticket 4.2's pre-fix test) — the interstitial's GET no longer gives a prescanner anything to consume. Extracted the real link from Postmark's message body and completed the human side of the loop against it: `GET /auth/verify?token=...` rendered the interstitial for the real token, `POST /api/auth/complete-verify` consumed it, set the session cookie, and redirected to `/app?_r=1`; fetching `/app` with that cookie returned "Zalogowano jako pedram@reviewguide.eu". Test `auth_tokens`/`customers` rows for `pedram@reviewguide.eu` deleted from RDS after verification, matching ticket 4.2's precedent. 208/208 backend tests still pass (untouched); frontend `eslint`, `next build`, and the ticket 3.2 key-leak grep (re-run against this build's `.next/static`) all clean. | ✅ ACCEPTED (PM, 2026-08-07) — carried by 4.5's real-hardware evidence, ratified explicitly. |
| 4.3 | Stripe test-mode billing loop | ✅ | Backend: app/routers/billing.py (new), app/auth.py (+decode_session_token), app/config.py (+stripe_secret_key/stripe_webhook_secret/stripe_price_id), app/main.py, pyproject.toml (+stripe SDK), .env.example, tests/test_billing.py (new, 18 tests). App repo (reviewguide-app): lib/billingApi.ts (new), app/api/billing/checkout/route.ts (new), app/api/billing/portal/route.ts (new), app/(customer)/app/page.tsx (status card + two buttons) | Three endpoints per spec: `POST /api/billing/checkout` (creates/reuses a Stripe Customer, then a Checkout Session — `mode=subscription`, `trial_period_days=14`, `payment_method_collection=if_required` for trial-without-card), `GET /api/billing/portal` (Customer Portal session), `POST /api/billing/webhook` (signature-verified via `STRIPE_WEBHOOK_SECRET`, handles `customer.subscription.{created,updated,deleted}`, idempotent by construction — every handled event just sets `subscription_status` to whatever Stripe currently says, so replaying the same event re-applies the same value rather than compounding a side effect). **Auth design call, disclosed:** the ticket doesn't specify how the backend identifies "the logged-in customer" for checkout/portal. Rather than trusting a caller-supplied `customer_id` (which anyone with network access to the backend could then spoof to open billing sessions for a stranger's account), `get_current_customer` requires `Authorization: Bearer <session_jwt>` and independently re-verifies that JWT's signature with `AUTH_JWT_SECRET` (`app.auth.decode_session_token`) — the same JWT the app already stores as its session cookie, so reviewguide-app's Route Handlers just forward the cookie's raw value rather than inventing a second credential for one login. **Disclosed addition beyond the ticket's literal endpoint list:** `GET /api/billing/status`, because "/app shows subscription status" needs a way to read the *current* status — the session JWT only carries what was true at login time, not anything a webhook changed since. App: `/app`'s card now shows the live status (PL-labelled: brak/okres próbny/aktywna/...) and exactly one button — "Rozpocznij okres próbny" before a Stripe customer exists, "Zarządzaj subskrypcją" after — both plain `<form method="post">` buttons (no client JS) hitting new Route Handlers that forward the session cookie as the bearer token and 303-redirect the browser straight to Stripe's own hosted page. Checkout/portal failures (expected right now — Stripe isn't configured) redirect back to `/app?error=billing_error` with a friendly PL message rather than a raw 5xx, verified live locally end-to-end (button click → graceful redirect → error banner renders) with a DB-inserted test customer, including the post-trial-start branch (manually flipped `stripe_customer_id`/`subscription_status` to confirm the button swaps correctly). 226/226 backend tests pass (18 new, all Stripe SDK calls mocked — no test ever touches the real API); frontend `eslint`/`next build`/key-leak grep all clean. **Blocked on the Stakeholder action this ticket is explicitly gated on** (SPRINT_04.md's Stakeholder actions table: "Create Stripe account... gates 4.3"): no `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_ID` exist yet, so the code above is built and unit-tested but not yet live-verified against real Stripe test mode (checkout redirect, `stripe listen --forward-to` webhook delivery, trial-without-card confirmed, `/app` reflecting a real `trialing` status). Stripe CLI installed locally (`stripe/stripe-cli`, v1.45.1) ready for that session. Same "build everything, stop before the gated live step" pattern as ticket 4.2's Postmark wiring. **Also fixed in passing:** this row had been accidentally duplicated (two identical blank `⬜` rows) since the sprint-open commit — collapsed back to one. <br><br>**Live verification, 2026-08-07 (Stakeholder-provided TEST-mode secret key, confirmed non-live via `stripe.Account.retrieve()` before anything else ran):** created the "ReviewGuide" 129 zł/mies product+price directly via the Stripe API rather than asking for dashboard clicking (idempotent — re-running the script reuses the existing product/price instead of duplicating them), and a real webhook endpoint pointed at the live App Runner URL (`/api/billing/webhook`, all three subscription events), both one-time setup that now lives permanently in the Stakeholder's Stripe test-mode account rather than an ephemeral `stripe listen` tunnel — chosen over the ticket text's literal `stripe CLI listen --forward-to` because the backend is already a public HTTPS URL, so a real registered endpoint exercises the exact same signature-verified path production traffic will use, rather than a local relay. `STRIPE_SECRET_KEY`/`STRIPE_PRICE_ID`/`STRIPE_WEBHOOK_SECRET` added to App Runner (all 9 prior env vars preserved verbatim, service reached `RUNNING` twice — once per var batch). **Bug found and fixed before any of this could work:** the session JWT used for testing was signed with the *local* `.env`'s `AUTH_JWT_SECRET`, which had silently drifted from the value actually deployed on App Runner (a leftover from ticket 4.2's mid-session secret rotation, which updated App Runner + Netlify but never re-synced the backend developer's own local file) — the live `/api/billing/checkout` call 401'd with "Session is invalid or expired" until local `.env` was overwritten to match the deployed value. **Full loop run against the live deployed backend (not a local server) with a DB-inserted test customer (`billing-test@reviewguide.eu`), mirroring ticket 4.2's "test subscription" allowance in the ticket text:** (1) `POST /api/billing/checkout` returned a genuine `checkout.stripe.com/c/pay/cs_test_...` URL and, as a side effect, created+persisted a real Stripe Customer (`stripe_customer_id` written to the DB row) — confirms `_get_or_create_stripe_customer` end-to-end; (2) rather than driving Stripe's hosted Checkout UI via browser automation to complete a card-free trial, created a real `Subscription` directly against that same Stripe customer (`trial_period_days=14`) — this is a genuine Stripe object indistinguishable from one Checkout would have produced, and Stripe fires the identical `customer.subscription.created` webhook for it either way, so it exercises the full webhook path without needing to script a multi-step hosted-JS checkout form; **within 5 seconds the live webhook updated the DB row to `subscription_status='trialing'` with zero manual intervention**, and `GET /api/billing/status` against the live backend confirmed `{"subscription_status": "trialing", "has_subscription_ever_started": true}`; (3) canceling that subscription (`Subscription.delete`) fired `customer.subscription.deleted` live and the DB correctly reset to `subscription_status='none'` (the handler's intentional deleted→`none` mapping, not a bug) — confirms all three of the ticket's required event types round-trip through the real webhook signature verification, not just the mocked unit tests. Test customer row deleted from RDS and the test Stripe Customer deleted from Stripe afterward; 226/226 backend tests still pass post-verification. **Not done, disclosed:** the hosted Stripe Checkout page itself (card-collection-skipped confirmation click) was not driven through a real browser — the `checkout_url` was confirmed genuine and reachable but the human-facing "Rozpocznij okres próbny" → Stripe page → back to `/app` click-path is unverified beyond the Route Handler → redirect-URL layer already covered by ticket 4.3's earlier local `/app` testing (button → graceful error when unconfigured); worth a Stakeholder or PM click-through before 4.5's milestone if a fully human-driven pass through Stripe's own UI is wanted. | ✅ ACCEPTED (PM, 2026-08-07) — live webhook endpoint over ephemeral tunnel approved; env-drift catch on AUTH_JWT_SECRET valued; hosted-Checkout human click-through deferred to 4.5 milestone. |
| 4.4 | Hardening build + CUTOVER.md (no prod exec) | ✅ | docs/CUTOVER.md (new) | Full 8-step ordered runbook (private RDS + NAT + Secrets Manager) with rollback per step, written against the real live AWS state (VPC/subnet/SG/RDS/App Runner IDs pulled via `describe-*`, nothing guessed). **No prod resource created or modified**, per the ticket's explicit instruction, except two items the ticket itself calls out as safe to pre-create: (1) the Secrets Manager secret (`reviewpilot-backend/prod-env-secrets`) populated with the 8 true secrets classified out of the 12 live env vars — inert until an instance role references it; (2) the IAM role click-path doc, confirmed to genuinely require a human (this session's own `cursor-dev` credentials were denied even `iam:SimulatePrincipalPolicy`, so it's not just convention). **Design simplification found while rehearsing:** RDS doesn't need a subnet migration to go private — only `PubliclyAccessible=false` + an SG edit; only the App Runner VPC connector needs new subnets/NAT (since attaching a connector routes *all* app egress through the VPC, not just DB traffic), which shrinks the RDS-side blast radius to a flag flip with no reboot. **Ordering designed for zero planned downtime:** attach the connector while RDS is still publicly accessible (its endpoint already resolves to the private IP from inside the VPC via AWS's own split-horizon DNS) → verify `/health` + external API reachability over the new path → only then flip `PubliclyAccessible=false` → only then remove the `0.0.0.0/0` SG rule. Both App Runner-touching steps rely on App Runner's existing health-check-gated auto-rollback (already the mechanism behind tickets 3.2/4.2/4.3's env var updates) as the safety net, not a new one built for this ticket. **Rehearsal evidence, run for real today:** every EC2-level command (`create-subnet` ×2, `create-route-table`, `allocate-address`, `create-security-group`, and — against the real, currently-live rule — `revoke-security-group-ingress` on the `0.0.0.0/0` rule) passed `--dry-run` (`Request would have succeeded`). RDS/App Runner APIs have no dry-run; validated instead via live `--generate-cli-skeleton` schema checks confirming `RuntimeEnvironmentSecrets`, `NetworkConfiguration.EgressConfiguration`, and `InstanceConfiguration.InstanceRoleArn` all exist with the shape the runbook uses. **Real open item disclosed, not solved here (out of this ticket's scope):** once RDS is `PubliclyAccessible=false`, every local/CI direct-`psql` debugging session this project has used since Sprint 0 permanently loses access — CUTOVER.md §6 lists the standard follow-up options (SSM bastion, VPN, in-VPC job) without picking one, since the ticket didn't ask for that build. Single NAT Gateway (not per-AZ) keeps cost at the budgeted ~$37/mo, disclosed as a single point of failure for outbound-only traffic. Full runbook — cost estimate, downtime assessment (none planned), verification checklist, and the IAM console doc — is in `docs/CUTOVER.md`, pasted to the PM in chat this same turn per the ticket's explicit review-before-done requirement. **Not marked ✅ yet — awaiting PM sign-off on the runbook itself before this row closes, and separately awaiting the Stakeholder's NAT-timing go/no-go before any of §3's steps actually execute.** <br><br>**PM amendment, 2026-08-07 (before execution):** new Step 0 (SSM bastion bridge — t4g.nano, no public IP, Session Manager port-forward, `DATABASE_URL` override so every `RUNBOOK_LEADS.md` command works unchanged) added as a cutover *prerequisite*, not a follow-up — local pipeline-job debugging access was flagged as an open question in the original runbook and the PM verdict closed it before execution rather than after. Step 6's debugging-access paragraph rewritten: bastion is the bridge, moving job execution into App Runner itself (BACKLOG's "Pipeline ops in admin panel" item, already scoped as needing Sprint 4's real auth) is the permanent Sprint 5/6 fix. Step 6's command gained an explicit note that `--apply-immediately` will drop (not corrupt) any still-open public-path connection, which must reconnect — App Runner's own connections are already on the private path by that point and are unaffected. All three new/changed commands (bastion SG, `run-instances`, plus the existing dry-run set) re-verified with `--dry-run` before being trusted. <br><br>**CUTOVER EXECUTED LIVE, 2026-08-07 (Stakeholder: "GO NOW. Full cutover today.").** All 8 steps + Step 0 run in order against the real production service, each gated on its own verification before the next step ran; pre-change `SourceConfiguration` snapshots saved to disk before steps 5 and 8. **Step 0 (bastion):** `t4g.nano` (`i-0db2ab990c2c8a354`) launched in the new private subnets, no public IP; SSM Agent registered online; a Session Manager port-forward tunnel (`aws ssm start-session ... AWS-StartPortForwardingSessionToRemoteHost`) to RDS:5432 verified end-to-end by reading the live 213-lead count through a `DATABASE_URL` override, confirming `RUNBOOK_LEADS.md`'s existing local commands keep working unchanged once RDS goes private. **Steps 1-3:** private connector subnets, NAT Gateway (`nat-0f57d8501579c3813`) + Elastic IP + private route table, connector security group all created and associated; all matched their dry-run rehearsal exactly. **Step 4-5:** App Runner VPC connector created and attached — service completed a rolling deploy to `RUNNING` on the new network path with zero manual restart; `/health` and `/api/admin/stats` (DB read) and a live Stripe call (external reachability) all verified green over the connector+NAT path before touching RDS itself. **Step 6:** RDS flipped `PubliclyAccessible=false` with `--apply-immediately`; verified the public DNS path is genuinely gone (resolves to a private IP, connection refused from outside the VPC) while App Runner and the bastion tunnel both stayed healthy throughout — confirming existing DB connections re-established on the private path exactly as the PM amendment's added note anticipated, no dropped traffic observed. **Step 7:** the `0.0.0.0/0` and developer-IP ingress rules removed from the RDS security group, leaving exactly the connector's and bastion's security-group references (verified via `describe-security-groups`); App Runner stayed healthy. **Console tasks (Stakeholder, both confirmed done same day):** IAM roles `reviewpilot-bastion-instance-role` and `reviewpilot-backend-instance-role` created via the runbook's own click-path doc; `cursor-dev` granted `iam:PassRole` scoped to both roles so this session could attach them without gaining broader IAM write access. **Step 8:** App Runner's `RuntimeEnvironmentVariables` cut from 12 keys down to the 4 true non-secrets (`APP_ORIGIN`, `PYTHONPATH`, `REPLY_ADDRESS`, `STRIPE_PRICE_ID`); the other 8 (`ADMIN_API_KEY`, `ANTHROPIC_API_KEY`, `AUTH_JWT_SECRET`, `DATABASE_URL`, `OUTSCRAPER_API_KEY`, `POSTMARK_TOKEN`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`) moved to `RuntimeEnvironmentSecrets` pointing at the pre-created Secrets Manager secret, `InstanceRoleArn` attached — deploy succeeded (`RUNNING`) with no `PassRole`/permission error, confirming the console task was done correctly on the first try. **Full §4 post-cutover checklist run against the live system, not just each step's own gate:** `GET /health` → 200 `{"status":"ok","db":"ok"}`; a real `python -m app.jobs.enrich --recheck` run **locally through the bastion tunnel** (not App Runner — per the PM amendment, local pipeline jobs are bastion-bridged, not yet hosted in-VPC) read the live DB correctly (7 candidate leads, 3 domains, cost estimate printed); `reviewguide-app`'s `/app` billing card resolved a real status end-to-end through a full fresh magic-link loop (real Postmark send → Postmark activity API confirms `Status: Sent` → interstitial GET → POST-consume → session cookie → `/app` rendered "Rozpocznij okres próbny", proving both the private-RDS customer lookup and the NAT-routed Stripe status call both worked); `describe-security-groups` on the RDS SG shows zero CIDR-based rules, exactly two source-group rules (connector + bastion); `describe-db-instances` confirms `PubliclyAccessible: false`; `describe-service` confirms the plain env vars are down to exactly the 4 non-secret keys. **One checklist item only indirectly verified, disclosed rather than silently skipped:** `reviewguide-app`'s `/admin` page itself is gated by a Netlify-managed HTTP Basic Auth secret (`ADMIN_PASS`) this session never held in plaintext (same masking behavior encountered with `AUTH_JWT_SECRET` mid-ticket 4.2) — the gate itself was confirmed fail-closed (401 without valid creds), and the page's actual data call is the same `/api/admin/stats` backend endpoint already verified above with real counts, so the chain is verified end-to-end minus the basic-auth click itself. Test customer row and its `auth_tokens` row deleted from RDS after verification (same hygiene as tickets 4.2/4.2b/4.3); bastion SSM tunnel processes killed at the end of this session (the bastion instance itself stays running, by design, as ongoing debugging infrastructure per Step 0). **Cost:** NAT Gateway adds the budgeted ~$37/mo (hourly charge + per-GB data processing), effective today. **Zero customer-facing downtime observed** across the whole runbook, matching the plan's own zero-planned-downtime design. | ✅ ACCEPTED (PM, 2026-08-07) — design simplification, connector-first ordering, secret classification, scoped IAM approved. Amendment: local pipeline jobs (lead engine) depend on public RDS — bastion added as cutover prerequisite. Cutover executed same day per Stakeholder go-ahead; full post-cutover checklist green. |
| 4.5 | End-to-end milestone (landing→trialing) | ✅ | Backend: app/main.py (CORS dual-origin). App repo (reviewguide-app): app/(customer)/auth/verify/page.tsx, app/(customer)/auth/verify/VerifyForm.tsx (new, replaced the short-lived VerifySubmitButton.tsx), playwright.config.ts (new), e2e/verify-interstitial.spec.ts (new), e2e/live-login.spec.ts (new) | **Branded domain cutover (prerequisite, done before the walkthrough attempt):** `app.reviewguide.eu` attached as the `reviewguide-app` Netlify site's custom domain (SSL auto-provisioned, verified `CN=app.reviewguide.eu` on the live cert), marketing site's `NEXT_PUBLIC_APP_URL` flipped to it and redeployed, `reviewguide.eu`'s CTA confirmed pointing at `https://app.reviewguide.eu/signup`, old `dynamic-puppy-631956.netlify.app` URL confirmed still serving 200s throughout (no redirect configured, existing links unaffected). <br><br>**First Stakeholder walkthrough attempt, 2026-08-07, FAILED at step 4-5 — two real findings, both root-caused from data before fixing (a milestone walkthrough catching bugs is the walkthrough doing its job):** <br><br>**Finding 1 (confirmed config bug — the domain cutover above was incomplete):** the magic-link email Postmark actually sent linked to `https://dynamic-puppy-631956.netlify.app/auth/verify?token=...`, not `app.reviewguide.eu` — confirmed by pulling the real message body via Postmark's API. Root cause: `APP_ORIGIN` on App Runner was never updated when the custom domain was attached; `app/routers/auth.py`'s `magic_link_url = f"{settings.app_origin}/auth/verify?token=..."` is genuinely config-driven (not hardcoded, not derived from request headers — verified by reading the code, not assumed), so this was purely a missed env var, not a code bug. Fixed: `APP_ORIGIN` updated to `https://app.reviewguide.eu` on App Runner (the other 3 plain env vars and all 8 Secrets-Manager-backed ones left untouched), redeployed, verified `RUNNING`. **Disclosed second-order issue caught while fixing, not asked for but too easy to miss to skip:** `app/main.py`'s CORS middleware reuses the exact same `settings.app_origin` value, so fixing it would have silently started rejecting cross-origin calls from the old domain — added `LEGACY_APP_ORIGINS` (the old Netlify default domain) alongside the current origin rather than a single-origin swap, so any still-open tab or bookmarked old link doesn't also get a CORS error on top of whatever else might be stale about it. In practice this is defense-in-depth either way (confirmed by grep: every `reviewguide-app` client-side `fetch()` call is same-origin to the app's own Route Handlers, never cross-origin straight to the backend), but "verify, don't assume" per the ticket's own instruction. Live-verified: a fresh `POST /api/auth/request-link` now produces an email whose link is `https://app.reviewguide.eu/auth/verify?token=...`; `OPTIONS` preflight checks against the live backend confirm both `https://app.reviewguide.eu` and the legacy origin get `Access-Control-Allow-Origin` echoed back correctly. <br><br>**Finding 2 (diagnosed from data, not guessed — turned out to be neither expiry nor a real single-use bug):** pulled the exact `auth_tokens` row for the Stakeholder's real signup email — `created_at` 09:38:50 UTC, `expires_at` 09:53:50 UTC (the standard 15-min TTL, nowhere close to elapsing), `used_at` 09:40:09 UTC — token consumed successfully only **79 seconds** after send. Cross-checked against App Runner's application logs for the same window: two `POST /api/auth/verify` calls, same source connection, **4.7 seconds apart** — `200 OK` at 09:40:09.323Z, then `401 Unauthorized` at 09:40:14.061Z. This is exactly what the Stakeholder saw as "already used/expired": the first submission actually succeeded (created his `customers` row, `subscription_status='none'`, matching what this session's own live DB monitoring caught in real time), and a second submission 4.7s later — his own retry, most likely a second click on a plain unguarded `<button>` that gave no visual feedback that the first click's navigation was already in flight, plausibly made worse by Finding 1 having just landed him on an unfamiliar domain — hit the backend's atomic single-use check exactly as designed and was correctly rejected. **Verdict: not a token-expiry problem (79s ≪ 15min, no TTL change warranted) and not a backend consumption bug (the single-use enforcement did precisely its job both times — this is what it's for)**; it's a frontend UX gap that turns a legitimate double-click into a confusing error screen instead of a harmless no-op. Fixed: `VerifySubmitButton` (new client component, no network call of its own — the form still does a real POST navigation to `/api/auth/complete-verify` unchanged, so ticket 4.2b's "directly-curlable consuming endpoint" property holds) disables itself and shows "Logowanie…" on click, so a second click before the page navigates away is now a no-op instead of a second POST. Verified via `tsc --noEmit`/`eslint` clean (no Playwright available in this repo to click-test live, disclosed rather than silently skipped); backend's own single-use behavior is unchanged and still covered by ticket 4.2b's tests. Both fixes committed (`fix: CORS admits both current and legacy app origins post-domain-cutover` / `fix: disable interstitial submit button to prevent double-submit`), pushed, backend auto-deployed and reached `RUNNING`; 226/226 backend tests still pass. The failed attempt's partial `customers`/`auth_tokens` rows (`pedram@defraged.com`) deleted — not the milestone's real evidence trail, just a bug repro. **Stakeholder asked to re-run the full 7-step walkthrough from scratch (fresh signup, new email request) now that both fixes are live.** <br><br>**Second Stakeholder walkthrough attempt, 2026-08-07, FAILED at step 5 — Finding 3, and it was a regression introduced by Finding 2's own fix. Owning that plainly: the previous fix shipped on `tsc`/`eslint` clean with "no Playwright available to click-test" disclosed as a known gap, and that exact gap is what let a submit-cancelling bug reach the Stakeholder.** <br><br>**Finding 3 (diagnosed from data before touching code, and the data was unambiguous):** the Stakeholder tapped the interstitial button at ~10:06 UTC; the button flipped to "Logowanie…" and hung forever with no navigation. Pulled the `auth_tokens` row: `created_at` 10:04:22 UTC, `expires_at` 10:19:22 UTC, **`used_at` NULL** — never consumed. Cross-checked App Runner application logs across the full 10:04–10:11 UTC window: exactly **one** non-health request in the entire window, the `POST /api/auth/request-link` at 10:04:23 that sent the email, and **zero `POST /api/auth/verify`**. So the request never left the browser at all — this was never a backend, token, expiry or CORS issue. **Root cause:** `VerifySubmitButton` set React state in the *button's* `onClick`, flipping the button's `disabled` prop. `onClick` runs *before* the browser's form-submission algorithm, and a disabled submit control cannot activate form submission — so the guard silently cancelled the very submission it was added to protect. The "Logowanie…" label the Stakeholder saw was itself the proof rather than a symptom of slowness: it could only render because the page never navigated away. **The initial mobile-race hypothesis was disproved by the evidence** — the regression test fails identically on desktop Chromium, so this was never iOS-specific; desktop simply had not been tested either. **Fix:** replaced with `VerifyForm`, which moves the guard to the *form's* `onSubmit` (where submission is already committed and cannot be cancelled) and implements it as a synchronous `useRef` latch rather than React state, so it is immune to render timing — the first submit passes through completely untouched and only repeats are `preventDefault()`ed. Nothing is ever `disabled`. The form is still a plain `<form method="POST">` to `/api/auth/complete-verify`, so ticket 4.2b's directly-curlable-consuming-endpoint property is unchanged. **Deliberately removed the "Logowanie…" pending state rather than re-implementing it safely:** once a native form POST begins the browser suspends the page, so a React re-render never paints — verified empirically, the state-based version failed that assertion on both mobile and desktop even with the disable deferred past submission. A pending label there could only ever become visible when the flow is broken, which is precisely backwards. Users get the browser's own native loading indicator instead, and Finding 2's actual harm is fixed by the ref latch making a stray second click a genuine no-op. **Testing gap closed:** Playwright added to `reviewguide-app` (`npm run test:e2e`) with 3 specs × mobile + desktop projects, all green — asserting that a click actually fires a POST carrying the token, that a second click while the first is in flight fires no second POST, and that the page actually navigates instead of hanging (the Stakeholder-visible symptom). **These were validated as genuine regression tests, not just green ones:** temporarily restoring the broken component made "POST actually fires" and "page actually navigates" fail on both projects, and only those; the fix turns them green. **Disclosed limitation:** Playwright's only WebKit build for macOS 14 is the frozen `webkit_mac14_special`, which crashes on launch (`Bus error: 10`) on this machine, so the mobile project is device-class (touch input, mobile viewport, mobile UA) on Blink, **not** the iOS engine — stated rather than glossed as "tested on iOS Safari". Mitigating: the underlying behavior (a disabled submit control cannot submit) is spec-level and engine-independent, and it reproduced identically on desktop. True iOS confirmation is the Stakeholder's own retry. **Live-verified on the deployed site, not just locally:** added an `E2E_BASE_URL` override plus an opt-in `e2e/live-login.spec.ts` that consumes a real single-use token against `https://app.reviewguide.eu` in a mobile context — interstitial renders, click fires the POST, backend consumes the token (`used_at` 10:32:36 UTC confirmed in RDS), redirect lands on `/app`, and the session cookie asserts `httpOnly=true`, `secure=true`, `SameSite` Lax — with a reload proving the cookie is actually replayed and the session survives, which clears the Set-Cookie/redirect concern for the same-origin Route Handler flow. The test token was seeded directly into `auth_tokens` rather than requested through Postmark, specifically so the live check would not pollute the Postmark activity feed being monitored for the Stakeholder's run; the resulting e2e `customers`/`auth_tokens` rows were deleted afterwards, leaving `customers` at 0 rows so the retry starts clean. Commits `fix(auth): stop the double-submit guard from cancelling the login submit` (d4f3097) and `test(e2e): allow the suite to run against the deployed site` (bcd4a28), pushed; Netlify deploy confirmed `ready`. **The Stakeholder's stuck token expired unused at 10:19:22 UTC, so the retry must start from a fresh link.** <br><br>**Third Stakeholder walkthrough attempt, 2026-08-07 — PASSED end to end, landing→trialing in ~96 seconds, on the Stakeholder's own real device with no intervention.** Monitored live across four independent sources (RDS row-change watcher, App Runner application logs, Postmark outbound API, Stripe API). Full evidence trail, all times UTC: <br>• `10:37:02.554` — `POST /api/auth/request-link` 200; `auth_tokens` row created. <br>• `10:37:04` — Postmark outbound "Twój link logowania do ReviewGuide" → `pedram@defraged.com`, status `Sent`. <br>• `10:37:30.033` — token consumed, `used_at` set. **Exactly one `POST /api/auth/verify` (200) in the whole window — no second 401**, so Finding 2's double-submit guard and Finding 3's fix are confirmed working together on real mobile hardware, which is also the true iOS confirmation that the frozen-WebKit limitation above prevented locally. <br>• `10:37:33` — `GET /api/billing/status` 200; `/app` rendered the billing card. <br>• `10:38:17` — `POST /api/billing/checkout` 200 → hosted Stripe Checkout. <br>• `10:38:21` — `customers.stripe_customer_id` = `cus_V1onnk07RzXhpE`. <br>• `10:38:29` — subscription `sub_1U1lAXIDs1qO8e1Tnp2yfvlA` created, `trial_end` `2026-08-21 10:38:29` (exactly 14 days). <br>• `10:38:31` — `POST /api/billing/webhook` 200 (signature-verified), immediately followed by `GET /api/billing/status` 200. <br>• `10:38:38` — `customers.subscription_status` = `trialing`. <br><br>**Two spec guarantees confirmed against reality rather than against mocks for the first time:** the subscription reports `livemode: False` (genuinely TEST mode, as required), and `default_payment_method: None` — **the trial started with no card collected at all**, proving ticket 4.3's `payment_method_collection="if_required"` actually behaves as specced in real hosted Checkout, not just under the mocked tests and Stripe CLI runs it was originally verified with. **This closes the item PM explicitly deferred from the 4.3 verdict ("hosted-Checkout human click-through deferred to 4.5 milestone").** <br><br>**Test data deliberately retained, not cleaned up:** the `customers` row for `pedram@defraged.com` (`stripe_customer_id` `cus_V1onnk07RzXhpE`, `subscription_status` `trialing`) is the milestone's evidence and is **left in place per instruction — do not delete**. **Disclosed:** it is flagged as `STAKEHOLDER-TEST` *here in this document only*, not in the database — the `customers` table has no notes/flag column, and the honest options were to add a migration for a one-off marker or to repurpose an existing column (`place_id`/`notification_email`), the latter of which would have corrupted real field semantics for a labelling convenience. Neither seemed worth it for a single identifiable row; it is unambiguously identified by that email and Stripe customer ID. **Carry to Sprint 5 as a cleanup precondition: this row and its TEST-mode Stripe subscription must be removed before the first real customer onboards** (added to BACKLOG). <br><br>**Milestone verdict: the full commercial path a real customer would walk — `reviewguide.eu` → CTA → `/signup` → real Postmark magic-link email → interstitial → `/app` → "Rozpocznij okres próbny" → hosted Stripe Checkout → webhook → `/app` showing "trialing" — now works end to end on production infrastructure behind the hardened private-RDS network, on a real device, unassisted.** Ready for PM review. | ✅ ACCEPTED (PM, 2026-08-07) — milestone demonstrated on Stakeholder's device, 96s landing→trialing, four-source evidence trail. Trial-without-card confirmed in real hosted Checkout (closes 4.3's deferred item). Three walkthrough-caught bugs (APP_ORIGIN, double-tap, submit-canceling guard) fixed same-day — the milestone process working as intended. |
| 4.6 | Card-upfront copy finalization + brand assets + section elevation | ✅ | Backend: app/routers/billing.py, tests/test_billing.py (both unchanged from CR-1 — already `payment_method_collection="always"`, re-verified, no drift). Marketing (reviewguide-marketing): components/pricing-section.tsx, components/faq-section.tsx, components/ui/illuminated-hero.tsx, components/site-nav.tsx, components/demo-section.tsx, components/how-it-works.tsx, app/layout.tsx, app/globals.css, app/icon.tsx (deleted), app/opengraph-image.tsx (deleted), scripts/generate-brand-assets.cjs (new), package.json (+png-to-ico devDep), README.md, public/favicon.ico, public/icon-192.png, public/icon-512.png, public/apple-touch-icon.png, public/og-image.png, public/brand/icon-flat-silhouette.png (all new/generated). App repo (reviewguide-app): app/layout.tsx, app/favicon.ico (replaced), app/(customer)/layout.tsx, public/icon-192.png, public/icon-512.png, public/apple-touch-icon.png, public/brand/icon-source.png, public/brand/icon-flat-silhouette.png (all new/copied) | **Filed as a Sprint 4 amendment ticket per the Stakeholder's own framing** (re-reads SPRINT_04.md + ROADMAP §4b), even though the underlying trial-payment-model change was already built and live-verified as **CR-1** in the new Sprint 6 section two sessions ago — this ticket **finalizes the exact landing copy** (different wording than CR-1 shipped, see below) and adds two workstreams CR-1 never scoped (brand assets, visual elevation). CR-1's row is left as-is (it's the accurate record of the backend change + Stripe research + live verification); this row supersedes only the marketing copy strings and adds the new material. <br><br>**PART A — card-upfront alignment:** `payment_method_collection` re-checked in `app/routers/billing.py` — already `"always"` from CR-1, zero drift, `test_billing.py`'s assertion already matched; no backend change needed this round. **Pricing card subtext replaced** (CR-1's "14 dni za darmo · anuluj w każdej chwili · pierwsza płatność dopiero po okresie próbnym" → this ticket's exact wording): **"14 dni za darmo — karta wymagana, 0 zł przez okres próbny."** **FAQ answer replaced** (CR-1's longer version → this ticket's exact wording): **"Tak — podajesz kartę przy starcie, ale przez 14 dni nie pobieramy żadnej opłaty. Możesz anulować w każdej chwili w panelu, zanim naliczy się pierwsza płatność."** **Hero third line changed**: "Zanim zdążysz się zdenerwować." → **"Zanim zdążysz ją przeczytać."** (same position/styling, `components/ui/illuminated-hero.tsx`). Repo-wide grep for "bez karty" (marketing + app, `.tsx`/`.ts`/`.md`) returns **zero hits**, confirmed before and after. <br><br>**PART B — brand assets**, source `public/brand/icon-source.png` (Stakeholder-provided, 975×1024 RGBA, confirmed present before starting — the ticket's own "fail loudly if missing" branch in `scripts/generate-brand-assets.cjs` was never exercised because it was there). New `scripts/generate-brand-assets.cjs` (Node + `sharp`, committed, re-runnable): generates `favicon.ico` (16/32/48), `icon-192.png`, `icon-512.png`, `apple-touch-icon.png` (180×180, padded onto `#0A0806` — not left transparent, since iOS fills transparent apple-touch-icons with white), and `og-image.png` (1200×630). **Favicon legibility check actually run, not assumed:** rendered the photoreal source at true 16px and separately a hand-authored flat-silhouette variant (dark bubble `#241a12` + solid amber `#ffb069` star, no metallic texture) at 16px, both upscaled 8× nearest-neighbor for real inspection — the photoreal render was an illegible muddy brown blob at that size, the flat version read clearly as a star-in-bubble mark. **Decision: flat silhouette ships in `favicon.ico`**; full-detail source stays in `icon-192/512`/`apple-touch-icon`/OG where it renders cleanly. **OG image** built via `next/og`'s `ImageResponse` (Satori) rather than plain `sharp` compositing, specifically so the "ReviewGuide" wordmark renders in the *actual* bundled Geist font (`node_modules/next/dist/compiled/@vercel/og/Geist-Regular.ttf`) instead of a system-font guess: icon at ~400px left with a soft amber radial glow behind it, wordmark + tagline to the right, `#0A0806` background — two iterations to fix initial edge-crowding on "Google". Wired into `app/layout.tsx`'s Metadata API (`icons`, `openGraph.images`, `twitter.images`) rather than file-convention; the old `app/icon.tsx`/`app/opengraph-image.tsx` placeholders (generic gradient "R" glyph, from before a real mark existed) **deleted**, superseded. Full set copied to `reviewguide-app/public/` + `public/brand/`, with the same `icons`/`apple` metadata added to its `app/layout.tsx` (its `app/favicon.ico` file-convention slot replaced directly with the generated flat-silhouette `.ico`). **Icon placed in both UIs**: marketing `site-nav.tsx` navbar (28px, `icon-192.png`, left of the wordmark) and the app's `(customer)/layout.tsx` top bar (same 28px treatment) — covers `/signup`, `/login`, `/app` per the ticket (`/admin` inherits nothing new, unchanged, correct per spec). <br><br>**PART C — section elevation** (hero untouched, confirmed by diff): **Demo block** (`demo-section.tsx` + new `.demo-connector`/`.demo-response-card` rules in `globals.css`): review card desaturated/dimmed (`text-white/60`, `border-white/[0.06]`, `saturate-[0.85]`); response card gets a left amber border + inset amber glow + bolder amber "ODPOWIEDŹ REVIEWGUIDE" label; a small downward chevron connector SVG sits between the two boxes in each pair. **Three-step cards** (`how-it-works.tsx` + `.step-card`/`.step-card-number` rules): hover lift (`translateY(-4px)`) + a top-edge amber gradient line that fades in on hover; step numbers rendered large (`text-4xl`) and ghosted via `-webkit-text-stroke` (outline only, transparent fill) instead of solid amber. **Pricing card** (`pricing-section.tsx` + `.pricing-card-frame`/`.pricing-card-inner`): wrapped in a 1px gradient-border frame (amber→transparent→amber) with a near-opaque dark inner panel — **first pass leaked the full amber gradient across the entire card interior** because `backdrop-blur` on a translucent (`bg-white/[0.06]`) inner panel sampled the gradient frame directly behind it; fixed by swapping the inner panel to a near-opaque `bg-[#0b0906]/95` so the gradient only shows as the intended thin rim, with a separate subtle inset amber glow (`.pricing-card-inner`) for warmth — caught by actually rendering and looking at it, not by reading the CSS. Added the 48px `icon-192.png` at the card's top. **All new hover/transition effects guarded** under a `@media (prefers-reduced-motion: reduce)` block in `globals.css` (transitions disabled, hover transform removed), following the same pattern already established for the hero's orb animation and the glow button. <br><br>**Verification:** `npm run build` clean on both frontend repos (Turbopack, TypeScript, static export for marketing / SSR for app); full backend suite **353/353** (bastion tunnel opened for this run — `test_health`'s DB check needs it; the very same suite reports 352/353 with only that one failing when the tunnel isn't open, confirming it's the known local-connectivity gap, not a regression). `grep -ri "bez karty"` across both frontend repos: **zero hits**. **Lighthouse re-run against the built static export** (headless Chrome via `npx lighthouse`, `npx serve out` locally): mobile performance **88/100** (target ≥85), desktop performance **100/100** (target ≥90) — both comfortably clear, no regression from the new CSS (pre-change baseline was 87/96 on the same machine). Screenshots of all four changed sections (steps, demo, pricing, navbar) plus both favicon variants captured via a scratch `puppeteer-core` script driving the system's own Google Chrome (no `playwright`/`puppeteer` available as a project dependency in either frontend repo) and shown to the PM/Stakeholder in-chat this session. | Pending PM review — supersedes CR-1's landing copy with this ticket's exact wording; brand assets and section elevation are new material CR-1 never scoped. |

> Sprint closed — summary row in **Closed sprints** below. The per-ticket detail above is kept
> in place rather than condensed away (unlike Sprints 0–1): it is the only written record of the
> security cutover's execution and of the 4.5 milestone's four-source evidence trail, both of
> which are the kind of thing an audit or a post-incident review asks for months later.

### Sprint 4 blockers
_(none at open — Stakeholder actions table gated 4.2/4.3 mid-ticket; all cleared)_

### Sprint 4 open questions for Stakeholder
- ~~Postmark account + mail.reviewguide.eu DNS (gates 4.2)~~ — **done 2026-08-07**, live and
  verified (see ticket 4.2/4.2b notes above).
- ~~Stripe account (gates 4.3)~~ — **done 2026-08-07**, TEST-mode key provided, "ReviewGuide"
  129 zł/mies product+price and live webhook endpoint created; full checkout→webhook→trialing
  loop verified live (see ticket 4.3 notes above).
- **STILL OPEN, carried past the sprint close — G3: launch price** (99 / 129 / 149 zł). ROADMAP
  §gates times this at "end of Sprint 4", which is now. 129 zł is already live on the landing page
  and is the price of the real Stripe product, so this is no longer a paper decision: changing it
  later means editing published pricing and creating a new Stripe price. Stakeholder decision
  needed.
- ~~NAT timing: cutover now or launch week (gates 4.4 execution)~~ — **decided
  2026-08-07: now.** Full cutover executed same day (private RDS + NAT + Secrets Manager); see
  ticket 4.4 notes above.
- ~~Create empty GitHub repo reviewguide-marketing (gates 4.1 push)~~ — **done 2026-08-07**, repo
  live and deployed.
- ~~STILL OPEN — `reviewguide-marketing` push-to-deploy (ticket 4.1)~~ — **RESOLVED 2026-08-07.**
  Stakeholder granted the Netlify GitHub App repository access. Verified with two empty-commit
  pushes: the first (`dafad1e`, before the grant) triggered **no** deploy after a 5-minute poll,
  confirming the gap was real and not a timing fluke; the second (`4878a68`, after the grant)
  auto-triggered deploy `6a75bf66f5ef4c0008b65add` within seconds — `commit_ref` matches exactly,
  `state: ready`, published 23s after creation, and `reviewguide.eu` serves 200 with that content
  live. Push-to-deploy on `reviewguide-marketing` now works exactly like `reviewguide-app`'s.

---

## SPRINT 5 — Value Delivery — ✅ CLOSED 2026-08-09

| # | Ticket | Status | Files touched | Cursor notes | PM verdict |
|---|---|---|---|---|---|
| 5.1 | Connect flow + day-one value | ✅ | alembic/versions/005_customer_connect_alerts.py (new), app/models.py, app/routers/customer.py (new), app/jobs/day_one.py (new), app/services/maps_url.py (new), app/prompts.py, app/services/claude_client.py, app/services/postmark_client.py, app/templates.py, app/auth.py, app/routers/billing.py, app/logic_rules.py (new), app/jobs/qualify.py, app/main.py, tests/test_maps_url.py, tests/test_prompts_positive.py, tests/test_day_one.py, tests/test_customer.py | **Migration 005** (applied live to prod RDS): `customers.tone_preference TEXT DEFAULT 'formal'`, `customers.connected_at`, and `alerts(alert_id PK, customer_id FK, review_id FK, response_text, is_urgent, kind, sent_at, postmark_message_id, created_at)` — schema verified column-by-column against `information_schema.columns` post-apply, exact match to spec. **`GET /api/customer/search-place`**: session-auth (4.2's JWT, not the admin key), routes through the existing `OutscraperClient`/`cost_guard` so LOGIC §4's abort-before-spend applies unchanged; returns name/address/rating/place_id, capped at 5. **`POST /api/customer/connect-place`**: accepts `place_id` OR `maps_url`; refuses 409 if `customer.place_id` is already set; upserts into the **shared** `places` table with `COALESCE(existing, new)` on name/address/rating (same non-destructive-merge pattern as `app/jobs/enrich.py`'s `apply_contacts()`) so a customer connecting a restaurant the Sprint 1 sweep already discovered can't clobber richer swept data with thinner customer-supplied metadata. **`app/services/maps_url.py`** (new): parses `place_id` from a query param or a raw `ChIJ...` token, resolves `goo.gl`/`maps.app.goo.gl`/`app.goo.gl` short links via one `httpx` redirect-following GET, and extracts a `suggested_query` from the URL's `/maps/place/<name>` segment for the search fallback when only a feature ID (not a resolvable place_id) is present in the URL — returning `422` with `suggested_query` rather than guessing wrong. **Day-one job (`app/jobs/day_one.py`)**: reuses the connect request's own DB session (no new connection); skips the Outscraper call entirely when `place.last_polled_at` is already set (LOGIC §8a: "free for the 619" already-swept places) — otherwise fetches 10 newest reviews via the existing `fetch_reviews`/`upsert_reviews` pipeline; filters to reviews ≤60 days old; drafts via the new rating-aware `render_for_customer()`; records one `alerts` row per draft (`kind='digest'`); composes one welcome digest email gated by a new `WELCOME_DIGEST_APPROVED_ON` constant (mirrors ticket 2.4's `TEMPLATE_APPROVED_ON` — **stays `None` until 5.4's template is PM-reviewed**, so the job runs and drafts get generated and stored, but no half-approved copy reaches a real inbox yet; matches the ticket prompt's own "stub log ok" allowance). **`app/prompts.py` extended rating-aware** (LOGIC §8a): new `POSITIVE_RESPONSE_PROMPT` + `POSITIVE_PROMPT_VERSION = "1.3"` (Cursor-drafted thank-you variant, added to `SPRINT_05.md` as `## Prompt v1.3`, **not yet PM-reviewed** — flows through the normal review cycle like every SPRINT_02.md tuning round); `render_for_customer(lead)` dispatches to it for `rating >= 4`, else falls through to the existing negative/apology `RESPONSE_PROMPT` unchanged. Doc-parity enforced the same way as the negative prompt (`tests/test_prompts_positive.py` pins both the prompt text and `POSITIVE_PROMPT_VERSION` to the SPRINT_05.md heading). **Refactors along the way, disclosed:** `_detect_health_keyword` moved out of `app/jobs/qualify.py` into new `app/logic_rules.py` as `detect_health_keyword` (qualify.py re-exports it under the old private name so its existing tests are untouched) — needed in two call sites now (System A leads, System B digest drafts) and a second copy of the same regex was the wrong way to get there. `get_current_customer` moved from `app/routers/billing.py` into `app/auth.py` so the new customer router can depend on it without importing across routers. `ClaudeClient` gained `generate_customer_response()` + a private `_call()` extracted from the existing `generate_response()`. `postmark_client` gained a generic `send_email(to, subject, body) -> str \| None` (returns the Postmark `MessageID` for persistence) that `send_magic_link_email` now also routes through. <br><br>**Live verification against the real STAKEHOLDER-TEST customer (customer_id 13, `pedram@defraged.com`, the same trialing row ticket 4.5 retained as milestone evidence), 2026-08-07:** migration 005 applied live via the bastion tunnel (`alembic upgrade head`, 004→005, verified against `information_schema`). Deployed to App Runner (auto-deploy on push, reached `RUNNING`, `/health` green). **`GET /api/customer/search-place?q=McDonald's Warszawa Marszałkowska`** against the live backend with a real session JWT returned a genuine Outscraper result (`Restauracja McDonald's`, Świętokrzyska 35, rating 4.2, a real `place_id`) — chosen deliberately as a high-review-volume place, which doubles as useful continuity for ticket 5.5's milestone note about picking a McDonald's-class place if no organic review lands in time. **`POST /api/customer/connect-place`** with that `place_id` against the live backend: real Outscraper fetch of 10 reviews, real Claude calls for all 10 (rating-aware — verified by reading the actual generated text: positive drafts read as warm thank-yous with zero apology language, negative ones open with genuine apology; one review was in Ukrainian and the model correctly matched its own KROK-0 language-detection rule to respond in Ukrainian rather than defaulting to Polish/English, which the prompt text doesn't even explicitly enumerate as a supported language — an unplanned but correct generalization), `digest_sent: false` (as expected — the approval gate). DB verified directly: `customers.place_id`/`connected_at`/`tone_preference` all set correctly, `places` row upserted with the right name/address/rating and `last_polled_at` stamped, exactly 10 `alerts` rows with a correct urgent/non-urgent split by rating. **Already-connected refusal verified live**: a second `connect-place` call on the same session returned `409` with the expected PL message. <br><br>**Real bug found and fixed during this same live verification, disclosed rather than quietly patched:** re-running the day-one job for the now-connected customer (`python -m app.jobs.day_one --customer-id 13 --yes`, checking the "skip-fetch-when-fresh" + idempotency paths the ticket's test list asks for) correctly skipped the Outscraper call and sent no duplicate alerts/digest — but it re-generated **10 real Claude drafts first**, discovering the conflict only at the per-row `ON CONFLICT DO NOTHING` insert, after the money was already spent. Root cause: the idempotency check lived at the DB-write layer, one row at a time, always after that row's Claude call. Fixed same-day: `run_day_one_for_customer` now looks up already-alerted `review_id`s for the customer up front, in one query, and excludes them from both `enforce_call_cap`'s count and the generation loop entirely — a re-run's cost now scales with genuinely new reviews, not the customer's full history. The per-row `ON CONFLICT DO NOTHING` stays as a defense-in-depth race guard (two concurrent runs could still both pass the up-front check for the same review) rather than being removed. Re-ran the exact live scenario after the fix: `$0`, zero Claude calls, correct "already alerted, skipped ($0)" log lines for all 10, finished in ~4s (down from ~74s). Two tests strengthened/added to lock this in (`test_second_run_is_idempotent_and_generates_nothing_new` now asserts Claude is never called on the re-run, not just that no new alert is written; new `test_cap_is_enforced_only_against_pending_not_already_alerted_reviews`). **Disclosed, not cleaned up:** the STAKEHOLDER-TEST customer is left connected to the real McDonald's place with its 10 real drafts in place — this is useful continuity for ticket 5.2 (an already-connected customer to poll) and 5.5 (a high-volume place already identified for the milestone), not just leftover test residue; flagged here the same way ticket 4.5 flagged its own retained Stripe test data. **Scope note on the ticket's test list:** "digest single-send idempotency" is covered (a second run sends no second digest — verified both live and by test), but the actual email *send* itself is unverified end-to-end since `WELCOME_DIGEST_APPROVED_ON` is `None` pending ticket 5.4's template review, exactly as the ticket's own Cursor-prompt text allows ("stub log ok"). 267/268 backend tests pass (39 new + 2 strengthened for this ticket); the one pre-existing failure (`test_templates.py`'s SPRINT_02.md doc-parity check) is ticket 5.4's already-pulled promise-wording fix landing in the doc ahead of the code — disclosed, out of this ticket's scope, not fixed here. `ruff check` clean. <br><br>**Post-verdict follow-up (2026-08-07):** template wording parity fixed same day as flagged (`fix: template wording parity — maks. 2 godzin`), full suite green before the verdict finalized. Prompt v1.3 approved with one amendment — see the KROK-0 third-language line added to both `RESPONSE_PROMPT` (→ v1.2.1) and `POSITIVE_RESPONSE_PROMPT` (stays v1.3, text amended pre-5.2-multiplication) — logged in this same row's next update. | ✅ ACCEPTED (PM, 2026-08-07) — pre-spend idempotency fix commended; live verification incl. organic Ukrainian case. Prompt v1.3 APPROVED with one amendment (KROK 0 third-language line, see follow-up). Draft-3 "confirming facts" lean logged as a tuning observation for the reviewer-feedback round. |
| 5.2 | Polling engine + EventBridge | ✅ | alembic/versions/006_alerts_stop_reason.py (new), app/models.py, app/jobs/day_one.py, app/jobs/poll_customers.py (new), app/routers/jobs.py (new), app/config.py, app/templates.py, app/main.py, tests/test_poll_customers.py (new), tests/test_jobs_router.py (new); AWS: reviewpilot-poll-customers-dlq (new SQS queue) | **Migration 006** (applied live): `alerts.generation_stop_reason` — retroactive gap found while diagnosing the Ukrainian draft's missing trailing punctuation (see below); nullable, mirrors `leads.generation_stop_reason`. **`app/jobs/poll_customers.py`**: core polling logic for LOGIC §8a — eligible customers (`trialing`/`active`, connected), fetches 5 newest reviews/customer, generates rating-aware drafts for genuinely new reviews only (idempotent: pre-checks already-alerted `review_id`s up front, same fix pattern as 5.1's day-one bug, so a re-run never re-spends), enforces caps (`MAX_REVIEW_RECORDS_PER_CUSTOMER=10`, `MAX_RECORDS_TOTAL=500`, `MAX_CLAUDE_CALLS_TOTAL=100`, `MAX_ALERT_EMAILS_PER_CUSTOMER_PER_DAY=10` — abort-before-spend on any total cap, per-customer skip-not-abort on the daily cap) and the 08:00–23:00 Europe/Warsaw window guard (`is_within_poll_window()`, real `ZoneInfo`, half-open `[8,23)`). **`app/routers/jobs.py`**: `POST /api/jobs/poll-customers`, `X-Job-Key` header auth (`secrets.compare_digest`, new `settings.job_api_key`) — separate from both session auth and the admin key since this is a machine-to-machine trigger, not a human one. **`app/templates.py`**: `render_alert_email()` + `ALERT_EMAIL_APPROVED_ON` gate (same pattern as 5.1's `WELCOME_DIGEST_APPROVED_ON` — stays `None` pending 5.4's template review, so the poller is fully functional/testable today without unpolished copy reaching a real inbox). 2 new test files covering: window boundaries, non-eligible no-ops, new-review processing, idempotency (second run = zero new Claude calls), all three total-cap aborts, per-customer daily-cap skip, and the router's auth + delegation. <br><br>**Ukrainian draft investigation (pre-5.2, blocking per PM verdict):** confirmed the alert's `generation_stop_reason` was `end_turn` (not `max_tokens`) — the missing final punctuation is a genuine model quirk on that generation, not a truncation/token-ceiling bug, but the investigation surfaced that `alerts` had no column to answer the question at all (unlike `leads`, which has had `generation_stop_reason` since ticket 2.2 Round 4) — fixed via migration 006 rather than left as a blind spot for future debugging. KROK 0 amendment applied to both prompts per PM verdict (third-language line; `RESPONSE_PROMPT`→v1.2.1, `POSITIVE_RESPONSE_PROMPT` stays v1.3) — doc-parity synced in `SPRINT_02.md`/`SPRINT_05.md`, zero effect on existing PL/EN test assertions (all green). <br><br>**AWS infra — real deviation from the ticket's plan, disclosed:** `JOB_API_KEY` generated, stored in `reviewpilot-backend/prod-env-secrets`, wired into App Runner's `RuntimeEnvironmentSecrets`, redeployed (`/health` green). EventBridge connection (`reviewpilot-poll-customers-job-key`, API_KEY auth) + API destination (`reviewpilot-poll-customers` → the App Runner endpoint) created as planned. **The ticket's assumed design — EventBridge *Scheduler* with a timezone-aware cron directly targeting the API destination — turned out to be impossible**: AWS's `scheduler create-schedule` rejects an API-destination ARN outright (`ValidationException: Provided Arn is not in correct format`) — Scheduler's `Target.Arn` only accepts natively-supported resource types or a generic `aws-sdk:*` universal action target, neither of which can directly invoke a pre-existing API destination. Only the older, separate **classic EventBridge Rules** service (`put-rule`/`put-targets`) can target an API destination directly — confirmed against AWS docs before switching, not guessed. Trade-off disclosed and Stakeholder-approved: classic Rules has no timezone parameter (UTC-only cron), so exact Europe/Warsaw wall-clock alignment isn't possible without a second Scheduler→putEvents→bus→Rule hop (rejected as unnecessary complexity). Landed on `cron(0 6-21/2 * * ? *)` UTC — exact 08–23 Warsaw match in summer (CEST, UTC+2), edge ticks shift an hour in winter (CET, UTC+1); `is_within_poll_window()`'s real `ZoneInfo` check means a mistimed edge tick is skipped cleanly rather than executed at the wrong local hour (fail-safe, not fail-dangerous) — documented in `LOGIC.md` §8a and in the rule's own `--description`. Reused `reviewpilot-scheduler-role` (Step 6 Stakeholder console click-path) rather than creating a second role: required two additional Stakeholder-side IAM edits, both disclosed and applied rather than worked around — (1) the role's trust policy needed `events.amazonaws.com` added alongside `scheduler.amazonaws.com` as a trusted principal, since classic Rules is a different assuming service than Scheduler; (2) `cursor-dev`'s scoped `iam:PassRole` inline policy needed `events.amazonaws.com` added to its `iam:PassedToService` `StringEquals` condition list alongside `scheduler.amazonaws.com`. Rule `reviewpilot-poll-customers-2h` verified `ENABLED`, target verified wired (zero `FailedEntries`). <br><br>**Step 8.1 window-guard test (run live, 2026-08-08 ~01:15 Warsaw — outside window by design):** two back-to-back manual `POST /api/jobs/poll-customers` calls both returned `{"skipped_reason":"outside_poll_window","customers_considered":0,...,"aborted":false}`, HTTP 200, zero customers/records/Claude calls/emails on both — proves the code-level guard is the real backstop regardless of when EventBridge fires. `/tmp` secret backup + scratch config files shredded/removed post-verification per Step 1's condition, confirmed gone. **Still pending, sequenced for after 08:00 Warsaw (not manually forced early — needs the real window open):** the substantive double-fire (actual polling on STAKEHOLDER-TEST's connected McDonald's, zero duplicate alerts on the second call) and letting the first *unattended* scheduled tick fire on its own as the final "no human involved" proof — will report both with counters once observed. <br><br>**Regression found, root-caused, and fixed, 2026-08-08 — the unattended scheduled tick was NOT succeeding.** CloudWatch showed `FailedInvocations: 1` on the 08:00, 10:00, and 14:00 CEST ticks (12:00 CEST's happened to succeed — see below); zero App Runner application-log trace of the request for those three, meaning it never reached the app at all. Stakeholder pulled the scheduler-role trust policy, `cursor-dev`'s `iam:PassRole` condition, and the API destination's status from the console — all confirmed clean (role assumed successfully "1 hour ago" at check time, `events:InvokeApiDestination` correctly scoped, connection `Authorized`, endpoint/method correct). A DLQ (`reviewpilot-poll-customers-dlq`) was attached to the rule's target to capture the next failure's own error attributes as ground truth rather than continuing to guess from absence of evidence. **DLQ message from the 16:00 CEST tick, decoded verbatim:** `ERROR_CODE: UNKNOWN` / `ERROR_MESSAGE: "Unable to invoke ApiDestination endpoint: Timeout calling endpoint for API Destination: socket closed."` / `EXHAUSTED_RETRY_CONDITION: MaximumRetryAttempts` / `RETRY_ATTEMPTS: 2` / `TARGET_ARN` and `RULE_ARN` both correctly resolved to the real API destination and rule (confirmed via `aws events list-targets-by-rule`, which also showed the target's own `RetryPolicy: {MaximumRetryAttempts: 2, MaximumEventAgeInSeconds: 3600}`, matching the DLQ message's retry count exactly). This **rules out** the two prime suspects raised before the DLQ evidence landed — no ARN mismatch (both ARNs correct), no auth/connection error (no `AccessDenied`/`401`/`403` anywhere in the message, consistent with the Stakeholder's clean IAM/connection findings) — and **confirms** the third: EventBridge API destinations enforce a **hard, non-configurable 5-second client execution timeout** on every invocation (AWS docs, `eb-api-destinations.html`: "EventBridge requests to an API destination endpoint must have a maximum client execution timeout of 5 seconds"), while the real poll job takes 30–70s under real Outscraper+Claude work. **Independently corroborated, not just taken from the DLQ text or the docs**: searched today's App Runner application logs (uvicorn's own access log) for every `/api/jobs/poll-customers` occurrence — of 5 scheduled ticks (08:00/10:00/12:00/14:00/16:00 CEST), only the 12:00 one shows any trace at all, and even that one landed at 12:03:02 CEST, 3 minutes after the tick's nominal fire time (a retry that happened to finish fast enough to respond inside its own 5s window) — the other 4 left *zero* log lines, meaning the connection never survived long enough for uvicorn's access-log middleware (which logs after a response is generated) to record anything, exactly the signature a ~5s client-side abort on a still-blocked 30-70s handler would leave. **Fix, exactly as instructed — refactored to the async-202 pattern, not a retry/timeout tuning workaround** (there is nothing to tune; the 5s ceiling is fixed and undocumented-as-raisable): `POST /api/jobs/poll-customers` now only validates `X-Job-Key`, schedules the real run via FastAPI `BackgroundTasks`, and returns `202 {"accepted": true, "run_id": "..."}` immediately — regardless of how long the underlying poll takes, closing the gap at its source rather than chasing it as job/customer count grows. The job logs its own completion summary (`poll-customers[<run_id>]: run complete — {...}`) since there's no longer an HTTP response body to carry the result back to EventBridge. **Idempotency/caps genuinely unchanged**: `run_poll_customers()` itself (window guard, records/Claude-call/daily-email caps, `ON CONFLICT DO NOTHING` idempotency-by-alerts) was not touched at all — the refactor is entirely in the router + one new wrapper. **New protection this refactor required, disclosed rather than assumed safe by default:** decoupling accept-the-trigger from do-the-work means two rapid triggers can now genuinely *overlap in time* (previously impossible — the synchronous design meant one in-flight request blocked EventBridge's own next attempt from being a concurrent execution). The existing idempotency-by-alerts (`alerts`' `(customer_id, review_id)` unique constraint + the up-front already-alerted pre-check) guarantees no duplicate alert row/email ever results from two runs that each *complete*, but does nothing to stop two truly concurrent runs from both passing the pre-check for the same review before either commits, each spending a real Claude call on it — same class of bug as ticket 5.1's own pre-spend idempotency fix, here for cross-run overlap instead of single-run re-entrancy. Closed with a new in-process, non-blocking run-lock (`run_poll_customers_locked` in `app/jobs/poll_customers.py`, plain `threading.Lock`, `acquire(blocking=False)`) — a second trigger while one run is in flight is coalesced into a documented no-op (`skipped_reason: "already_running"`) rather than started as a second concurrent run. An in-process lock is sufficient and deliberately not a distributed one: only one instance of this service runs today, and the ticket doesn't call for scaling out the poller. **Tests**: `tests/test_jobs_router.py` rewritten for the 202 contract (auth tests unchanged; new test asserts `202` + `{accepted, run_id}` shape, delegation to `run_poll_customers_locked`) plus a genuine **double-fire test at the HTTP layer** — two real Python threads firing `client.post()` concurrently against the actual endpoint (confirmed empirically that `TestClient` really does execute two threads' background tasks in true overlap, not just interleaved), with the mocked job sleeping 0.3s to force the overlap: both requests get `202`, but the underlying job function only actually runs once — proving the run-lock, not idempotency-by-alerts, is what's under test (stated explicitly in the test's own docstring per the ticket's own ask to say which). 15/15 router+poller tests green, full suite 333/334 (`test_health`'s pre-existing local-DB-connectivity issue, unrelated), `ruff check` clean. **First clean unattended tick, 18:00 CEST 2026-08-08 — the fix works at the layer that was broken:** `Invocations: 1`, `FailedInvocations: []` (zero — the metric has no datapoint at all for that window, vs. a `Sum: 1.0` on every failing tick before it), DLQ empty (purged of the already-decoded 16:00 message beforehand precisely so "empty" would be unambiguous rather than stale), and the App Runner access log shows the scheduled request landing at 16:00:13 UTC with **`"POST /api/jobs/poll-customers HTTP/1.1" 202 Accepted`** — 13 seconds after the nominal fire time, well inside the 5s-per-attempt ceiling that was killing every previous tick. <br><br>**Second bug found by that same tick, disclosed and fixed rather than glossed over as "the tick passed":** the run's own completion summary was **missing entirely** from the logs — the 202 access line was there (uvicorn's own logger), but not one line from the job. Root cause: uvicorn configures handlers only for its own loggers (`uvicorn`/`uvicorn.access`, both `propagate=False`) and leaves the **root logger bare**, so every `logging.getLogger(__name__)` record in this codebase below WARNING propagated to a handler-less root and fell through to `logging.lastResort`, which only emits WARNING and above — silently dropped. This was harmless for as long as nothing important logged at INFO, and stopped being harmless at the exact moment this ticket went async-202: the log line *is* the only record of what an unattended run did, since the HTTP response is now just a 202 receipt EventBridge never reads. Fixed with a `logging.basicConfig(level=INFO, ...)` in `app/main.py`; **verified empirically against a real uvicorn process, not reasoned about** — spun one up locally with a background task emitting the same logger call and confirmed both that the `[app.routers.jobs]` INFO line now appears *and* that uvicorn's access line still appears exactly once (no duplicate-logging side effect from adding a root handler, which `propagate=False` on uvicorn's own loggers is what prevents). <br><br>**Consequence for this tick's evidence, stated honestly:** the 18:00 tick proves the EventBridge→App Runner delivery path end-to-end (accepted, not timed out, not DLQ'd), which is exactly the failure this ticket was stuck on — but it does **not** prove what the job body did, because the counters it would have logged were being swallowed. No error/traceback appears in that window either (uvicorn's error logger IS configured, so an exception in the background task would have surfaced), so the run most likely completed cleanly with `new_alerts: 0` — the expected outcome, since the connected McDonald's reviews were all already alerted by ticket 5.1's day-one run and no genuinely new review is known to have landed since. That's inference, not proof, and is called out as such: the 20:00 CEST tick, with logging deployed, is what will actually show the counters. <br><br>**Closed out — three consecutive clean unattended ticks, with full counters, 2026-08-08 evening.** `FailedInvocations` has exactly one datapoint in the whole 14:00–02:00 range and it is the 16:00 CEST tick (the last one before the fix deployed at 16:11); 18:00, 20:00 and 22:00 all show `Invocations: 1` with **no `FailedInvocations` datapoint at all**, DLQ still empty, and zero ERROR/WARNING/traceback lines anywhere in the app log across all 16 non-healthcheck events since. **20:00 CEST run (first with logging live), verbatim counters:** `poll-customers[735a8ea8…]: Customer 13: fetched 5 newest, 0 inserted / 5 updated` → `Customer 14: fetched 5 newest, 0 inserted / 5 updated` → `No new reviews to draft across any polled customer` → `run complete — {'customers_considered': 2, 'customers_polled': 2, 'reviews_fetched': 10, 'new_alerts': 0, 'emails_sent': 0, 'daily_cap_skipped_customers': 0, 'aborted': False, 'abort_reason': None}` — i.e. the job really did poll both live customers, correctly found nothing new, and spent zero Claude calls doing it (the pre-spend idempotency check working exactly as designed on a real unattended run, not just in tests). **22:00 CEST run — the full pipeline fired end-to-end on a genuinely new review, unattended, with no human involved at any point:** `Customer 13: fetched 5 newest, 1 inserted / 4 updated` (a real new review landed on the McDonald's) → `httpx: POST https://api.anthropic.com/v1/messages "HTTP/1.1 200 OK"` (one Claude call, for the one new review) → `httpx: POST https://api.postmarkapp.com/email "HTTP/1.1 200 OK"` → `Review Ci9DQUlRQUNvZENodHljRjlvT2t0RFdsaG5UR05HY1ZodFQwTlNaM0p6V0dKblZFRRAB — alerted (urgent), email_sent=True` → `run complete — {'customers_considered': 2, 'customers_polled': 2, 'reviews_fetched': 10, 'new_alerts': 1, 'emails_sent': 1, 'daily_cap_skipped_customers': 0, 'aborted': False, 'abort_reason': None}`. Whole run: 14 seconds wall clock (20:00:14 → 20:00:29) — for reference, ~3x the 5s API-destination ceiling that was killing it, which is exactly why the async-202 refactor was the right fix rather than trying to make the job faster. The review was scored urgent (rating ≤3), so it went out with the PILNE styling from 5.4's template, to `pedram@reviewguide.eu` (5.3's interim `notification_email` fix). | ✅ **ACCEPTED (PM, 2026-08-09)** — *"DLQ-confirmed root cause (5s API-destination ceiling), async-202 + disclosed run-lock, logging gap found/fixed as second bug, three clean unattended ticks; monitor-mortality process notes valued."* Root cause found via DLQ ground truth (EventBridge API destinations' hard 5s timeout vs. our 30-70s synchronous job; not IAM/ARN/auth, which the Stakeholder's console findings independently cleared and the DLQ's own error text contradicts), async-202 refactor + run-lock deployed, second bug (swallowed INFO logs) found by the first clean tick and fixed. **Three consecutive unattended scheduled ticks now succeed with zero FailedInvocations and an empty DLQ, and the 22:00 CEST one drafted and sent a real alert email for a genuinely new urgent review end-to-end with no human involved** — the "SCHEDULED tick succeeds end-to-end" bar this ticket was held to. |
| 5.3 | Customer panel expansion | ✅ | Backend: app/routers/billing.py, tests/test_billing.py, docs/LOGIC.md, docs/sprints/SPRINT_05.md, app/routers/customer.py, tests/test_customer.py. App repo (reviewguide-app): middleware.ts, app/(customer)/app/page.tsx, app/(customer)/app/CustomerPanel.tsx (new), ConnectRestaurantFlow.tsx (new), AlertsList.tsx (new), SettingsForm.tsx (new), app/api/billing/checkout/route.ts, app/api/customer/{state,settings,search-place,preview-maps-url,connect-place,alerts}/route.ts (all new), lib/customerApi.ts (new), lib/format.ts, playwright.config.ts, e2e/customer-panel.spec.ts (new), e2e/fixtures/{stub-backend.mjs,session.ts} (new) | **Three findings from Stakeholder's live walkthrough of app.reviewguide.eu, 2026-08-08, fixed ahead of the panel-expansion work proper** (each small/isolated enough to land immediately rather than wait for the rest of 5.3): <br><br>**1. Root page placeholder.** `/` served the pre-customer-audience "internal dashboard, see /admin" placeholder from ticket 3.2. Fixed in `middleware.ts` (not `app/page.tsx` — the existing gate pattern for `/admin` and `/app` already lives there): `/` now redirects → `/app` on a valid session cookie, else → `/login`, reusing the same `verifySessionToken` the `/app` gate calls. `app/page.tsx`'s placeholder body is now unreachable in normal use (middleware intercepts `/` before the page renders) but left in place rather than deleted — harmless, and Next.js still wants a route file at `/`. <br><br>**2. Checkout double-subscription guard — real bug, confirmed then fixed.** `POST /api/billing/checkout` had no check against the customer's current `subscription_status` at all; `_get_or_create_stripe_customer` reuses the existing Stripe Customer but nothing stopped a second `stripe.checkout.Session.create(...)` call from creating a genuine second Stripe subscription for a customer already `trialing`/`active` (Stripe allows multiple subscriptions per customer by default). Fixed: new `_ALREADY_SUBSCRIBED_STATUSES = ("trialing", "active")` guard returns `409` with a PL message before any Stripe call; scoped to exactly those two statuses per the finding (past_due/unpaid/incomplete/canceled left as-is, disclosed as a judgment call for a future ticket, not silently folded in). **Root cause of how the button let this happen**: the `/app` button's own visibility check used `has_subscription_ever_started` (`stripe_customer_id is not None`) rather than `subscription_status` — that flag flips true the instant *any* checkout is ever started (even an abandoned one, since the Stripe Customer object is created up front), which isn't the same question as "is there a subscription right now." Fixed: button condition now keys off `subscription_status ∈ {trialing, active}` directly, matching the backend's own guard; a 409 from checkout now shows a distinct friendly "already subscribed, manage it below" message (amber, not red) instead of the generic billing-error banner. 11 new/parametrized backend tests (409 for trialing/active, 200 for the other 7 statuses); frontend typechecks clean. **Verified against real Stripe test-mode data**: queried `stripe.Subscription.list(customer=cus_V1onnk07RzXhpE, status="all")` for the STAKEHOLDER-TEST customer directly — exactly one subscription exists (`sub_1U1lAXIDs1qO8e1Tnp2yfvlA`, trialing). The walkthrough did not actually create a duplicate; nothing to cancel. <br><br>**3. Notification email interim fix.** The settings UI (where a customer edits `notification_email`) is part of the panel-expansion work still to come, but the milestone alert needs to land where the Stakeholder actually watches now. Via the bastion tunnel, ran `UPDATE customers SET notification_email = 'pedram@reviewguide.eu' WHERE customer_id = 13 AND email = 'pedram@defraged.com'` (shown before running, per the ask) — `rows updated: 1`, verified by re-selecting the row (`notification_email` now `pedram@reviewguide.eu`; login `email` unchanged). Both `app/jobs/day_one.py` and `app/jobs/poll_customers.py` already send to `customer.notification_email or customer.email`, so this took effect with no code change needed. <br><br>**Core panel-expansion scope, same day.** Backend (session-auth, `app/routers/customer.py`): `GET /api/customer/state` (place info-or-null + `tone_preference` + `notification_email`, so the frontend knows connect-flow-vs-home in one call); `PATCH /api/customer/settings` (validates `tone_preference` against a closed `("formal","friendly")` set, `notification_email` via pydantic `EmailStr`); `GET /api/customer/alerts` (joined with `reviews`, newest-first — `alert_id` added as an explicit ORDER BY tiebreaker after a same-`created_at` race surfaced as a flaky-looking test, a real determinism gap worth fixing at the query level, not just the test); new `POST /api/customer/preview-maps-url` (reuses `parse_maps_url()` with zero Outscraper spend — added because the ticket's "confirmation card before connect" requirement doesn't hold for the URL-paste path without *some* server-side parse, and duplicating that parse client-side would only ever be a guess pending the real connect call anyway). 27 new/updated backend tests, all green; `ruff` clean. <br><br>**Frontend (`reviewguide-app`, dark glass theme continuity, plain PL copy):** `/app` now server-fetches `getCustomerState()` alongside the existing billing status and renders a new `CustomerPanel` client component. **Connect-restaurant flow** (`ConnectRestaurantFlow.tsx`): debounced live search (400ms, min 2 chars) against `/api/customer/search-place`, a "Wklej link" tab that calls the new preview endpoint and shows a confirmation card (name only — address/rating aren't knowable before the real connect resolves the URL, disclosed in the card's own copy rather than faked), a shared confirmation card for the search path (name+address+rating from the already-fetched result, no second lookup) before the real `POST /api/customer/connect-place` fires. **Post-connect home** (`CustomerPanel.tsx`): connected restaurant card + last-checked time (pl-PL locale — new `formatDateTimePl` in `lib/format.ts`, since every other date helper there is en-GB for the admin-only UI), recent alerts (`AlertsList.tsx`: review + draft in a select-all copy block + Copy button with a 2s "Skopiowano ✓" confirmation + red "PILNE" badge on `is_urgent`), settings (`SettingsForm.tsx`: notification email defaulting to the login email, formal/friendly tone toggle, dirty-tracking Save), and a compact "Zarządzaj subskrypcją" link into the existing Stripe portal form when already subscribed. PL empty states: "Nie masz jeszcze połączonej restauracji" (not connected) and "Nie masz jeszcze żadnych alertów..." (connected, zero alerts yet). All backend calls proxied through new same-origin `app/api/customer/*` route handlers (`lib/customerApi.ts` is `server-only`, same pattern as `lib/billingApi.ts`) so the session JWT never reaches client JS. **Disclosed scope note on `tone_preference`:** it's now customer-editable and persisted, but `render_for_customer()` (LOGIC §8a) still only branches on rating, not tone — an actual "friendly" wording variant needs its own PM-reviewed prompt text, same process as the positive-prompt v1.3 round, not something to invent unilaterally inside a UI ticket; flagged here as a follow-up rather than silently left unimplemented. **Two ESLint `react-hooks/set-state-in-effect` catches, fixed not suppressed:** a synchronous `setState` inside a `useEffect` body (both the search-reset branch and the alerts-loading-start) — fixed via derived-at-render values for the former and an inner async function for the latter, not an eslint-disable. **Playwright** (`e2e/customer-panel.spec.ts`, local-only — `test.skip` when `E2E_BASE_URL` is set): connect flow with mocked search+connect+state+alerts, copy-to-clipboard, PILNE badge presence/absence. Needed a new local stub backend (`e2e/fixtures/stub-backend.mjs`, plain Node `http`, zero new deps) because `/app`'s SSR data fetch happens server-side inside the Next.js process — `page.route()` can't see that call at all, only the browser-side ones. Real bug caught building this: the 3 tests raced on one shared stub-backend fixture across the desktop-chromium/mobile-chromium projects running concurrently against the same long-lived process; fixed by keying fixtures off the session JWT's `sub` claim (parsed unverified — the stub holds no secrets) rather than serializing the whole suite. `tsc`/`eslint`/`next build` all clean; 12/12 local Playwright tests pass (2 live-only skipped). <br><br>**Live-verified against the real STAKEHOLDER-TEST customer (customer_id 13, McDonald's Świętokrzyska) post-deploy:** `GET /api/customer/state` → real connected place + `notification_email: pedram@reviewguide.eu` (the interim fix from above) + `tone_preference: formal`; `GET /api/customer/alerts` → the real 10 day-one drafts from 5.1 including the Ukrainian one, correct `is_urgent` split; `POST /api/customer/preview-maps-url` on a real Maps URL → correct `suggested_query`, zero cost; `POST /api/customer/connect-place` on the already-connected account → `409` as expected, no mutation. `curl https://app.reviewguide.eu/app` with a minted session cookie confirmed the actual deployed SSR HTML: connected card shows "Restauracja McDonald's" / address / ★4.2 / "Ostatnio sprawdzono: 7 sie 2026, 13:43" (pl-PL formatting correct), settings form prefilled with the real `notification_email` and "Formalny" selected, portal link present (subscription is trialing). Alerts list itself renders empty in the raw curl (expected — it's a client-side fetch after hydration, confirmed working via the Playwright suite instead, not re-tested with a real browser against prod to avoid an unnecessary live session round-trip). Deployed: backend App Runner (commit `4bb0ae6`, reached `RUNNING`) and `reviewguide-app` on Netlify (commit `594ad72`, deploy `6a767dac`, `ready`) — both auto-deployed from the `main` push, no manual trigger needed this time. | ✅ **ACCEPTED (PM, 2026-08-08)** — *"5.3 → ✅ pending a Stakeholder panel walkthrough."* Accepted on the built-and-verified evidence; the hands-on panel walkthrough remains outstanding as a confirmation step, not a blocker. Core panel-expansion scope built, tested (backend + local Playwright), deployed, and live-verified read-only against real STAKEHOLDER-TEST data. **Deploy note:** the frontend push (`7d35d64`) initially failed on Netlify — `error_message: "Skipped due to account credit usage exceeded"` (team was on the Free plan). Stakeholder upgraded to the Personal plan; re-triggered the build via `netlify api createSiteBuild` (no new commit needed) — deploy `6a7674fa` reached `state: ready`, `commit_ref` confirmed `7d35d64`, `context: production`. Live-verified: `curl https://app.reviewguide.eu/` now returns `307 → /login` instead of the old placeholder. Backend guard (App Runner) was already live throughout, unaffected by the Netlify billing issue. **Costs note (per PM instruction, next metrics update):** add a Netlify paid-plan (Personal) line to the running-costs note — was Free, now a recurring cost, not previously tracked. |
| 5.4 | Alert + digest emails (PL) + promise-wording fix | ✅ | Backend: app/templates.py, app/services/postmark_client.py, app/jobs/day_one.py, app/jobs/poll_customers.py, tests/test_templates.py, tests/test_day_one.py, tests/test_poll_customers.py. Marketing (reviewguide-marketing): components/ui/illuminated-hero.tsx, components/how-it-works.tsx, components/pricing-section.tsx, app/layout.tsx | **PM finding, 2026-08-08 — CHANGES REQUESTED, now fixed:** the "Otwórz panel ReviewGuide" CTA button in both live proof emails opened `localhost`, not `app.reviewguide.eu` — third bug in this codebase's APP_ORIGIN-config family (ticket 4.2's forwarded-host issue, ticket 4.5's stale-App-Runner-env-var issue). **Root-caused, not guessed:** `app/templates.py`'s app link (`_APP_LINK`) was a *module-level constant*, computed exactly once — whenever `app.templates` first got imported into a process — from whatever `settings.app_origin` happened to be at that moment. Harmless for the real App Runner server (imported once, well after `APP_ORIGIN=https://app.reviewguide.eu` is already set by its environment), but the one-off `/tmp/send_proofs.py` script that sent the two proofs imported `app.templates` under the local repo's `.env` (`APP_ORIGIN=http://localhost:3000`, the local-dev default) — so both proofs baked in localhost at import time and no later config change could have fixed them without re-importing the module. `app/routers/auth.py`'s magic-link URL was never affected — it's built fresh inside the request handler on every call, not cached, which is exactly why the family's other two members never froze this way. **Fix:** `_APP_LINK` → `_app_link()`, a function reading `settings.app_origin` live on every call, matching the magic-link pattern; the 3 call sites (`_html_app_link_button()`, both `Panel: {...}` text-body lines) updated accordingly. Kept reusing `settings.app_origin` rather than adding a separate `PANEL_URL` — it's already the same "public URL of the reviewguide-app deployment" concept CORS and the magic-link email both key off, and a second env var would only double the chance of the two drifting apart, not reduce it. **Regression coverage added, template-wide not single-URL, per the PM's own instruction:** `test_no_rendered_email_ever_leaks_a_dev_or_preview_host` renders the alert, digest, and magic-link templates with the real production `app_origin` patched in (not the ambient test-suite `.env` default, which is itself localhost — patching in the prod value is what makes the assertion meaningful) and asserts none of the 8 rendered subject/text/HTML parts ever contain `"localhost"` or a `".netlify.app"` host, plus a positive check that the prod URL genuinely shows up (so the sweep isn't vacuously true); `test_app_link_is_read_live_not_cached_at_import_time` proves the link changes mid-process when `app_origin` changes, which would have failed outright against the old frozen-constant code. 332/333 backend tests pass (`test_health`'s DB-connection failure is the same pre-existing local-environment issue noted in every prior row), `ruff` clean. **Both proof emails re-sent to pedram@reviewguide.eu with the fix live** (`[PROOF 5.4 v2 - fixed link]` subject prefix, same real McDonald's content as the original two proofs — alert message ID `d0459a15-…`, digest `d7e3441e-…`) — **verified via Postmark's own message-details API, not just re-reading the source that composed them**: both delivered messages' actual `HtmlBody` contain exactly one link, `https://app.reviewguide.eu/app`, and zero occurrences of `"localhost"` in either `HtmlBody` or `TextBody`. Deployed: backend App Runner (commit `e649983`, reached `RUNNING`, `/health` green). Temp scripts (`/tmp/send_proofs.py`, its replacement) deleted after use, same hygiene as every other one-off ops script in this project. <br><br>**Finalized both Postmark templates as code** (`app/templates.py`), replacing the `[SZKIC]`-marked draft copy from tickets 5.1/5.2 with the ticket's exact spec text: alert subject "Nowa opinia (X★) — gotowa odpowiedź" / urgent "PILNE: Nowa opinia 1★ — odpowiedz dziś"; welcome digest subject "Twoje odpowiedzi są gotowe". Both now render **HTML + plain-text parts** (`render_alert_email`/`render_welcome_digest` return `(subject, text_body, html_body)`, up from a 2-tuple) — no external images (inline styles only, nothing fetched from a URL), a visually distinct copy block per review/response pair (background + left border, generous padding — the "easy to select on mobile" affordance a plain HTML email can honestly offer, since email clients don't support scripted auto-select), a health-flag warning box when applicable, and a "Otwórz panel ReviewGuide" button linking to `{app_origin}/app`. **Real injection boundary closed, not just tidiness**: reviews are untrusted third-party text scraped from Google, so every review/response string is HTML-escaped (`html.escape`) before landing in the HTML part — 2 new tests assert a `<script>`/`<img onerror>` payload never survives unescaped. `app/services/postmark_client.py`'s `send_email()` gained an optional `html_body` param (additive — `HtmlBody` only added to the Postmark payload when passed; the magic-link email, which never passes it, is unaffected) so TextBody keeps going out either way per the ticket's "plain-text alternative parts included" instruction. Both approval gates (`ALERT_EMAIL_APPROVED_ON`, `WELCOME_DIGEST_APPROVED_ON`) **stay `None`** — not because the copy is a draft anymore (this IS the reviewed template), but because the ticket's own text ("PM reviews the rendered emails before 5.5") makes the gate about Stakeholder/PM sign-off on the live proof, not code-readiness; same posture as every other approval gate in this codebase. 14 new tests (subject text for both normal/urgent, HTML+text both contain the review/response/`/app` link, escaping, health-warning presence/absence, digest multi-item + PILNE badge, gate-unset guard) — full suite 314/315 green (`test_health`'s DB-connection failure is the same pre-existing local-environment issue noted in every prior row, not from this change), `ruff check` clean. <br><br>**Live proofs sent to pedram@reviewguide.eu** (per the ticket's own Cursor-prompt instruction), bypassing the gate for this one Stakeholder-review send only (not a customer send — pulled `POSTMARK_TOKEN` directly from Secrets Manager, no bastion needed since that's a public AWS API): one **alert** proof using the real McDonald's Świętokrzyska 1★ "mięso rozmoczone" review + its real Claude-generated response (urgent styling), and one **welcome digest** proof bundling 3 real drafts (5★ thank-you, the same 1★ negative, and the real Ukrainian 1★ review+response from ticket 5.2's investigation — deliberately included to show the digest layout holds up with non-Latin script). Message IDs: `72ad5bcf-…` (alert) and `1274adba-…` (digest). <br><br>**Landing "hour promise" fix, real deviation from the ticket's literal wording, disclosed:** searched `reviewguide-marketing` for the literal string `"w ciągu godziny"` the ticket names — it does not exist verbatim anywhere in the repo (checked full git history too, `git log -S"godzin"`, only match is unrelated: "w godzinach wzmożonego ruchu" in a demo review quote). The actual analogous claim — the thing a Stakeholder skimming the site would reasonably summarize as an hour promise — is the hero subtext + `<meta description>`: **"ReviewGuide codziennie sprawdza... w kilka sekund"** ("checks *daily*... ready response in *a few seconds*"), which both understated the real check frequency (daily vs. the actual every-2h) and, combined, implied a near-instant response the real pipeline can't promise. Treated this as the intended fix rather than leaving it as a doc/reality mismatch unaddressed: hero subtext, `<meta>` description (+ OG/Twitter, which reuse the same constant), and the "How it works" step 1 card all now say **"co 2 godziny"** (check frequency) **/ "w ciągu maksymalnie 2 godzin"** (response promise) — LOGIC §8a's exact wording. Also found and fixed the same stale "Codzienne" (daily) claim in the pricing feature-list bullet while verifying the fix live — same class of mismatch, not the ticket's named target but directly adjacent. `next build` + `tsc` + `eslint` clean on every touched file (repo-wide lint warnings are all pre-existing, confined to the generated `.netlify/` build-artifact folder, not source). Deployed: Netlify auto-deploy on push (two commits, `5f20ca8` then `f10a076`), both reached `state: ready`; live-verified via `curl https://reviewguide.eu/` that the hero, meta tags, "how it works," and pricing copy all match post-deploy. <br><br>**PM approval + gates flipped for real, 2026-08-08:** both `ALERT_EMAIL_APPROVED_ON` and `WELCOME_DIGEST_APPROVED_ON` set to `"2026-08-08"` (were `None`) — real sends now reach exactly one inbox (STAKEHOLDER-TEST → `pedram@reviewguide.eu`), which the 5.5 milestone requires. **Real test-isolation bug surfaced by the flip, fixed same day, disclosed rather than just re-gated:** `tests/test_day_one.py` and `tests/test_poll_customers.py` never mocked Postmark's `send_email()` in most of their tests — they got away with it only because the gate being `None` meant the job's code never reached that call. The instant the gate became a real date, every test with a qualifying review made a genuine, unmocked HTTPS call to `api.postmarkapp.com` using the real local `.env` `POSTMARK_TOKEN`, which Postmark correctly rejected (`422`) since the fixture data isn't a real send-able payload — 16 tests failed. Fixed by adding an autouse `_mock_send_email` fixture to both files (mocks Postmark for every test regardless of the gate's real value — matches each file's own pre-existing docstring promise that "every external service ... is still mocked, no real network call, ever," which had quietly stopped being true); the handful of tests whose assertions assumed "digest/alert never sent by default" were updated to reflect the new approved-by-default reality (one, `test_digest_is_not_sent_while_approval_is_unset`, was kept but now explicitly re-patches the gate back to `None` to prove that code path still works if the gate is ever unset again). Full suite 332/333 green after the fix (`test_health`'s DB-connection failure is the same pre-existing local-environment issue noted in every prior row), `ruff check` clean. | ✅ ACCEPTED (PM, 2026-08-08) — localhost root cause + live-read fix + template-wide regression sweep + delivered-HTML verification. Tone-lean logged for reviewer round. |
| 5.5 | Milestone: scheduled alert lands on Stakeholder's phone | ✅ | (no code — milestone evidence from tickets 5.1–5.4's shipped pipeline) | **The milestone event occurred unattended at 22:00 CEST on 2026-08-08**, with no human triggering, approving, or touching any part of it. Full chain, all from the App Runner application log (see ticket 5.2's row for the verbatim lines): the EventBridge rule fired on its own UTC cron → the API destination hit `POST /api/jobs/poll-customers` with its `X-Job-Key` → the endpoint returned `202 Accepted` in ~1s and ran the job in the background → the poller fetched the 5 newest reviews for the connected McDonald's and found **one genuinely new review** (`1 inserted / 4 updated`) → the up-front idempotency check correctly limited spend to that single review (exactly one Claude call: `POST api.anthropic.com/v1/messages 200 OK`) → the review scored urgent (rating ≤3) → 5.4's approved alert template rendered with PILNE styling → `POST api.postmarkapp.com/email 200 OK` → `email_sent=True` to `pedram@reviewguide.eu`. Run summary: `{'customers_considered': 2, 'customers_polled': 2, 'reviews_fetched': 10, 'new_alerts': 1, 'emails_sent': 1, 'daily_cap_skipped_customers': 0, 'aborted': False, 'abort_reason': None}`. End-to-end in 14 seconds. **Every gate this milestone depended on was already independently closed first**, which is why it fired cleanly the first time a real new review appeared: 5.1's connect flow (the McDonald's was connected and its history already alerted, so this review was unambiguously new rather than backfill), 5.2's poller + the same-day scheduler fix, 5.3's `notification_email` pointing at a real inbox, and 5.4's PM-approved template gate flipped earlier that afternoon. **Stakeholder confirmation received 2026-08-09**, closing the one thing Postmark's `200 OK` alone couldn't prove: the PILNE alert plus the two backfilled digests (5.7's sweep, 10 drafts each for customers 13/14) all delivered and were readable in the actual inbox — not just accepted by Postmark. | ✅ **ACCEPTED (PM+Stakeholder, 2026-08-09)** — "milestone demonstrated: organic new review caught by an unattended scheduled tick, urgent-flagged, alerted end-to-end; digests delivered via 5.7's sweep." |
| 5.6 | Admin: customers view (pulled from BACKLOG) | ✅ | Backend: app/routers/admin_customers.py (new), app/services/postmark_client.py, app/main.py, tests/test_admin_customers.py (new), tests/test_postmark_client.py (new). App repo (reviewguide-app): lib/api.ts, app/admin/layout.tsx, app/admin/customers/page.tsx (new), app/admin/customers/[id]/page.tsx (new) | Read-only v1, same `X-Admin-Key` auth as the existing leads/stats admin API (`app/routers/admin.py`'s `require_admin_key`) rather than a second boundary — this is internal ops on System B, not customer session auth. **`GET /api/admin/customers`**: one row per customer (email, connected restaurant name-or-null via `outerjoin(Place)`, `subscription_status`, `connected_at`, `last_alert_at`) — `last_alert_at` comes from a `MAX(Alert.created_at) GROUP BY customer_id` subquery joined in, not a per-row lookup, same "one query not N+1" habit as every other list endpoint in this codebase. **`GET /api/admin/customers/{id}`**: full detail — customer + place info, complete alert history joined with `reviews` (review text/rating/date, draft, `is_urgent`, `kind`, `sent_at`, `postmark_message_id`, `generation_stop_reason`, newest-first), plus Postmark delivery status for up to the 5 most recent alerts that actually have a `postmark_message_id` (none do yet — both send gates from 5.4 are still `None`, so this path is built and tested but has nothing live to show until those flip). **Disclosed scope note on "last poll outcome":** the ticket's spec text asks for it as a health signal, but no schema anywhere in this codebase persists a poll success/failure status — `places.last_polled_at` (already surfaced on the detail page) is the only signal that exists, and a failed Outscraper call today just leaves that timestamp stale rather than recording an error. Treated `last_polled_at`'s presence/recency as that signal rather than adding a new column/migration for it, since the ticket didn't ask for a schema change and "no editing, v1 read-only, lean" was the stated intent; a real "last poll error" field is a `poll_customers.py` follow-up, not invented here. New `get_message_delivery_status()` in `postmark_client.py`: hits Postmark's message-details API, returns `None` on *any* failure (no token, 404, network error) rather than raising — one flaky Postmark lookup degrades this one admin signal instead of 500ing the whole detail page, per the ticket's own "degrade gracefully" instruction. Message IDs deduplicated via `dict.fromkeys` before lookup so a request never queries Postmark twice for the same ID. 16 new tests (auth on both endpoints, unconnected-customer row, `last_alert_at` correctness, 404 on unknown id, place+alert-history inclusion, alert ordering, Postmark success/error/no-token degradation) — full suite green, `ruff` clean. <br><br>**Frontend** (`reviewguide-app`, same light-glass admin theme as `/admin/leads`/`/admin/replies`, new "Customers" nav link next to them): `/admin/customers` lists email, restaurant-or-"—", a status badge, connected date, last-alert date. `/admin/customers/[id]` shows customer info, connected-place card, a "Postmark delivery — last N sent alerts" section (only rendered when non-empty), and the full alert history with the PILNE badge and stop-reason. `tsc`/`eslint`/`next build` all clean. No Playwright coverage added for this ticket — consistent with the existing precedent that `/admin/leads` and `/admin/replies` have none either; the ticket's own test list was backend-only (auth, joins, empty states), which is fully covered above. <br><br>**Live-verified against the real STAKEHOLDER-TEST customer (customer_id 13) post-deploy, both backend and frontend**: `GET /api/admin/customers` on the App Runner URL directly returned the one real row (McDonald's, trialing, real `connected_at`/`last_alert_at` timestamps); `GET /api/admin/customers/13` returned the full real alert history — all 10 day-one drafts including the Ukrainian one, `postmark_message_id: null` on all of them (expected — day-one sends one digest email, not one email per draft row, and the digest gate is still off) so `recent_delivery_statuses` is correctly empty. `curl -u <ADMIN_USER>:<ADMIN_PASS> https://app.reviewguide.eu/admin/customers` → 200, real row rendered with the "trialing" badge and the new "Customers" nav link present; `/admin/customers/13` → 200, 8 PILNE badge occurrences (matches the 4 `is_urgent=true` alerts), `notification_email` correctly showing the 5.3 interim fix value (`pedram@reviewguide.eu`). Deployed: backend App Runner (commit `b939315`, reached `RUNNING`) and `reviewguide-app` on Netlify (commit `b400e9c`, auto-deployed, `ready`). | ✅ **ACCEPTED (PM, 2026-08-08)** — *"poll-outcome gap disclosed honestly (timestamp as signal), Postmark degradation graceful, live-verified."* Read-only admin customers view built, tested, deployed, and live-verified against real STAKEHOLDER-TEST data. Nothing here can mutate customer state (no PATCH/DELETE in v1), so no rollback risk if left unreviewed. |

| 5.7 | Alert retry/backfill sweep | ✅ **ACCEPTED (PM, 2026-08-09)** — "backfilled:20 via unattended tick, zero Claude calls, sweep's first run = its own live proof." | app/jobs/poll_customers.py, tests/test_poll_customers.py, docs/sprints/SPRINT_05.md | **Stakeholder finding, 4am 2026-08-09, change of plan from the earlier E-answer**: rather than one-off manually sending the 2 stuck day-one digests, build the fix that makes the whole class of problem self-heal — every `run_poll_customers()` call now sweeps `alerts` rows with `sent_at IS NULL` created within the last **7 days** (`SWEEP_LOOKBACK_DAYS`) BEFORE touching Outscraper/Claude for new reviews. Two shapes, matching how each kind was originally going to be sent: `kind='alert'` rows are retried one email each (`render_alert_email`, re-deriving `health_flagged` from the stored review text — free, deterministic, no re-spend); `kind='digest'` rows are grouped per customer and retried as **one** email covering all of that customer's still-unsent drafts, exactly the shape ticket 5.1's original send used. **Zero re-spend, by construction**: every swept row already has its `response_text` from the Claude call that created it — the sweep only ever calls Postmark, never Outscraper or Claude — so it is placed *before* the records-total/Claude-call caps' abort checks (those caps protect spend the sweep doesn't cause) but *does* share the same per-customer daily ALERT-email cap and the same `ALERT_EMAIL_APPROVED_ON`/`WELCOME_DIGEST_APPROVED_ON` template gates a fresh send would. **One deliberate asymmetry, disclosed and locked in by a dedicated test** (`test_digest_backfill_does_not_consume_the_alert_daily_cap`): a retried digest (however many drafts it bundles) does NOT consume the 10/day alert cap — that cap exists to stop an ongoing *new-review* flood, a concern ticket 5.1's original one-time digest send was never subject to either. Idempotent by construction: the retry-send's own `UPDATE ... WHERE sent_at IS NULL` (single row for alerts, `review_id IN (...)` for a digest batch) only stamps rows it can prove were still unsent at that instant, mirroring the `ON CONFLICT DO NOTHING` pattern this codebase already uses for the insert side. Added `result["backfilled"]` to the run summary (0 when nothing was stuck). Refactored the pre-existing per-customer daily-cap lazy-init (previously local to Phase 3) into `_get_or_init_daily_count()`, shared by the sweep and Phase 3 so a customer whose backlog just consumed 3 of today's 10 slots genuinely has 7 left for new reviews, not a fresh 10 — and moved `daily_cap_skipped_customers` into the result immediately after the sweep (not only at the function's final line) so the early-return paths (records-cap abort, "no new reviews") still report sweep-caused skips correctly. **9 new tests**: retry-delivers a stuck alert (zero Claude calls — no re-spend); an already-sent row is never re-touched; an 8-day-old row is outside the 7-day cutoff and stays unsent; gate-closed skips cleanly and the identical row is delivered once the gate reopens (same test, both halves); a genuine Postmark failure leaves the row retryable and a later healthy run delivers it; the daily cap is enforced row-by-row mid-sweep (2 of 3 stuck alerts delivered, 1 stays for tomorrow); 3 stuck digest drafts land in exactly one email with one shared `postmark_message_id`; the digest gate-closed path skips cleanly; and the cap-exemption test above. Full suite 346/347 (the one failure is `test_health`'s pre-existing local-DB-connectivity issue, unrelated), `ruff check` clean. **Doc note**: pulled `docs/sprints/SPRINT_05.md` from a stale working copy that had ticket 5.7 pasted onto an older snapshot missing the 5.3 root-redirect amendment and the entire `## Prompt v1.3` block (the same accidental-deletion class this file already hit once this week) — merged 5.7 onto the last-committed version instead of committing the stale copy verbatim, verified by diff before committing. **Deployed; LIVE PROOF OBSERVED at the 08:00 CEST tick on 2026-08-09**, unattended, exactly as predicted. From the App Runner application log (run id `8198c8a5653e4dc4a8119c68a8774dc6`): `POST /api/jobs/poll-customers → 202 Accepted` → `06:00:13 POST api.postmarkapp.com/email 200 OK` → `Backfill: customer 13 — delivered 10 stuck digest draft(s) in one email.` → `06:00:14 POST api.postmarkapp.com/email 200 OK` → `Backfill: customer 14 — delivered 10 stuck digest draft(s) in one email.` → `Backfill sweep: 20 previously-stuck email(s) delivered.` → then the normal phases (`Customer 13: fetched 5 newest, 0 inserted / 5 updated`, same for 14, `No new reviews to draft`) → `run complete — {'customers_considered': 2, 'customers_polled': 2, 'reviews_fetched': 10, 'new_alerts': 0, 'emails_sent': 0, 'backfilled': 20, 'daily_cap_skipped_customers': 0, 'aborted': False, 'abort_reason': None}`. **`backfilled: 20` as specified**, 20 drafts in exactly 2 emails (one per customer), and **zero Claude calls** — `new_alerts: 0` with no `api.anthropic.com` line anywhere in the run, confirming the no-re-spend guarantee held in production, not just in tests. DB reconciles: all 21 alert rows now carry a `sent_at`, the 20 digests all stamped `2026-08-09 06:00:13Z`; **unsent count is 0 for both customers**. | Live proof in hand — the only thing left for 5.5/sprint close is the Stakeholder confirming the two digest emails landed in his inbox (alert + both digests). |

| 5.8 | Prompt v1.4/v1.4.1 — star-only branch, thinking-token fix, city-derived persona | ✅ **ACCEPTED (PM, 2026-08-09)** — "star-only branch + thinking-token root cause + MAX_TOKENS 1200 + PL-default; migration 008 approved." | app/prompts.py, app/services/claude_client.py, docs/sprints/SPRINT_02.md, docs/sprints/SPRINT_05.md, tests/test_prompts.py, tests/test_prompts_positive.py | **Stakeholder finding, 2026-08-09**: System B drafts full-length responses for star-only reviews. The lead engine's Q4 (`_passes_q4_length`, >=80 chars) lives in `app/jobs/qualify.py` and only ever gated System A — the customer path drafts for *every* review by design (LOGIC §8a), and neither prompt variant had a branch for "no material". **Audit of all 21 alert rows ever produced: 9 (43%) were star-only** — zero-length review text, all `kind='digest'`, split 4 (customer 13) / 5 (customer 14); a further 5 were 20–79 chars, so **14 of 21 (67%) would have failed Q4** had it applied. No row fell in the 1–19 char band, so "star-only" and "<20 chars" are the same 9 rows here. **Did it invent specifics to satisfy rule 3? Yes, on the positive side.** Three 5★ star-only drafts asserted things no review said: alert 9 "mogli Państwo miło spędzić czas przy naszym stoliku" (a McDonald's review that may well have been drive-thru), alert 28 "chętnie powitamy Państwa przy stole", alert 30 "częstować kolejnymi smakami z naszej kuchni" — all assert the guest sat and ate. Alerts 7/9/10 additionally place the visit "na Świętokrzyskiej", a specific lifted from the *place address* field, not from the reviewer. The negative side behaved better: alerts 8, 21 and 22 explicitly said no details were given ("Choć w recenzji nie podano szczegółów", "Bez szczegółów trudno nam ocenić") — honest, but padded to 86–89 words of nothing to fill the 60–120 budget. **Second, undisclosed-until-now defect found during the same audit — 4 of 21 drafts were truncated mid-word and all 4 were emailed** (alerts 4, 21, 22, 25; test customers 13/14 only, since real customers = 0, so no real-world blast radius). Three carry `stop_reason='max_tokens'` and **all three are star-only**; the fourth (alert 4, a 35-char review) predates the `generation_stop_reason` column and was caught by the punctuation heuristic. **Root-caused, not guessed:** a live replay of alert 25's exact input reported `output_tokens=485, thinking_tokens=299` — this model emits `thinking` blocks that are billed and counted against `max_tokens`, but `ClaudeClient._call` keeps only `type == "text"` blocks, so ~60% of the 500-token ceiling was spent before the answer began. Reviews with no text are the worst case because the model deliberates longest when there is nothing to work with, which is exactly where all three max_tokens truncations landed. This is the true cause of the v1.1 "truncated mid-word" family that the 350 → 500 bump in v1.2 only made rarer. **Fix, two parts.** (1) **Prompt v1.4, both variants** (PM instruction — the two lineages converge here from 1.2.1/negative and 1.3/positive): new `KROK 0a` immediately after the language step — empty or <20-char review → 25–50 words, warm and generic, thanks → brief regret only when the rating is ≤3 → offline invitation, and an explicit ban on guessing the dish, service, wait, price, reason for the rating or course of the visit, on referring to details the review doesn't contain, and on naming the address or location. The positive variant's ban names the exact observed failures (don't write that the guest ate, sat at a table, or spent time here). KROK 0a replaces rules 3–4 only; language, no-signature, and the never-rules still apply. Doc-parity synced in the same commit (SPRINT_02.md for the negative, SPRINT_05.md for the positive), both `## Prompt v1.4` headings added above the retained version history. (2) **`MAX_TOKENS` 500 → 1200** — headroom, not a length change: the prompts' own word limits bound the response and generation stops at `end_turn`, so a higher ceiling costs nothing on a normal call; it only stops thinking tokens from eating the visible answer. Note this also fixes System A, which shares the client. **Live-verified on the three worst inputs, replayed under v1.4** (3 real Claude calls, ~$0.02): alert 25 (5★) → 41 words, alert 22 (3★) → 33 words, alert 9 (5★) → 25 words; all three inside the 25–50 band, `stop_reason=end_turn`, zero truncation, no signature, no denial wording, no street name, no invented dining. The 3★ correctly carried regret + a request for direct contact; the 5★s thanked and invited a return without inventing a reason. Telling detail: `thinking_tokens` dropped to **0** on all three — the model had been deliberating precisely because rule 3 demanded specifics it had no material for. Two new tests pin `KROK 0a` into each variant (doc-parity alone would pass if the branch were deleted from code and doc together) and one asserts the branch reaches both templates through `render_for_customer()` at ratings 1/3/4/5/None. Full suite **350/350 green**. **NOT deployed** — per the PM's "PM reviews the branch text before it ships", App Runner still runs v1.2.1/v1.3; deploy on the PM's word. **PM verdict 2026-08-09: v1.4 branch text APPROVED verbatim, `MAX_TOKENS=1200` APPROVED** (root cause of the entire truncation family; thinking-token discovery commended). **Deployed to App Runner 08:39:34 CEST** — auto-deploy on push to main per the ROADMAP decisions-log deploy method, `START_DEPLOYMENT` triggered 20s after the push of `b67a9a7` and reached `SUCCEEDED` in 4m20s, `/health` 200 throughout. (Honest limit on that evidence: the service exposes no version/commit endpoint, so "the running container is b67a9a7" rests on `git ls-remote origin main` = `b67a9a7` plus the deploy's timing and success, not on a direct read from the running process.) **Post-deploy cleanup, PM-ordered:** all 9 star-only drafts regenerated in place under v1.4 — `response_text`, `generation_stop_reason` and the new `notes = "regenerated v1.4"` updated, **`sent_at` deliberately untouched**, so nothing was re-emailed and 5.7's sweep (which only ever selects `sent_at IS NULL`) cannot pick them up either. 9 Claude calls, 1035 output tokens, ~$0.05. All three previously-truncated rows came back clean and every row landed shorter and specifics-free. **Migration 008** (`alerts.notes`, nullable, applied live to prod RDS and verified against `information_schema.columns`) — a **disclosed addition**: `alerts` had no notes column to mark the rows with, unlike `leads`, so the PM's "notes-marked" instruction could not be carried out without one. **The cleanup then found a real gap in the approved v1.4, so this ticket stays 🧪:** KROK 0 sets the response language by detecting *the review's* language, and a star-only review gives it nothing to detect — so the output language is effectively a coin flip. Two of the nine (alerts 21 and 22, Istanbul Kitchen, both 3★) came back in **English** for a Warsaw restaurant, one of them at 74 words, outside the 25–50 band; the same input had produced Polish during the earlier verification run, which is what makes it a coin flip rather than a consistent bug. **Proposed v1.4.1** (both variants, one sentence, PENDING PM APPROVAL): "Pusta recenzja nie daje ŻADNEJ wskazówki co do języka — w takim wypadku KROK 0 nie ma czego wykrywać i odpowiedź jest po polsku (język lokalu)." Dry-run verified on the two English rows plus one 5★ control, generation only with no DB writes: 40 / 34 / 30 words, all Polish, none truncated. Suite still 350/350, `ruff` clean, doc-parity synced in both sprint docs. **v1.4.1 shipped 2026-08-09 (PM approved both changes, deployed 09:08 CEST, commit `9cf57ce`)** with two changes. (a) **PL default for star-only reviews** — the language fallback described above. (b) **Customer-path persona fix — the PM's third ask, and the honest answer is that it did NOT ship in v1.4: nothing in `b67a9a7` touched that line, so there was no diff to show.** Both prompts opened with "Jesteś doświadczonym właścicielem restauracji **w Warszawie**", true of every System A lead by construction (`app/jobs/discover.py` only ever searches `CITY = "Warszawa"`) but false for System B, where a customer can connect a restaurant in any city. Now city-derived from `places.city` and carried as an attribute on the existing structured tag — `<restauracja miasto="{city}">` — with the persona itself neutral and `UNKNOWN_CITY = "nieznane"` when the column is empty, matching the existing `UNKNOWN_DATE`/`UNKNOWN_RATING` pattern. **Deliberately an attribute rather than inline prose:** Polish takes the locative after "w" ("w Krakowie", "w Gdańsku", "w Łodzi"), which cannot be derived from an arbitrary nominative city name without a declension table, so interpolating `w {city}` would have emitted "w Kraków"; attribute values are naturally nominative, so this sidesteps declension while still giving the model the real city. `city` is the last `LeadContext` field with a default so `generate.py`'s positional `LeadContext(*row[1:])` unpacking stays valid; `load_generated_batch`'s index-based unpacking was shifted to match, and `day_one.py`/`poll_customers.py` now pass `place.city`. Live-verified on a synthetic Kraków restaurant: 75 words, correct Polish, zero Warsaw leakage. **Follow-up regenerations under v1.4.1** (3 calls, ~$0.02, `notes = "regenerated v1.4.1"`, `sent_at` untouched as before): alerts 21 and 22 rewritten from English into Polish at 49 and 31 words; **alert 4** regenerated too per the PM's go-ahead — it is not star-only (35 chars of real text, so KROK 0a correctly did not fire) and came back at 65 words, untruncated, **in Ukrainian**, which is right: this is the organic Ukrainian-language review that migration 006's docstring was written about, and KROK 0's third-language rule handled it. **Final DB state, verified: all 21 alert rows, zero truncated, zero unsent** — 7 marked `regenerated v1.4`, 3 marked `regenerated v1.4.1`, 11 untouched originals. Suite 353/353, `ruff` clean. | Closed. LOGIC.md §8a star-only bullet remains the PM's, next doc sync. Backlog gained "`/health` returns the git commit sha" out of this ticket's deploy verification — today's evidence is the push/deploy timestamps lining up, not a direct read from the running process. |

> Sprint closed — summary row in **Closed sprints** below. The per-ticket detail above is kept in
> place rather than condensed away (same precedent as Sprint 4): it is the only written record of
> the EventBridge Scheduler-vs-classic-Rules discovery, the DLQ-confirmed 5s API-destination
> timeout root cause behind the async-202 refactor, and the thinking-token truncation root cause
> behind prompt v1.4/v1.4.1 — all the kind of thing an audit or a post-incident review asks for
> months later.

### Sprint 5 blockers
_(none at open)_

### Sprint 5 — pre-close cleanup: who is Customer 14? (Stakeholder/PM, 2026-08-09)

PM asked, before sprint close, to identify the second customer appearing in the poller's logs and
confirm it wasn't an unexpected real signup. **It is the Stakeholder's own walkthrough account, and
it is not a real signup:** `customer_id 14`, login `pedram@reviewguide.eu` (company domain), signed
up 2026-08-08 03:56 CEST and connected a restaurant 2 minutes later — a manual test pattern, not
organic use. The connected place is **Istanbul Kitchen, San Leandro, California** (`ChIJ-wSQPwCRj4ARwlZdXQpfieE`),
i.e. a search-and-pick target for exercising ticket 5.3's connect flow, with no plausible customer
relationship. Its 10 alerts are all day-one digests from 01:58 UTC, all unsent (composed before
5.4's send gates were flipped that afternoon). **Zero billing exposure:** the Stripe key in prod is
`sk_test_`, and both live subscriptions come back `livemode: false` (`sub_1U1zVRIDs1qO8e1TZH92i0UF`
trialing to Aug 22, `sub_1U1lAXIDs1qO8e1Tnp2yfvlA` to Aug 21) — test-mode rows that can never charge.
It was flagged to the Stakeholder when it first appeared (2026-08-08) and not disputed.

**The honest finding underneath the question, and what was done about it:** the row was *not*
"known/flagged" in any machine-readable sense, and neither was customer 13. `customers` had no test
flag and no naming convention — "STAKEHOLDER-TEST" only ever existed as a label in this file. Nothing
in production could tell traction from testing. Harmless while every row is a test row; wrong the
moment the first real signup lands. Fixed with **migration 007** (`customers.is_test`, default false
so a real signup is never mis-flagged by omission), surfaced in both admin customer views as a badge
plus a "N real · M test" count, and applied live: rows 13 and 14 flagged, prod now at revision `007`.
**Deliberately marks rather than filters** — test accounts keep being polled and keep receiving
alerts, since they are the only live proof the unattended poller works end-to-end (they are what
produced ticket 5.2's two-customer evidence); a test in `tests/test_poll_customers.py` locks that in,
because "flag exists, therefore filter on it" is the obvious wrong next edit. Backfill was done as a
one-off ops UPDATE rather than inside the migration, since customer_ids are environment-specific.

**Also reverted (Stakeholder decision, same session):** customer 13's `notification_email`, which
5.3's interim fix had pointed at `pedram@reviewguide.eu`, is back to `pedram@defraged.com`. Both
accounts had been routing alerts to the same inbox, which is the real reason only one address was
receiving mail — the two accounts now test two different inboxes, as intended.

**Known gap, logged not fixed:** all 20 day-one digest drafts (10 per customer) were composed while
the send gates were closed and can never be delivered — the day-one job runs once at connect time,
and its up-front already-alerted check would skip every one of those reviews on any re-run. Harmless
here (both are test accounts, and the drafts are still visible in the panel), but the underlying
class matters: **a customer who connects during a gate-closed window silently misses the day-one
email**, and there is no retry or backfill path for an alert stuck unsent. Left as a product decision
for the PM rather than fixed unprompted.

### Sprint 5 open questions for Stakeholder
- ~~Which real restaurant to connect for the milestone test (high review volume recommended)~~
  **Resolved 2026-08-07** — Restauracja McDonald's, Świętokrzyska 35 (real Outscraper search
  result, chosen for high review volume; see ticket 5.1's live verification). This is the same
  connected place whose organic new review became the 5.5 milestone event.

---

## Current sprint: SPRINT 6 — Video + Polish + Iterate (opened by CR-1)

> Full PM+Stakeholder scoping session for Sprint 6 is still pending (see ROADMAP.md §3). CR-1
> below opened this section ahead of that session because it's a self-contained, Stakeholder-
> decided change with its own compliance deadline, not scope that needed sprint planning to shape.

| # | Ticket | Status | Files touched | Cursor notes | PM verdict |
|---|---|---|---|---|---|
| CR-1 | Trial payment model: cardless → card-upfront | 🧪 | Backend: app/routers/billing.py, tests/test_billing.py. Marketing (reviewguide-marketing): app/layout.tsx, components/pricing-section.tsx, components/faq-section.tsx, README.md. App repo (reviewguide-app): audited, no changes needed. | **Stakeholder decision, 2026-08-09** (`docs/ROADMAP.md` decisions log): trial switches from cardless (`payment_method_collection: "if_required"`, ticket 4.3) to card-upfront (`"always"`) — overriding the PM's original cardless-launch recommendation and the day-11-nudge middle path, in favor of UX continuity (subscription auto-converts to paid at day 14 with zero second action from the customer) over top-of-funnel volume. Landed as CR-1 rather than a numbered Sprint 5 ticket because Sprint 5 had already closed (5.5's Stakeholder confirmation, same session) by the time this arrived — opens Sprint 6 per the Stakeholder's own instruction. **1. Backend**: `payment_method_collection` `"if_required"` → `"always"` on `POST /api/billing/checkout`; `trial_period_days=14` unchanged. `subscription_data.trial_settings.end_behavior.missing_payment_method` was checked against the live codebase (`grep -r trial_settings`) and **was never configured here** — the instruction to "remove/adjust if configured" had nothing to act on; disclosed rather than silently assumed. `test_checkout_creates_stripe_customer_and_session` updated to assert `"always"`. **2. Card-network compliance research**: Stripe's built-in trial-ending reminder email is **Dashboard-only, not API-configurable** — confirmed against current Stripe docs (`billing/revenue-recovery/customer-emails`, `billing/subscriptions/trials/manage-trial-compliance`), no `trial_settings`/business-profile/webhook-registration API field toggles it. Exact toggle path for the Stakeholder: **Settings → Billing → Subscriptions and emails → "Send a reminder email 7 days before a free trial ends"** (dashboard URL: `/settings/billing/automatic`; also enable "Link to a Stripe-hosted page" alongside it for the compliance-required payment-update link). Separately confirmed: `customer.subscription.trial_will_end` fires as its own webhook ~3 days before trial end regardless of that email setting — available if a second, self-hosted reminder is ever wanted, not wired up here since the ticket only asked to identify/enable Stripe's own mechanism. **Not enabled by Cursor** — this is a Dashboard-only toggle with no API path, so it's a Stakeholder action item, listed below. **3. Marketing**: `app/layout.tsx`'s shared meta/OG/Twitter `DESCRIPTION` constant, `pricing-section.tsx`'s pricing-card subtext, and `faq-section.tsx`'s card-question answer all had their "bez karty" (or equivalent "nie wymaga karty") language removed. New copy direction landed verbatim: **"14 dni za darmo · anuluj w każdej chwili · pierwsza płatność dopiero po okresie próbnym."** FAQ answer rewritten honestly rather than just having the false claim deleted: "Tak — kartę podajesz przy rejestracji, ale nic nie płacisz przez 14 dni. Pierwsza płatność (129 zł) pojawia się dopiero po zakończeniu okresu próbnego. Możesz anulować w dowolnym momencie jednym kliknięciem w panelu, zanim do niej dojdzie." `README.md`'s landing-copy summary updated to match, with a pointer back to this decision. Zero remaining "bez karty" occurrences repo-wide (grep-verified before and after). `next build`-equivalent checks: `tsc --noEmit` and `eslint` both clean on the touched files (repo-wide lint noise is 100% pre-existing `.netlify/` build-artifact warnings, same disclosed fact as every prior marketing-repo ticket). Deployed: Netlify auto-deploy on push (commit `49497bc`), reached `state: ready` in ~30s; live-verified via `curl https://reviewguide.eu/` — zero "bez karty" occurrences, 8 occurrences of the new copy (hero-shared meta description × meta/OG/Twitter + pricing card + FAQ). **4. App repo audited, no changes needed**: grepped `reviewguide-app` for "bez karty"/trial copy — none exists; the only trial-related strings are the status label ("okres próbny") and the checkout button ("Rozpocznij okres próbny"), both instructed to stay as-is. **5. Live verification against the real deployed backend + real Stripe TEST mode** (bastion tunnel per CUTOVER.md §Step 0, for a throwaway `is_test=true` DB row, cleaned up after): minted a session JWT with the live App Runner `AUTH_JWT_SECRET` (pulled from Secrets Manager, not assumed to match the local `.env` — ticket 4.3's env-drift bug taught that lesson once already) and called the **live deployed** `POST /api/billing/checkout`; retrieved the resulting Checkout Session object directly from Stripe's API (not just re-reading our own request) and confirmed `payment_method_collection: "always"` on the real object Stripe will render its hosted page from. **Hosted-Checkout browser click-through itself was not driven by browser automation** (same disclosed substitution as ticket 4.3's original live verification) — instead attached a real test card (`tok_visa`, the API equivalent of `4242 4242 4242 4242`) as the customer's default payment method and created a real `Subscription` object against it directly, which is indistinguishable to Stripe from one hosted Checkout would have produced and fires the identical webhook. Result: `subscription.status == "trialing"`, `subscription.default_payment_method` set to the real attached card — **exactly the required post-checkout state**. The **live webhook** (registered against the real App Runner URL, not simulated) picked this up within the 5-second poll window: `GET /api/billing/status` against the live backend reflected `trialing` with no manual intervention. **Portal**: `GET /api/billing/portal` against the live backend returned a genuine `billing.stripe.com` session URL. **Cancel**: cancelled the subscription via the Stripe API (the same underlying action the portal's own cancel button performs) and confirmed the live webhook flipped `GET /api/billing/status` back to `"none"` — proving the webhook's deletion-handling path is live and correct, which is what "portal cancel works" ultimately depends on end-to-end. Test Stripe customer and DB row deleted after verification (same hygiene as tickets 4.2/4.3/4.5). **6. Doc-parity + suite**: no doc-parity test binds `SPRINT_04.md`'s original ticket-4.3 spec text to `billing.py` (confirmed — unlike the prompt files, which do have dedicated `test_prompts*.py` pins), so that historical ticket text is left as the record of what 4.3 originally shipped rather than rewritten; this CR's own record lives here and in `SPRINT_06.md`. `docs/LOGIC.md` has no card-related language to update either (grep-confirmed). Full backend suite: **352/353** (`test_health`'s pre-existing local-DB-connectivity gap when the tunnel isn't open at test-run time, same as every prior report, not from this change), `ruff check` clean. | Pending PM review — full evidence above; the one open item is the Stakeholder's own Dashboard toggle (not something Cursor can flip via API). |

### Sprint 6 open questions for Stakeholder
- **Enable Stripe's trial-ending reminder email** (Dashboard-only, no API path — see CR-1's notes
  for the research): **Settings → Billing → Subscriptions and emails → "Send a reminder email 7
  days before a free trial ends"** (`dashboard.stripe.com/settings/billing/automatic`), and enable
  **"Link to a Stripe-hosted page"** alongside it so the reminder email also carries a
  payment-update link — this is the card-network compliance requirement CR-1's scope item 2 named.

---

## Review cycle log

| Date | Cycle | What was reviewed | Outcome | Follow-ups |
|---|---|---|---|---|
| 2026-07-29 | Kickoff | Docs created (WORKFLOW, ROADMAP, PROGRESS, BACKLOG, SPRINT_00) | Plan approved pending Stakeholder read | Stakeholder: approve Sprint 0 scope + answer G1 |
| 2026-07-29 | Decisions | Stack + G1 decided together (see ROADMAP §4b): AWS, 2-repo frontend, Netlify, Postmark, Stripe, Sonnet 5, Warsaw | Docs updated: ROADMAP stack+gates, SPRINT_00 tickets 0.2/0.5 | Stakeholder: create AWS/Outscraper/Anthropic accounts, then start ticket 0.1 |
| 2026-08-05 | Sprint close | Sprint 0 — Foundations: all 5 tickets (0.1-0.5) | Sprint accepted and closed. Milestone demonstrated: live App Runner deploy, `/health` green with DB, fake lead seeded in prod DB | Stakeholder: fund Outscraper balance before Sprint 1; PM/Cursor: scope Sprint 1 |
| 2026-08-05 | Sprint close | Sprint 1 — Data Pipeline: all 5 tickets (1.1-1.5) | Sprint accepted and closed. Milestone demonstrated: 213 qualified leads in prod DB (target 100+), $16.56 actual spend (budget ~$20-25). Two live-found LOGIC.md refinements (zatru regex, sub-area discovery) and one live-found SDK batching bug (HTTP 414, fixed + hardened) shipped along the way | PM/Cursor: scope Sprint 2 |
| 2026-08-07 | Sprint close | Sprint 4 — Product Foundation: all 5 tickets (4.1-4.5) plus follow-up 4.2b | Sprint accepted and closed, opened and closed the same day. Milestone demonstrated on the Stakeholder's own device: `reviewguide.eu` → magic-link email → interstitial → `/app` → hosted Stripe Checkout → webhook → `trialing`, in 96 seconds, evidenced from four independent sources. Sprint 0's hard gate honored before the first customer row existed: security cutover executed live (private RDS + NAT + Secrets Manager). Three bugs caught by the milestone walkthrough itself (stale `APP_ORIGIN`, double-tap double-submit, and a double-submit guard that cancelled the very submission it was guarding) — all root-caused from data and fixed same-day; the third also closed a standing test gap by adding Playwright browser coverage to `reviewguide-app` | Stakeholder: decide G3 launch price (now due, and already live at 129 zł); grant Netlify GitHub App access to `reviewguide-marketing`. PM/Cursor: stand down — remaining board items (2.4/2.5 PL reviewer, 3.5 sending week) are external-gated |
| 2026-08-09 | Sprint close | Sprint 2 — outreach quality gate (plan B) + tickets 2.4/2.5: PM full read of all 40 v1.2 responses (39 SEND / 1 EDIT / 0 BAD; health-flagged all clean) + Stakeholder confirmation, logged honestly as NOT native-verified (see docs/ROADMAP.md decisions log, 2026-08-09 row) | **2.4 → ✅ ACCEPTED (PM, 2026-08-09) — template approved via plan-B joint review, assembly executed.** `app/templates.py` `TEMPLATE_APPROVED_ON = "2026-08-09"`. Lead 46 (Thai Me Up, batch #26) regenerated under prompt v1.2.1 for the one EDIT verdict (rule-5 admission language in the old response — "przepraszamy za jakość"/"standard nie był na odpowiednim poziomie" class); new response verified to contain neither phrase class, automated checks clean (104 words, no signature/denial/truncation). **2.5 → ✅ ACCEPTED with Stakeholder scope amendment: first batch 100 leads; remaining 113 on demand per RUNBOOK.** `generate --limit 60 --yes`: 60/60 generated, 0 failures, $0.34 (100-response batch checks: 0 truncated, 0 signatures, 0 >130 words, 1 denial-wording flag for human look — lead 76 Mezza Lounge & Bar, "Zapewniamy, że..."). `enrich --yes`: 42 domains, 37 emails + 33 Facebook URLs found, 55 promoted to enriched, $0.13. `assemble_outreach` (gate now open): **87 queued** (facebook 58, email 6, contact form 23), **6 health-flagged excluded** (never queued), **7 left with no channel** (held at `response_generated`). DB reconciles exactly: 213 total = 113 `new` + 7 `response_generated` + 6 `enriched` + 87 `queued`. Session spend: Claude $0.35 (regenerate $0.01 + batch $0.34), Outscraper $0.13. All via the bastion tunnel (CUTOVER.md §Step 0). Full test suite 347/347 green (1 test needed real network, unrelated to this change — passes with it), `test_template_is_not_marked_approved...` renamed to `test_template_is_approved` and re-pinned to the real date; `test_unapproved_template_blocks_every_write` now explicitly patches the gate to `None` since the module default is no longer `None`. | Sprint 2 closed (5/5 tickets ✅) — see Closed sprints below. PL-native reviewer read still outstanding, revisit at first real replies (ROADMAP decisions log). Sending is manual per LOGIC §6 (10–20/day cap, Stakeholder picks daily) — 86 queued leads are ready whenever sending starts (ticket 3.5). |
| 2026-08-09 | PM read of the 60-lead batch | PM verdicts: #26 Thai Me Up regeneration → SEND (cleared); 60-lead batch → clean; all pipeline numbers accepted. One hold raised: **lead 76 (Mezza Lounge & Bar)** — the review alleges theft and physical harassment by other patrons ("okradziona z telefonu i pieniędzy", "sprawa jest na policji"), legal-gravity content outside the current health-flag net (LOGIC §2 only matches food-safety keywords) | Lead 76 pulled from the queue: status set back to `enriched` (direct DB fix — **disclosed:** `queued → enriched` has no edge in `app/routers/admin.py`'s `ALLOWED_TRANSITIONS`, so this could not have been done through the dashboard's own PATCH endpoint as-is; done as a one-off administrative correction, same category as prior manual DB fixes, e.g. ticket 3.4's baseline-restore), `notes = "PM-HOLD: serious-allegation review — human clearance required before queue"` so it cannot be queued/sent from the dashboard until a human clears it. Sendable queue corrected: 213 total = 113 `new` + 7 `response_generated` (no channel) + 7 `enriched` (6 health-flagged + 1 PM-hold, lead 76) + **86 `queued`** (58 facebook / 6 email / 22 contact form). Added to `docs/BACKLOG.md`: extend §2-style keyword gating to crime/theft/harassment/discrimination classes so this never has to be caught by hand again (earliest Sprint 6). | Stakeholder/PM: clear or kill lead 76 by hand whenever convenient — it will not surface on `/admin` as sendable until then. Sprint 6 planning: scope the serious-allegation keyword-gating ticket from BACKLOG. Longer-term, `ALLOWED_TRANSITIONS` has no legal "pull back for human review" edge (`queued`/`sent` → `enriched`) — today's fix bypassed it directly; worth a real edge if this kind of hold recurs. |
| 2026-08-09 | PM decision block — ticket 5.8 + lead 76 | Stakeholder finding that System B drafts full-length responses for star-only reviews, worked as ticket 5.8; plus the PM's read of lead 76's full review, response and outreach message (pasted in chat) | **5.7 → ✅ ACCEPTED**, **5.8 → ✅ ACCEPTED** (v1.4 branch text approved verbatim, `MAX_TOKENS=1200` approved with the thinking-token discovery commended, v1.4.1's PL default + city-derived persona approved, migration 008 approved) — see both ticket rows for the full evidence. **Lead 76 → `dead`**, `notes = "PM verdict 2026-08-09: review alleges organized crime with active police case — outside product scope entirely; no outreach appropriate in any wording. Do not resurrect."` Worth recording the contrast with this morning's entry above: `enriched → dead` **is** a legal edge in `ALLOWED_TRANSITIONS`, so this one went through the real `PATCH /api/admin/leads/76` endpoint with the guard enforcing it, rather than the direct DB write the earlier `queued → enriched` pull-back required. Sendable queue stands at **86**; DB reconciles 213 total = 113 `new` + 86 `queued` + 7 `response_generated` + 6 `enriched` + 1 `dead`. | Sprint 5 sits at 8/8 tickets ✅ pending only the Stakeholder's inbox confirmation for 5.5 (the alert + both backfilled digests), which is the last gate on the sprint close. |
| 2026-08-09 | Sprint close | Sprint 5 — Value Delivery: Stakeholder inbox confirmation of the 5.5 milestone (the 22:00 CEST PILNE alert plus the two 5.7-backfilled digests, 10 drafts each, all delivered and readable) | **5.5 → ✅ ACCEPTED (PM+Stakeholder, 2026-08-09)** — "milestone demonstrated: organic new review caught by an unattended scheduled tick, urgent-flagged, alerted end-to-end; digests delivered via 5.7's sweep." Sprint 5 closed at **8/8 tickets ✅** — see Closed sprints below. | Sprint 5 closed. Remaining board: ticket 3.5 (first real sending week, Stakeholder-paced per LOGIC §6's 10–20/day cap) and Sprint 6 planning (PM+Stakeholder session) — both external-gated, no further build work queued. |
| 2026-08-09 | CR opened | CR-1 — trial payment model cardless → card-upfront (Stakeholder decision, logged in ROADMAP.md decisions log) | Built, live-verified against the real deployed backend + real Stripe TEST mode, and deployed (backend to App Runner, marketing to Netlify) same session — see the CR-1 row in the new Sprint 6 section for full evidence. Ready for PM review. | PM: review CR-1's evidence. Stakeholder: enable Stripe's Dashboard-only trial-ending reminder email (exact toggle path in the CR-1 row / Sprint 6 open questions) — the one item this CR could not do via API. |
| 2026-08-09 | Ticket opened (filed as Sprint 4 amendment) | 4.6 — card-upfront copy finalization + brand assets + section elevation, re-reading SPRINT_04.md + ROADMAP §4b | Landing copy finalized with new exact wording (pricing card, FAQ, hero third line), superseding CR-1's copy strings; full brand asset set generated from the Stakeholder-provided `icon-source.png` (favicon, PWA icons, apple-touch, OG image, with an actually-rendered-and-compared flat-silhouette favicon decision) and wired into both frontend repos' Metadata APIs plus both navbars; three landing sections (demo block, three-step cards, pricing card) visually elevated per spec, `prefers-reduced-motion`-guarded. Builds clean on both frontend repos, backend suite 353/353 with the bastion tunnel open, zero "bez karty" hits repo-wide, Lighthouse 88 mobile/100 desktop (targets 85/90). Full evidence in the 4.6 row above. Ready for PM review. | PM: review 4.6's evidence (screenshots + favicon comparison shown in-chat this session). |

---

## Closed sprints

| Sprint | Summary | Dates |
|---|---|---|
| Sprint 0 — Foundations | 5/5 tickets accepted. Milestone demonstrated: live AWS App Runner deployment, `GET /health` green (including DB), fake lead seeded into prod DB via `scripts/seed_fake_lead.py`. Hard gate logged for Sprint 4: move RDS to private VPC + NAT and adopt Secrets Manager before any real customer data exists (see ROADMAP §4b, BACKLOG). | 2026-08-04 → 2026-08-05 |
| Sprint 1 — Data Pipeline | 5/5 tickets accepted (1.1 Outscraper client + cost guard, 1.2 discovery job, 1.3 review fetch job, 1.4 lead qualification, 1.5 pipeline runner milestone). **Milestone met: 213 qualified leads in the production DB vs. a 100+ target**, at **$16.56 actual Outscraper spend vs. a ~$20-25 approved budget** (verified against Outscraper's own billed-usage history, not just our internal cost-guard estimate). Two LOGIC.md refinements shipped from live findings: v1.1 two-tier health-keyword matching (fixed a "rat"-in-"akurat" false positive) and v1.2's `zatru(?!dni)` regex (fixed a "zatru"-in-"zatrudnieniu" false positive). One real SDK/infra bug found and fixed: an HTTP 414 "URI Too Long" in `fetch_reviews` from batching too many `place_ids` into a single GET request's query string — fixed by lowering the batch size and hardening the job to commit each batch immediately so a later failure can't discard already-paid-for progress. Discovery scope also had to move from one query per district to a list of named sub-area queries after finding Google Maps hard-caps a single query at ~120 listings. | 2026-08-05 |
| Sprint 4 — Product Foundation | 5/5 + 4.2b tickets ✅, opened and closed 2026-08-07: landing live on reviewguide.eu, magic-link auth (prescan-proof), Stripe test-mode billing loop, security cutover EXECUTED (private RDS + NAT + Secrets Manager, ~$37/mo live), app.reviewguide.eu. Three bugs caught by milestone walkthrough. Hard gate from Sprint 0 honored before first customer row. **Amendment 4.6 landed 2026-08-09** (post-close, same pattern as 4.2b): card-upfront copy finalized, full brand asset set (favicon/icons/OG) generated and wired, three landing sections visually elevated. | 2026-08-07 (+4.6 on 2026-08-09) |
| Sprint 2 — AI Generation + Enrichment | 5/5 tickets accepted. Prompt evolved v1 → v1.1 → v1.2 → v1.2.1 through four live tuning rounds (language-match fix, per-language word budget, signature/truncation/denial automated checks, third-language KROK 0 line). Outreach quality gate closed via **plan-B internal joint review** (PM full read of all 40 responses — 39 SEND / 1 EDIT / 0 BAD — + Stakeholder confirmation), explicitly logged as **not native-verified**; revisit prompt/template at first real replies (ROADMAP decisions log, 2026-08-09). Final: 100/213 leads generated (40 tuning batch + 60 more, Stakeholder-paced first batch; remaining 113 on demand per RUNBOOK), 86 queued and sendable (58 facebook / 6 email / 22 contact form) after lead 76 (serious-allegation review) was pulled to a PM-hold same day, 6 health-flagged held back, 7 with no reachable channel. Sprint spend: Claude ≈ $0.99, Outscraper ≈ $0.31 (well under LOGIC §4 caps throughout). | 2026-08-05 → 2026-08-09 |
| Sprint 5 — Value Delivery | 8/8 tickets ✅. The paid product went live end-to-end: connect flow + day-one value, 2h polling via EventBridge classic rules (architecture pivot from the originally-planned Scheduler + async-202 refactor + in-process run-lock, all found and fixed from live scheduler-tick failures), customer panel (connect/settings/alerts, Stripe checkout-guard fix), PL alert/digest emails (localhost-link + injection-boundary fixes), admin customers view, retry/backfill sweep (self-heals any stuck-unsent alert/digest class of bug), and a prompt line star-only + persona + language fix (v1.2.1 → v1.4.1, thinking-token root cause found — Claude's internal `thinking` tokens were silently eating the `max_tokens` budget on sparse input). **First autonomous alert 2026-08-08 22:00 CEST** — an organic new review caught, drafted, and emailed with no human involved at any step; digests for both test customers delivered via 5.7's backfill sweep at the very next unattended tick. Milestone confirmed in the Stakeholder's own inbox 2026-08-09. | 2026-08-07 → 2026-08-09 |

---

## Running metrics (updated at each sprint close)

| Metric | Value | As of |
|---|---|---|
| Qualified leads in DB | 213 | 2026-08-05 |
| Outreach messages sent | 0 (86 queued for manual daily sending per LOGIC §6, 10–20/day cap — ticket 3.5; the PM-HOLD lead 76 was killed outright, `dead`, not pending) | 2026-08-09 |
| Replies / positive replies | 0 / 0 | 2026-08-05 |
| Trial signups / paying | 2 / 0 — **0 real / 2 test** (`customer_id` 13 `pedram@defraged.com` and 14 `pedram@reviewguide.eu`, both Stripe TEST mode, both flagged `customers.is_test` per migration 007); neither is a real customer — 13 is retained as ongoing milestone/poller evidence, 14 is the Stakeholder's own walkthrough account (Istanbul Kitchen, San Leandro CA) | 2026-08-09 |
| Outscraper spend (month) | ~$21.04 ($20.73 Sprint 1 + $0.18 ticket 2.3 + $0.13 ticket 2.5's `enrich --yes`, verified via `get_requests_history()`) | 2026-08-09 |
| Claude spend (month) | ~$1.16 (Sprint 2 generation: $0.19+$0.21+$0.23+$0.01 tuning rounds v1→v1.2.1 + $0.01 ticket-2.4 Thai-Me-Up regenerate + $0.34 ticket 2.5's 60-lead batch; Sprint 5 ticket 5.8 ≈ $0.17 across 20 calls: 1 to root-cause the thinking-token truncation, 3 to live-verify the v1.4 branch, 9 for the star-only regeneration, 3 for the v1.4.1 language dry-run, 1 for the Kraków persona check, 3 for the v1.4.1 regenerations of alerts 21/22/4) | 2026-08-09 |
| Infra cost (month) | **~$55-65/mo** — AWS ~$55/mo (RDS + App Runner + NAT gateway ~$37 + `t4g.nano` bastion, up from ~$15-17 pre-cutover, the NAT gateway being the bulk of the Sprint 0 hard-gate increase) **+ Netlify Personal plan ~$9/mo** (upgraded from Free during ticket 5.3 after a deploy was blocked on account-credit usage — the first recurring cost on that side of the stack, not previously tracked here) | 2026-08-09 |

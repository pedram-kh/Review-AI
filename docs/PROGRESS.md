# PROGRESS.md — Living Log

> THE single source of truth for what is DONE, IN PROGRESS, and REMAINING.
> - **Cursor**: update your ticket's row in the SAME commit as the work. Work without a log entry = not done.
> - **PM (Claude)**: adds the Verdict column entries during review. Only the PM writes verdicts.
> - **Stakeholder**: re-uploads this file to the Claude Project after every review cycle.
>
> Status legend: ⬜ todo · 🔨 in progress · 🧪 ready for review · ✅ accepted · 🔧 changes requested · ❌ rejected · ⏸ blocked

---

## Current sprint: SPRINT 1 — Data Pipeline

| # | Ticket | Status | Files touched | Cursor notes | PM verdict |
|---|---|---|---|---|---|
| 1.1 | Outscraper client + cost guard | ✅ | app/services/outscraper_client.py, app/services/cost_guard.py, tests/test_cost_guard.py, tests/test_outscraper_client.py | `cost_guard.py` implements LOGIC.md §4 exactly: `MAX_PLACES_PER_RUN=1000`, `MAX_REVIEW_RECORDS_PER_RUN=12000`, `$3/1k places`, `$3/1k review records`; `enforce_caps()` raises `CostCapExceeded` before any API call, `estimate_cost()` is the pure cost math (also usable standalone for pre-flight `--yes`-less estimates in tickets 1.2/1.3). `OutscraperClient.search_places(query, limit)` and `.fetch_reviews(place_ids, reviews_per_place=10)` both call `enforce_caps()` as the first line, before touching the SDK — verified in tests that the mocked SDK method is never called when a cap would be breached. Wraps `outscraper.ApiClient.google_maps_search`/`.google_maps_reviews` (inspected the installed SDK's actual method signatures — there's no separate "search"/"reviews" class, just `ApiClient` with per-endpoint methods) and flattens Outscraper's per-query response wrapping into a plain `list[dict]`. No script calls the client directly — only importable, ready for jobs in 1.2-1.5. 12/12 tests pass (6 cost_guard, 5 outscraper_client, 1 pre-existing health), all with a mocked SDK — zero real Outscraper calls/spend. `ruff` passes. | ✅ ACCEPTED (PM, 2026-08-05) — caps verified to abort pre-call via mocked-SDK not-called assertions. SDK inspection + _flatten disclosed and approved. |
| 1.2 | Discovery job (Śródmieście pilot) | ✅ | app/jobs/discover.py, app/jobs/__init__.py, app/config.py, tests/test_discover.py | `python -m app.jobs.discover --district srodmiescie [--limit N] [--yes]`. District→query mapping lives in `app/config.py` `DISTRICT_QUERIES` (currently just `srodmiescie` → `"restaurants, Śródmieście, Warszawa, Polska"`; LOGIC.md §8's other districts can be added there with no other code change). `--limit` defaults to 1000 and is passed straight into `OutscraperClient.search_places`, which enforces the cost cap itself (ticket 1.1) — `discover.py` also calls `enforce_caps` up front so it can print a clean pre-flight message and exit 1 *before* even constructing the client. Without `--yes`: prints district/query/limit/estimated cost and exits 0, zero API calls. With `--yes`: upserts into `places` via `ON CONFLICT (place_id) DO UPDATE` on name/address/city/phone/website only — `last_polled_at` is excluded from the update `set_` (and never set on insert either — it's ticket 1.3's job to set it), so it's preserved exactly as specified. **Live smoke test (2026-08-05, within Outscraper's free 500-records/month tier — postpaid billing confirmed, $0 balance is normal):** `--limit 50 --yes` → 50 found/inserted, real Warsaw restaurant data landed correctly (verified 3 sample rows: name/address/phone/website all populated, `last_polled_at` NULL as expected). **Field mapping tightened after live inspection:** the ticket's original guess (`full_address`, `site`) was wrong — Outscraper's actual `google_maps_search` response uses `address` and `website` (confirmed via a 3-record inspection call, well within the free tier); removed the dead fallback keys and left a comment recording the confirmed field names. Re-ran the identical command: got **5 inserted / 45 updated** (not "0 new" as expected) — verified this is real Google Maps search non-determinism (ranking near the `--limit 50` boundary shifts slightly between calls on ~600+ candidate restaurants in the area), NOT an upsert bug: total place count grew by exactly 5 (51→56), zero duplicate `place_id` rows, and `last_polled_at` stayed NULL on all real rows across both runs — the PK-based upsert is correctly idempotent even though the API's result set itself isn't perfectly stable. Outscraper dashboard usage confirmation is a manual Stakeholder-side check per the Sprint 1 review checklist (the request-history API endpoint doesn't surface sync/fast-endpoint calls like this one). 19/19 tests pass (updated fixtures to match the real field names), `ruff` clean. | ✅ ACCEPTED (PM, 2026-08-05) — live smoke test passed within free tier; upsert + field mapping verified against real data. Defensive field-reading approved. Postpaid billing confirmed. |
| 1.3 | Review fetch job | ✅ | app/jobs/fetch_reviews.py, tests/test_fetch_reviews.py | `python -m app.jobs.fetch_reviews [--all] [--yes]`. Default scope = `places WHERE last_polled_at IS NULL`; `--all` re-polls every place instead. Requests `DEFAULT_REVIEWS_PER_PLACE` (10) newest reviews per place via `OutscraperClient.fetch_reviews`, batching place_ids into chunks of 250 per API call (Outscraper's documented max queries/request) — the cost-cap check runs once up front against the *total* selected-place count before any batching, so a large job can't sneak past the 12,000-record cap one small batch at a time. Upsert on `review_id` via `ON CONFLICT DO UPDATE`; `places.last_polled_at` stamped to `now()` only for place_ids that actually appear in the response. **Field mapping verified live *before* writing it** (tiny real calls against already-discovered places): used `review_timestamp` (Unix epoch) over the ambiguous `review_datetime_utc` string; confirmed `has_owner_reply = bool(owner_answer)`. **Live smoke test (2026-08-05):** `--yes` on all 55 real un-polled places from ticket 1.2 → 55 polled, 550 reviews inserted, $1.65 (within free tier). Verified: (1) 3 sample rows have correct name/rating/text/date/has_owner_reply; (2) distribution — 550 real reviews stored, 45 with rating ≤3, 35 of those with `has_owner_reply=false` (the future lead pool from this batch); (3) `review_date` range 2025-09-05→2026-08-05, sane, not epoch-zero; (4) `last_polled_at` now set on exactly the 55 real places (0 remain NULL); (5) re-run without `--yes` found 0 un-polled places, printed "Nothing to poll" and made no API call (equivalent to the expected $0/no-op, via the early-return path rather than a literal "$0" print). 32/32 tests pass, `ruff` clean. | ✅ ACCEPTED (PM, 2026-08-05) — live-verified on 55 places; field mapping built from real response inspection; cost scoping correct. |
| 1.4 | Lead qualification filter | ✅ | app/jobs/qualify.py, app/logic_rules.py, tests/test_qualify.py, pyproject.toml, docs/LOGIC.md | `python -m app.jobs.qualify` — no args, no API calls, no cost. Scans every row in `reviews`; for each, checks (in order) Q6 (place already has a lead → skip), Q1 (rating ≤3), Q2 (`has_owner_reply is False` — literal false required, NULL does NOT count as "no reply"), Q3 (review_date within 30 days of *now*), Q4 (text length ≥80), Q5 (language ∈ {pl, en} via `py3langid`). Reviews passing Q1–Q5 are grouped by place; only the most recent per place is inserted (`ON CONFLICT (place_id) DO NOTHING`) — a qualifying-but-not-chosen review for the same place is tallied under `skipped_q6` (disclosed judgment call: shares the one-lead-per-place intent with Q6; LOGIC.md defines only 6 rule buckets). **LOGIC.md v1.1 two-tier health-keyword matching** (added after the live false-positive catch below): `HEALTH_KEYWORDS_WHOLE_WORD` (regex `\b` boundaries — short standalone words like `rat`/`mysz`/`mold`, singular+plural listed separately since a boundary doesn't fall between e.g. "rat" and "s") checked first, then `HEALTH_KEYWORDS_SUBSTRING` (stems/phrases like `zatru`, `salmonell`, `brudn` that must still catch inflections) — both live in `app/logic_rules.py` mirroring LOGIC.md §2 exactly. **Live verification (2026-08-05, 551 reviews):** first run found 1 health-flagged lead that was a **false positive** — `rat` matched inside the unrelated Polish word "akurat" ("as it happens") under the old single-tier substring rule. PM/Stakeholder approved two-tier matching (LOGIC.md v1.1, changelog entry added); after implementing it, deleted the 17 smoke-test leads (`DELETE FROM leads;`, explicit approval given) and re-ran: **17 leads created again, 0 health flags** — "Podwale 25 Kompania Piwna" (the false-positive lead) now correctly has no flag, verified new tests confirm "akurat" doesn't match while "szczury" (plural) and "zatrułam" (inflected stem) do. 17 tests for this ticket (49/49 total pass), `ruff` clean. | ✅ ACCEPTED (PM, 2026-08-05) — LOGIC.md v1.1 two-tier matching implemented after live false-positive catch; re-verified on real data. |
| 1.5 | Pipeline runner (milestone) | 🧪 | app/jobs/run_pipeline.py, app/jobs/discover.py, app/jobs/fetch_reviews.py, tests/test_run_pipeline.py, README.md | `python -m app.jobs.run_pipeline --district X [--limit N] [--yes]`. **Refactor first:** extracted `discover.run(district, limit, yes) -> dict` and `fetch_reviews.run(poll_all, yes) -> dict` out of each job's `main()` (which now just calls `run()` and prints — identical output, all pre-existing tests pass unchanged) so `run_pipeline.py` can call the same code paths directly and get structured results instead of re-implementing or shelling out. `qualify(session)` already returned a counters dict, no refactor needed there. Without `--yes`: prints a **worst-case pre-flight estimate** (`enforce_caps` for `limit` places + `limit*10` review records, i.e. assumes every requested place is new/unpolled since we can't know real DB overlap before `discover` actually runs) and exits — zero API calls, matching LOGIC.md §4. With `--yes`: runs discover → fetch_reviews (global scope, `poll_all=False` — picks up whatever `discover` just inserted plus any other unpolled places, since there's no district column to scope by) → `qualify` sequentially, aborting non-zero if either paid step's cap check fails; prints one final report (places found/inserted/updated, reviews fetched/polled, leads created, health-flagged, cost per step + total, wall time). All 3 steps individually idempotent, so pipeline re-runs are safe. Added a "Pipeline" section to `README.md`. **Live-verified (2026-08-05):** dry run at `--limit 50` printed the correct worst-case estimate ($1.65 = $0.15 discover + $1.50 reviews); `--limit 1501 --yes` correctly aborted pre-flight with the cap error, zero API calls in both cases. 7 new tests (56/56 total pass), `ruff` clean. **Milestone run (2026-08-05, Stakeholder-approved full spend):** `python -m app.jobs.run_pipeline --district srodmiescie --yes` (default `--limit 1000`) → pre-flight estimate $33.00 worst-case, actual cost **$2.52** (discover $0.36 + fetch_reviews $2.16). Results: 119 places returned by the query (72 new, 47 already known from earlier tickets — some overlap churn from Google's ranking non-determinism, same phenomenon as ticket 1.2), 720 new reviews fetched for the 72 new places, 26 new leads created (3 health-flagged), bringing running totals to **127 places / 1,270 reviews / 43 leads total**. Wall time 97s. **Milestone (100+ leads) NOT yet met** — flagging two real findings rather than silently declaring success: (1) **Discovery is capped far below LOGIC.md §8's ~600-800 expectation** — confirmed via Outscraper's own docs (websearch) that Google Maps hard-caps a *single query* at ~120 unique listings regardless of the requested `limit` (our `--limit 1000` had zero effect past that); Outscraper's documented fix is splitting one query into several sub-area queries (e.g. by street/postal code) rather than raising `limit`. This is an industry-wide Google Maps limitation, not a bug in our code, but it means reaching the sprint milestone needs a discovery-scope change (multiple sub-queries per district) that I have not implemented, since it touches LOGIC.md §8 sweep scope and deserves a PM/Stakeholder call rather than a unilateral change. (2) **A second health-keyword false positive**, same family as the "rat"/"akurat" one from ticket 1.4: LOGIC.md v1.1's tier-2 substring `zatru` (meant to catch zatrucie/zatrułem/zatrułam) also matched inside "**zatru**dnieniu" (Polish for "hiring/employment" — a reviewer suggesting the pub hire more staff, unrelated to food safety). The other 2 of the 3 new health flags (a literal "cockroach" and literal "mold" mention) are genuine. Flagging both for a PM/Stakeholder decision rather than fixing either unilaterally. | |

### Sprint 1 blockers
_(none yet)_

### Sprint 1 open questions for Stakeholder
- ~~Outscraper funded?~~ ✅ RESOLVED (2026-08-05): billing is postpaid (card on file), $0 balance is normal, first 500 records/service/month are free. No funding action needed.
- Ticket 1.5 milestone run executed (2026-08-05, $2.52 actual spend) but only reached 43 total leads, short of the 100+ target — root cause is Google Maps' ~120-listing-per-query cap (confirmed via Outscraper docs), not our `--limit`. Reaching 100+ leads needs discovery to run multiple sub-area queries per district (e.g. by street/postal code) instead of one big query — this is a LOGIC.md §8 sweep-scope change; needs a PM/Stakeholder decision on the sub-query strategy before Cursor implements it.
- New health-keyword false positive found in the milestone run: tier-2 substring `zatru` also matches "zatrudnieniu" (hiring/employment, unrelated to food poisoning). Needs a decision alongside the discovery-scope one: tighten `zatru` (e.g. add a negative lookahead for `dni`/`dnien` suffixes) or accept it given health-flagged leads already require human review before outreach.

---

## Review cycle log

| Date | Cycle | What was reviewed | Outcome | Follow-ups |
|---|---|---|---|---|
| 2026-07-29 | Kickoff | Docs created (WORKFLOW, ROADMAP, PROGRESS, BACKLOG, SPRINT_00) | Plan approved pending Stakeholder read | Stakeholder: approve Sprint 0 scope + answer G1 |
| 2026-07-29 | Decisions | Stack + G1 decided together (see ROADMAP §4b): AWS, 2-repo frontend, Netlify, Postmark, Stripe, Sonnet 5, Warsaw | Docs updated: ROADMAP stack+gates, SPRINT_00 tickets 0.2/0.5 | Stakeholder: create AWS/Outscraper/Anthropic accounts, then start ticket 0.1 |
| 2026-08-05 | Sprint close | Sprint 0 — Foundations: all 5 tickets (0.1-0.5) | Sprint accepted and closed. Milestone demonstrated: live App Runner deploy, `/health` green with DB, fake lead seeded in prod DB | Stakeholder: fund Outscraper balance before Sprint 1; PM/Cursor: scope Sprint 1 |

---

## Closed sprints

| Sprint | Summary | Dates |
|---|---|---|
| Sprint 0 — Foundations | 5/5 tickets accepted. Milestone demonstrated: live AWS App Runner deployment, `GET /health` green (including DB), fake lead seeded into prod DB via `scripts/seed_fake_lead.py`. Hard gate logged for Sprint 4: move RDS to private VPC + NAT and adopt Secrets Manager before any real customer data exists (see ROADMAP §4b, BACKLOG). | 2026-08-04 → 2026-08-05 |

---

## Running metrics (updated at each sprint close)

| Metric | Value | As of |
|---|---|---|
| Qualified leads in DB | 0 | 2026-08-05 |
| Outreach messages sent | 0 | 2026-08-05 |
| Replies / positive replies | 0 / 0 | 2026-08-05 |
| Trial signups / paying | 0 / 0 | 2026-08-05 |
| Outscraper spend (month) | $0 | 2026-08-05 |
| Claude spend (month) | $0 | 2026-08-05 |
| Infra cost (AWS, month) | ~$15-17/mo (RDS + App Runner) — now live | 2026-08-05 |

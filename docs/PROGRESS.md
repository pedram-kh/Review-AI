# PROGRESS.md — Living Log

> THE single source of truth for what is DONE, IN PROGRESS, and REMAINING.
> - **Cursor**: update your ticket's row in the SAME commit as the work. Work without a log entry = not done.
> - **PM (Claude)**: adds the Verdict column entries during review. Only the PM writes verdicts.
> - **Stakeholder**: re-uploads this file to the Claude Project after every review cycle.
>
> Status legend: ⬜ todo · 🔨 in progress · 🧪 ready for review · ✅ accepted · 🔧 changes requested · ❌ rejected · ⏸ blocked

---

## Current sprint: SPRINT 0 — Foundations

| # | Ticket | Status | Files touched | Cursor notes | PM verdict |
|---|---|---|---|---|---|
| 0.1 | Repo + FastAPI skeleton + .env config | ✅ | app/main.py, app/config.py, app/db.py, app/routers/health.py, tests/test_health.py, .env.example, .gitignore, pyproject.toml, README.md | Verified locally: `uvicorn app.main:app` → `GET /health` returns `{"status":"ok"}`; `pytest` and `ruff check .` both pass. Dev machine only had Python 3.9; installed Python 3.11 via Homebrew to match the required runtime — no code depends on this, just noting for the record. | ✅ ACCEPTED (PM, 2026-08-04) — env fixes approved (py3.11 via brew, gitignore negation for __init__.py). No scope creep. |
| 0.2 | Postgres provisioned + connection from app | ✅ | app/db.py, app/routers/health.py, scripts/check_db.py, tests/test_health.py, pyproject.toml | Added SQLAlchemy 2.x sync engine + session factory in app/db.py (psycopg2-binary driver). `GET /health` now runs `SELECT 1` and returns `{"status":"ok","db":"ok"/"error"}`. `scripts/check_db.py` prints connection status. Verified live against reviewpilot-db: script prints `db: ok`, `/health` returns `{"status":"ok","db":"ok"}`, `pytest`/`ruff` pass. Updated the existing `/health` test since the response shape changed (no longer an exact `{"status":"ok"}` match). | ✅ ACCEPTED (PM, 2026-08-04) — live-verified against RDS. Test update disclosed and approved. |
| 0.3 | Schema v1: places, reviews, leads (+ migration) | ✅ | app/models.py, alembic.ini, alembic/env.py, alembic/versions/001_schema_v1_places_reviews_leads.py, pyproject.toml, README.md | Schema matches the ticket exactly: 3 tables, exact columns/types, PK/FK as specified, `UNIQUE(place_id)` on leads present, `lead_id` is a true Postgres SERIAL. No extra columns, no extra tables, no indexes beyond PK/FK/UNIQUE. One disclosed judgment call: FK columns (`reviews.place_id`, `leads.place_id`, `leads.review_id`) are NOT NULL — the ticket schema notation didn't state this explicitly, but a nullable FK would break the data model (reviews/leads must reference something); flagging for PM review rather than guessing silently. Alembic wired to read `DATABASE_URL` from `.env` (no hardcoded creds). Verified live: `alembic upgrade head` ran cleanly against reviewpilot-db; inspected the resulting schema via SQLAlchemy `inspect()` and confirmed columns/PK/FK/UNIQUE match exactly. `pytest`/`ruff` pass (migrations dir excluded from ruff — standard practice for autogenerated files). | ✅ ACCEPTED (PM, 2026-08-04) — schema live-verified via inspect(). NOT NULL on FK columns explicitly approved (correct integrity call, properly disclosed). |
| 0.4 | Outscraper + Anthropic keys wired, /health checks both | ✅ | scripts/check_apis.py, pyproject.toml | `scripts/check_apis.py` verifies both keys, prints one line per service, exits non-zero if either fails. Anthropic: single `messages.create` call, model `claude-sonnet-5`, `max_tokens=10` (cost guard — no loops, verified truncated reply). Outscraper: their SDK has no method for `/profile/balance` (checked its client's method list — only scraping endpoints), so I called the documented endpoint directly with `httpx` (matches Outscraper's own docs example); added the `outscraper` package to pyproject anyway per the ticket's "add both SDKs/deps" instruction since Sprint 1's data pipeline will use it. Verified live: `outscraper: ok (status=valid, balance=0.0)`, `anthropic: ok (reply='Pong! How can I help you today')`, exit code 0. `pytest`/`ruff` pass. | ✅ ACCEPTED (PM, 2026-08-04) — cost guard verified live (truncated reply = max_tokens working). SDK workaround for balance endpoint approved. Note: Outscraper balance $0 — Stakeholder to fund before Sprint 1. |
| 0.5 | Deploy to server; seed script writes fake lead to prod DB | 🧪 | app/config.py, Dockerfile, .dockerignore, apprunner.yaml, scripts/seed_fake_lead.py, scripts/wipe_fakes.py, README.md, docs/ROADMAP.md, docs/BACKLOG.md, .python-version (removed) | **Live at `https://ytjgivwddf.eu-west-1.awsapprunner.com`** (App Runner service `reviewpilot-backend`, `eu-west-1`, 0.25 vCPU/0.5 GB, GitHub connection `reviewpilot-github`, auto-deploy on push to `main`). `GET /health` → `{"status":"ok","db":"ok"}` HTTP 200, verified. `scripts/seed_fake_lead.py` run locally against prod `DATABASE_URL` — confirmed `FAKE-001` place+review+lead present. **RDS hardening (per approved decision):** custom parameter group `reviewpilot-pg-force-ssl` (`rds.force_ssl=1`) attached + rebooted, auto minor version upgrade enabled, security group widened to `0.0.0.0/0` on 5432 — live-verified a non-SSL connection is rejected (`no encryption` error) while `sslmode=require` still works. **Scope amendment (Stakeholder+PM, disclosed):** Secrets Manager + App Runner instance role deferred to Sprint 4 hardening batch (logged as a hard gate in `docs/ROADMAP.md` §4b and `docs/BACKLOG.md`, alongside the RDS-to-private-VPC gate) — `DATABASE_URL`/`OUTSCRAPER_API_KEY`/`ANTHROPIC_API_KEY` are set directly as App Runner `RuntimeEnvironmentVariables` for now; `app/config.py`'s `AWS_SECRETS_NAME` fallback stays in place unused for that future switch. **Technical deviation from ticket wording, disclosed:** `apprunner.yaml` is committed but not actually read by the live service — App Runner's `ConfigurationSource: REPOSITORY` (which reads `apprunner.yaml`) ignores API/console-provided env vars, and `apprunner.yaml` can only hold literal secret values, which would mean committing secrets to the repo. Used `ConfigurationSource: API` instead (same GitHub connection, same build/run commands, no Docker needed — Docker Desktop still unavailable locally) so secrets could be passed via `RuntimeEnvironmentVariables` without touching the repo. **Three real AWS bugs hit and fixed during deploy** (all disclosed, all documented in README for future reference): (1) a committed `.python-version` file breaks App Runner's Python 3.11 "revised build" with a generic `pre_build` failure — removed it; (2) the build image only has `pip3`/`python3`, not `pip`/`python`; (3) the revised build silently drops anything installed outside `/app`, so a plain `pip3 install .` leaves `uvicorn` unreachable at runtime — fixed by installing to `--target=/app/deps` and setting `PYTHONPATH=/app/deps`. `pytest`/`ruff` pass. | |

### Sprint 0 blockers
_(none yet)_

### Sprint 0 open questions for Stakeholder
- ~~G1: which city?~~ ✅ ANSWERED: Warsaw multi-district, central districts first (see ROADMAP §4)
- Stakeholder to create: ~~AWS account resources (RDS)~~ done (see Sprint 0 notes), Outscraper account, Anthropic API account

### Sprint 0 notes
- 2026-08-04: RDS `reviewpilot-db` provisioned in eu-west-1 (db.t4g.micro, PostgreSQL 18.4, 20 GB gp3, storage-encrypted, deletion-protected, Single-AZ, 1-day backup retention, ~$15/mo). Ticket 0.2's "Stakeholder action first" (create RDS Postgres, put `DATABASE_URL` in `.env`) is done — `DATABASE_URL` is set in `.env`, `SELECT 1` verified against the live instance.
- 2026-08-05: Backend deployed to AWS App Runner (`reviewpilot-backend`, eu-west-1, 0.25 vCPU/0.5 GB, ~$5-6/mo). RDS hardened for the interim public-access posture (force SSL via parameter group, security group opened to `0.0.0.0/0` on 5432, auto minor version upgrade on) — **hard gate logged for Sprint 4 start**: move RDS to private VPC connector + NAT before any real customer data exists (see ROADMAP §4b, BACKLOG). Secrets Manager + App Runner instance role also deferred to that same Sprint 4 hardening batch (Stakeholder+PM approved amendment) — env vars are set directly on the App Runner service for now.

---

## Review cycle log

| Date | Cycle | What was reviewed | Outcome | Follow-ups |
|---|---|---|---|---|
| 2026-07-29 | Kickoff | Docs created (WORKFLOW, ROADMAP, PROGRESS, BACKLOG, SPRINT_00) | Plan approved pending Stakeholder read | Stakeholder: approve Sprint 0 scope + answer G1 |
| 2026-07-29 | Decisions | Stack + G1 decided together (see ROADMAP §4b): AWS, 2-repo frontend, Netlify, Postmark, Stripe, Sonnet 5, Warsaw | Docs updated: ROADMAP stack+gates, SPRINT_00 tickets 0.2/0.5 | Stakeholder: create AWS/Outscraper/Anthropic accounts, then start ticket 0.1 |

---

## Closed sprints

_(none yet — Sprint 0 in progress)_

---

## Running metrics (updated at each sprint close)

| Metric | Value | As of |
|---|---|---|
| Qualified leads in DB | 0 | — |
| Outreach messages sent | 0 | — |
| Replies / positive replies | 0 / 0 | — |
| Trial signups / paying | 0 / 0 | — |
| Outscraper spend (month) | $0 | — |
| Claude spend (month) | $0 | — |
